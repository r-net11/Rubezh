<Window x:Class="Runner.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:view="clr-namespace:Runner.Views"
    WindowStartupLocation="CenterScreen"
    Title="Runner" Height="800" Width="1200">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Background="Salmon">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button Content="Load" Command="{Binding LoadCommand}" />
            </StackPanel>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <StackPanel Margin="5">
                    <view:DataView DataContext="{Binding dataViewModel}" />
                </StackPanel>
            </StackPanel>
            <Canvas Grid.Column="1" Width="800" Height="600" Background="Wheat" Name = "RunnerCanvas" />
        </Grid>
        <view:ControlDetailsView DataContext="{Binding ControlDetails}" />
    </StackPanel>
</Window>