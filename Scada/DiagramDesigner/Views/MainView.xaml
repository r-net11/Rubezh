<Window x:Class="DiagramDesigner.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:s="clr-namespace:DiagramDesigner"
        WindowStartupLocation="CenterScreen"
        Title="SCADA Designer"
        FontFamily="SegoeUI"
        Height="850"
        Width="1100" Focusable="False">
    <DockPanel Margin="0">
        <Grid Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230" />
                <ColumnDefinition Width="600" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="1"
                    BorderBrush="LightGray"
                    BorderThickness="1">
                <Grid>
                    <ScrollViewer Name="DesignerScrollViewer"
                                  Background="Transparent"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto">
                        <s:DesignerCanvas x:Name="MyDesignerCanvas"
                                          MinHeight="500"
                                          MinWidth="500"
                                          AllowDrop="True"
                                          Background="White">
                        </s:DesignerCanvas>
                    </ScrollViewer>
                </Grid>
            </Border>
            <Border Grid.Column="0"
                    BorderBrush="LightGray"
                    BorderThickness="1">
                <StackPanel>
                    <StackPanel>
                        <Button Content="Save" Command="{Binding SaveCommand}" />
                        <Button Content="Set Binding" Command="{Binding SetBindingCommand}" />
                    </StackPanel>
                    <StackPanel Name="Palette" />
                </StackPanel>
            </Border>
            <StackPanel Grid.Column="2" Name="PropertyStackPanel">
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
