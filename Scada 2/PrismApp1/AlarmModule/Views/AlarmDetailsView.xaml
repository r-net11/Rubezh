<UserControl x:Class="AlarmModule.Views.AlarmDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderThickness="2" BorderBrush="DarkGray" CornerRadius="5" Margin="2">
        <Border.Background>
            <LinearGradientBrush StartPoint="0.5, 0" EndPoint="0.5, 1">
                <GradientStop Offset="0" Color="DarkGray" />
                <GradientStop Offset="1" Color="LightGray" />
            </LinearGradientBrush>
        </Border.Background>
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" HorizontalAlignment="Left" Width="100">
                <Button Content="Сброс" Command="{Binding ResetCommand}" Height="30" />
                <Button Content="План" Command="{Binding ShowOnPlanCommand}" Height="30" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12" Margin="2" />
                <TextBlock Text="{Binding Description}" Foreground="Black" Margin="2" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
