<UserControl x:Class="AlarmModule.Views.AlarmGroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ItemsControl ItemsSource="{Binding MainAlarms}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding ShowCommand}" Width="70" Height="70" Margin="2" IsEnabled="{Binding HasAlarms}">
                        <StackPanel Orientation="Horizontal">
                            <Image Visibility="Visible" Source="D:/xxx2.png" Stretch="None" />
                            <TextBlock Visibility="Collapsed" Text="{Binding Name}" Foreground="White" TextWrapping="WrapWithOverflow" />
                            <TextBlock Text="{Binding Count}" Foreground="White" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
