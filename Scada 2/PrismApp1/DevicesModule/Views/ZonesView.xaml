<UserControl x:Class="DevicesModule.Views.ZonesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:WpfToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
<Grid>
        <StackPanel HorizontalAlignment="Left">
            <ListBox ItemsSource="{Binding ZoneViewModels}" Width="310" SelectedItem="{Binding SelectedZoneViewModel}" BorderThickness="0">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="Background" Value="Yellow"/>
                        <Setter Property="Background" Value="Pink"/>
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="BorderBrush" Value="Red" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Trigger.Setters>
                                    <Setter Property="BorderBrush" Value="White" />
                                    <Setter Property="BorderThickness" Value="0" />
                                </Trigger.Setters>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Width="50" Height="25" Background="Green" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding SelectCommand}">
                            <TextBlock Text="{Binding ZoneNo}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Width="300" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>

            <StackPanel DataContext="{Binding SelectedZoneViewModel}">
                <TextBlock Text="{Binding ZoneName}" />
            </StackPanel>
            
            <ListBox Visibility="Collapsed" ItemsSource="{Binding Devices}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ShortDriverName}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>



            <WpfToolkit:DataGrid ItemsSource="{Binding Devices}"
                            Height="500" AutoGenerateColumns="False" SelectionMode="Single" HeadersVisibility="Column"
                            CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            IsReadOnly="True" SelectionUnit="FullRow" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" HorizontalGridLinesBrush="Beige" VerticalGridLinesBrush="Beige"
                            Background="White" BorderBrush="Black" BorderThickness="0"
                            RowHeight="20">
                <WpfToolkit:DataGrid.Columns>
                    <WpfToolkit:DataGridTemplateColumn Header="Тип Устройства" Width="250">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="{Binding Level, Converter={StaticResource LevelToMarginConverter}}">
                                        <StackPanel Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <CheckBox Style="{StaticResource TreeExpanderStyle}" IsChecked="{Binding IsExpanded, Mode=TwoWay}"/>
                                        </StackPanel>
                                        <StackPanel Visibility="{Binding HasImage, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <Image Source="{Binding ImageSource}" Width="16" Height="16" Margin="5, 0, 5, 0" />
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding ShortDriverName}" />
                                    </StackPanel>

                                </Grid>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                    <WpfToolkit:DataGridTemplateColumn Header="Адрес" Width="100">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Address}" />
                                </StackPanel>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                    <WpfToolkit:DataGridTemplateColumn Header="Зона" Width="100">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding PresentationZone}" />
                                </StackPanel>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                    <WpfToolkit:DataGridTemplateColumn Header="Примечание" Width="*">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Description}" />
                                </StackPanel>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                </WpfToolkit:DataGrid.Columns>

                <WpfToolkit:DataGrid.CellStyle>
                    <Style TargetType="{x:Type WpfToolkit:DataGridCell}">
                        <Style.Setters>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type WpfToolkit:DataGridCell}">
                                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                            <ContentPresenter VerticalAlignment="Center"></ContentPresenter>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style.Setters>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="YellowGreen" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </WpfToolkit:DataGrid.CellStyle>

            </WpfToolkit:DataGrid>

        </StackPanel>
    </Grid>
</UserControl>
