<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Style x:Key="{x:Type Separator}" TargetType="Separator">
		<Setter Property="Margin" Value="0,0,0,-2" />
		<Setter Property="Focusable" Value="false" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Separator}">
					<StackPanel Background="{DynamicResource BaseWindowBackgroundBrush}" Orientation="Horizontal">
						<Border Width="5" Margin="0,0,2,0" Background="{DynamicResource BackgroundBrush}" CornerRadius="0,5,5,0" />
						<Border Width="5" Margin="2,0,0,0" Background="{DynamicResource BackgroundBrush}" CornerRadius="5,0,0,5" />
					</StackPanel>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>