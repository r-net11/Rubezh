<Window x:Class="FiresecService.LicenseEditor.Views.FiresecLicenseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:FiresecService.LicenseEditor.ViewModels"
        Title="Редактор лицензий Firesec Service" Height="220" Width="300" MinHeight="220" MinWidth="300" MaxHeight="220" MaxWidth="300">
    <Window.Resources>
        <vm:FiresecLicenseViewModel x:Key="firesecLicenseViewModel"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource firesecLicenseViewModel}">
        <StackPanel>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            
                <TextBlock Grid.Column="0" Grid.Row="0" Text="Количество пользователей:" Margin="5"/>
                <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding NumberOfUsers}" Margin="5"/>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Пожарная сигнализация и пожаротушение:" Margin="5"/>
                <CheckBox Grid.Column="1" Grid.Row="1" IsChecked="{Binding FireAlarm}" Margin="5"/>
                <TextBlock Grid.Column="0" Grid.Row="2" Text="Охранная сигнализация:" Margin="5"/>
                <CheckBox Grid.Column="1" Grid.Row="2" IsChecked="{Binding SecurityAlarm}" Margin="5"/>
                <TextBlock Grid.Column="0" Grid.Row="3" Text="СКД:" Margin="5"/>
                <CheckBox Grid.Column="1" Grid.Row="3" IsChecked="{Binding Skd}" Margin="5"/>
                <TextBlock Grid.Column="0" Grid.Row="4" Text="Сценарии управления:" Margin="5"/>
                <CheckBox Grid.Column="1" Grid.Row="4" IsChecked="{Binding ControlScripts}" Margin="5"/>
                <TextBlock Grid.Column="0" Grid.Row="5" Text="ОРС-Сервер:" Margin="5"/>
                <CheckBox Grid.Column="1" Grid.Row="5" IsChecked="{Binding OrsServer}" Margin="5"/>
            </Grid>
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button Content="Загрузить" Command="{Binding LoadLicenseCommand}" Margin="5" Padding="20 5"/>
                <Button Content="Сохранить" Command="{Binding SaveLicenseCommand}" Margin="5" Padding="20 5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
