<UserControl x:Class="StrazhService.Monitor.Views.ServiceControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lang="clr-namespace:Localization.StrazhService.Monitor.Views;assembly=Localization"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<StackPanel Orientation="Horizontal">
		<ToolBarButton Command="{Binding StartServiceCommand}" ImageSource="ServiceStart" ToolTip="{x:Static lang:CommonViews.StartServer}" />
		<ToolBarButton Command="{Binding StopServiceCommand}" ImageSource="ServiceStop" ToolTip="{x:Static lang:CommonViews.StopServer}" />
		<ToolBarButton Command="{Binding RestartServiceCommand}" ImageSource="ServiceRestart" ToolTip="{x:Static lang:CommonViews.RestartServer}" />
	</StackPanel>
</UserControl>
