<UserControl x:Class="Infrastructure.Common.BalloonTrayTip.Views.CustomBalloonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             xmlns:local="clr-namespace:Infrastructure.Common.BalloonTrayTip.Views"
             MaxWidth="220" Height="Auto" Opacity="1" Loaded="UserControl_Loaded" MouseUp="grid_MouseUp">
    <UserControl.Resources>
        <Storyboard x:Key="OpeningAnimation" TargetProperty="Opacity">
            <DoubleAnimation From="0" To="1" Duration="0:0:0.5"/>
        </Storyboard>
        <Storyboard x:Key="ClosingAnimation" TargetProperty="Opacity">
            <DoubleAnimation From="1" To="0" Duration="0:0:0.5"/>
        </Storyboard>
        <DrawingImage x:Key="close_cross">
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#00000000">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0 0 20 20"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing>
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <LineGeometry StartPoint="6 6" EndPoint="14 14"/>
                                <LineGeometry StartPoint="6 14" EndPoint="14 6"/>
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                        <GeometryDrawing.Pen>
                            <Pen Brush="Black" Thickness="3"/>
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>
        <DrawingImage x:Key="close_cross_mouse_enter">
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#44000000">
                        <GeometryDrawing.Pen>
                            <Pen Brush="Black" Thickness="2"/>
                        </GeometryDrawing.Pen>
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0 0 20 20"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing>
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <LineGeometry StartPoint="6 6" EndPoint="14 14"/>
                                <LineGeometry StartPoint="6 14" EndPoint="14 6"/>
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                        <GeometryDrawing.Pen>
                            <Pen Brush="Black" Thickness="3"/>
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>
    </UserControl.Resources>
    <UserControl.InputBindings>
        <MouseBinding MouseAction="LeftClick" Command="{Binding RemoveItemCommand}" />
    </UserControl.InputBindings>
    <Border x:Name="grid" Width="220" Height="Auto" CornerRadius="5" BorderThickness="1" BorderBrush="{DynamicResource WindowBorderBrush}" Background="{Binding LastItem.BackgroundColor}">
        <StackPanel>
            <DockPanel>
                <Image x:Name="close_Cross" Height="15" Margin="3" DockPanel.Dock="Right" VerticalAlignment="Top" MouseUp="image_MouseUp">
                    <Image.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding ClearCommand}"/>
                    </Image.InputBindings>
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Setter Property="Image.Source" Value="{StaticResource close_cross}"/>
                            <Style.Triggers>
                                <Trigger Property="Image.IsMouseOver" Value="True" >
                                    <Setter Property="Image.Source" Value="{StaticResource close_cross_mouse_enter}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
                <TextBlock x:Name="Title" TargetUpdated="Title_TargetUpdated" Margin="10 5 10 0" VerticalAlignment="Top" Text="{Binding LastItem.Title, NotifyOnTargetUpdated=True}" TextWrapping="Wrap" Foreground="{Binding LastItem.ForegroundColor}" FontWeight="Bold"/>
            </DockPanel>
            <TextBlock Margin="10 0 10 5" VerticalAlignment="Bottom" Text="{Binding LastItem.Text}" TextWrapping="Wrap" Foreground="{Binding LastItem.ForegroundColor}"/>
        </StackPanel>
    </Border>
</UserControl>
