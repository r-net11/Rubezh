<UserControl x:Class="Infrastructure.Common.Windows.Views.ShellView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Navigation="clr-namespace:Infrastructure.Common.Navigation" Width="{Binding Width}" Height="{Binding Height}" MinWidth="{Binding MinWidth}" MinHeight="{Binding MinHeight}">
	<!--<Grid>-->
	<!--
			<Grid.Resources>
			<Style TargetType="{x:Type Expander}">
			
			</Style>
			</Grid.Resources>
		-->
	<!--<Grid.ColumnDefinitions>
			<ColumnDefinition>-->
	<!--
					<ColumnDefinition.Style>
					<Style TargetType="{x:Type ColumnDefinition}">
					<Style.Triggers>
					<DataTrigger Binding="{Binding IsExpanded, ElementName=LeftExpander}" Value="False">
					<Setter Property="Width" Value="Auto" />
					</DataTrigger>
					<DataTrigger Binding="{Binding IsExpanded, ElementName=LeftExpander}" Value="True">
					<Setter Property="Width" Value="*" />
					</DataTrigger>
					</Style.Triggers>
					</Style>
					</ColumnDefinition.Style>
				-->
	<!--</ColumnDefinition>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition>
				<ColumnDefinition.Style>
					<Style TargetType="{x:Type ColumnDefinition}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding RightContent, Converter={StaticResource NullToBoolConverter}}" Value="False">
								<Setter Property="Width" Value="0" />
							</DataTrigger>
							<DataTrigger Binding="{Binding RightContent, Converter={StaticResource NullToBoolConverter}}" Value="True">
								<Setter Property="Width" Value="*" />
							</DataTrigger>-->
	<!--
								<DataTrigger Binding="{Binding IsExpanded, ElementName=RightExpander}" Value="False">
								<Setter Property="Width" Value="Auto" />
								</DataTrigger>
								<DataTrigger Binding="{Binding IsExpanded, ElementName=RightExpander}" Value="True">
								<Setter Property="Width" Value="*" />
								</DataTrigger>
							-->
	<!--</Style.Triggers>
					</Style>
				</ColumnDefinition.Style>
			</ColumnDefinition>
		</Grid.ColumnDefinitions>-->
	<!--  <Expander x:Name="LeftExpander" Grid.Column="0" ExpandDirection="Left" IsExpanded="True">  -->
	<DockPanel>
		<ContentPresenter Content="{Binding RightContent}" DockPanel.Dock="Right" />
		<Border Background="{DynamicResource WindowBlacHeaderkBackgroundBrush}" DockPanel.Dock="Top">
			<ContentPresenter Content="{Binding Toolbar}" />
		</Border>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid Grid.Column="0">
				<Navigation:NavigationView Margin="5 5 0 5" />
			</Grid>
			<ItemsControl Grid.Column="1" Margin="5 5 5 5" ItemsSource="{Binding ContentItems}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<Grid />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<ContentPresenter Content="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</Grid>
	</DockPanel>
	<!--  </Expander>  -->
	<!--<GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent" Cursor="SizeWE" Visibility="{Binding RightContent, Converter={StaticResource NullToVisibilityConverter}}" />-->
	<!--  <Expander x:Name="RightExpander" Grid.Column="2" ExpandDirection="Right" Visibility="{Binding RightContent, Converter={StaticResource NullToVisibilityConverter}}">  -->
	<!--<ContentPresenter Grid.Column="2" Content="{Binding RightContent}" />-->
	<!--  </Expander>  -->
	<!--</Grid>-->
</UserControl>