<UserControl 
    x:Class="Infrastructure.Common.Windows.Views.ProgressView" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    MinWidth="400" Foreground="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title, Mode=OneWay}" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="17" FontWeight="Black" Foreground="White" TextAlignment="Center" TextWrapping="Wrap" />
        <ProgressBar Grid.Row="1" Margin="10 3 10 3" IsIndeterminate="{Binding IsIndeterminate}" Maximum="{Binding StepCount}" Value="{Binding CurrentStep}" />
        <TextBlock Grid.Row="1" Margin="10 3 10 3" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="Black" Text="{Binding Text}" />
        <Button Grid.Row="2" Margin="5" Content="Отмена" Width="70" Command="{Binding CancelCommand}" Visibility="{Binding IsCancelVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</UserControl>