<UserControl x:Class="Infrastructure.Common.Windows.Views.SaveCancelDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10,10,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Name="_okCancelStackPanel" Margin="0, 4, 0, 0" >
            <Button Name="bOk" Content="OK" Width="100" Margin="0, 0, 4, 0" IsDefault="True" />
            <Button Name="bCancel" Content="Отмена"  Width="100" />
        </StackPanel>
    </Grid>
</UserControl>
