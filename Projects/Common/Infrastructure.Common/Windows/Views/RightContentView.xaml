<UserControl x:Class="Infrastructure.Common.Windows.Views.RightContentView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:loc="clr-namespace:Infrastructure.Common.Windows.ViewModels" xmlns:Navigation="clr-namespace:Infrastructure.Common.Navigation">
	<DockPanel>
		<Border Background="{DynamicResource WindowBlacHeaderkBackgroundBrush}" DockPanel.Dock="Top">
			<DockPanel>
				<Button Width="50" VerticalAlignment="Center" Command="{Binding Shell.MinimizeCommand}" CommandParameter="{x:Static loc:MinimizeTarget.RightPanel}" Content="&gt;&gt;" DockPanel.Dock="Left" />
				<ContentPresenter Content="{Binding Menu}" />
			</DockPanel>
		</Border>
		<ContentPresenter Content="{Binding Content}" />
	</DockPanel>
</UserControl>