<UserControl x:Class="Infrastructure.Client.Startup.Views.StartupSettingsView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended" 
             xmlns:xc="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
             xmlns:res="clr-namespace:Infrastructure.Client.Resources.Language.Startup.Views"
             MinWidth="400" MinHeight="150">
	<UserControl.Resources>
		<ContextMenu x:Key="ContextMenu">
			<IconedMenuItem HorizontalAlignment="Left" Command="ApplicationCommands.Copy" ImageSource="BCopy"  Text="{x:Static res:StartupSettingsView.Copy}" />
            <IconedMenuItem HorizontalAlignment="Left" Command="ApplicationCommands.Cut" ImageSource="BCut"  Text="{x:Static res:StartupSettingsView.Cut}" />
            <IconedMenuItem HorizontalAlignment="Left" Command="ApplicationCommands.Paste" ImageSource="BPaste"  Text="{x:Static res:StartupSettingsView.Paste}" />
		</ContextMenu>
		<Style TargetType="{x:Type extToolkit:WatermarkTextBox}">
			<Setter Property="ContextMenu" Value="{StaticResource ContextMenu}" />
		</Style>
		<Style TargetType="{x:Type TextBox}">
			<Setter Property="ContextMenu" Value="{StaticResource ContextMenu}" />
		</Style>
	</UserControl.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static res:StartupSettingsView.Server}" />
        <Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static res:StartupSettingsView.Port}" />
        <Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static res:StartupSettingsView.ReportServicePort}" />
        <Label Grid.Row="3" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static res:StartupSettingsView.Autoconnect}" />
        <Label Grid.Row="4" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static res:StartupSettingsView.AutoconnectLogin}" />
        <Label Grid.Row="5" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static res:StartupSettingsView.AutoconnectPassword}" />

		<TextBox Grid.Row="0" Grid.Column="1" Margin="2" Text="{Binding RemoteAddress, UpdateSourceTrigger=PropertyChanged}" />
		<extToolkit:IntegerUpDown Grid.Row="1" Grid.Column="1" Margin="2" Maximum="65535" Minimum="0" Value="{Binding RemotePort, UpdateSourceTrigger=PropertyChanged}" />
		<extToolkit:IntegerUpDown Grid.Row="2" Grid.Column="1" Margin="2" Maximum="65535" Minimum="0" Value="{Binding ReportRemotePort, UpdateSourceTrigger=PropertyChanged}" />
		<CheckBox Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding AutoConnect, UpdateSourceTrigger=PropertyChanged}" />
		<TextBox Grid.Row="4" Grid.Column="1" Margin="2" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" />
		<TextBox Grid.Row="5" Grid.Column="1" Margin="2" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
	</Grid>
</UserControl>