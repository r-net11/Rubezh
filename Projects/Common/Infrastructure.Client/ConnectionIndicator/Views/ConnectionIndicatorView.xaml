<UserControl
	x:Class="Infrastructure.Client.ConnectionIndicator.Views.ConnectionIndicatorView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Loaded="UserControl_Loaded"
	d:DesignHeight="300"
	d:DesignWidth="300"
	mc:Ignorable="d">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/Controls;component/XamlImages/Ind_C-P.xaml" />
				<ResourceDictionary Source="pack://application:,,,/Controls;component/XamlImages/Ind_CxP.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Border
		Name="_border"
		Width="50"
		Height="30"
		Margin="3 3"
		CornerRadius="5"
		ToolTip="Связь в норме">
		<Grid
			Name="_image"
			Width="40"
			Height="13"
			Margin="2, 0, 0, 0"
			Opacity="0.4"
			RenderOptions.BitmapScalingMode="NearestNeighbor">
			<Grid.Style>
				<Style>
					<Style.Triggers>
						<DataTrigger Binding="{Binding IsConnected}" Value="True">
							<DataTrigger.Setters>
								<Setter Property="Grid.Background" Value="{DynamicResource Ind_C-P}" />
							</DataTrigger.Setters>
						</DataTrigger>
						<DataTrigger Binding="{Binding IsConnected}" Value="False">
							<DataTrigger.Setters>
								<Setter Property="Grid.Background" Value="{DynamicResource Ind_CxP}" />
							</DataTrigger.Setters>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Grid.Style>
		</Grid>
	</Border>
</UserControl>
