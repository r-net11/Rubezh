<UserControl x:Class="ControllerSDK.Views.InterlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<StackPanel>
		<StackPanel Orientation="Horizontal">
			<Button Content="Считать" Command="{Binding GetInterlockConfigurationCommand}" Width="200" Margin="2" />
			<Button Content="Записать" Command="{Binding SetInterlockConfigurationCommand}" Width="200" Margin="2" />
		</StackPanel>
		<StackPanel Orientation="Horizontal">
			<TextBlock Text="Количество дверей" Margin="5 2"/>
			<TextBox Width="50" Text="{Binding DoorsCount}" IsEnabled="False" Margin="5 2"/>
		</StackPanel>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>
			<CheckBox Grid.Row="0" Grid.Column="0" Margin="2" IsChecked="{Binding IsActivated}" IsEnabled="{Binding CanActivate}" />
			<TextBlock Grid.Row="0" Grid.Column="1" Text="Активировать" Margin="5 2"/>
			<StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left">
				<TextBlock Text="Режим" Margin="5 2"/>
				<ComboBox Width="350" ItemsSource="{Binding AvailableInterlockModes}" SelectedItem="{Binding SelectedInterlockMode}" IsEnabled="{Binding IsActivated}" Margin="2">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
			</StackPanel>
		</Grid>
	</StackPanel>
</UserControl>
