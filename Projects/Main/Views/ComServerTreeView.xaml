<UserControl x:Class="Main.ComServerTreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <TreeView ItemsSource="{Binding ComDevices}" SelectedItemChanged="ComServerTreeView_SelectedItemChanged" BorderThickness="0" Background="{Binding ElementName=ComServerTab, Path=Background}">

        <TreeView.ItemContainerStyle>
            <Style>
                <Setter Property="TreeViewItem.IsExpanded" Value="True"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=Type}" Value="menu">
                        <Setter Property="TreeViewItem.IsSelected" Value="True"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </TreeView.ItemContainerStyle>

        <TreeView.ItemTemplate>
            <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                <TextBlock Text="{Binding DeviceName}" />
            </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
</UserControl>
