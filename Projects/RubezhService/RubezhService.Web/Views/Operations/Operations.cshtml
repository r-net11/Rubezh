@model IEnumerable<RubezhService.Models.Operation>

@{
    ViewBag.CurrentTab = "Operations";
}

<table class="table">
    <thead>
        <tr>
            <th>Название</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var operation in Model)
        {
            <tr>
                <td>@operation.Name</td>
            </tr>
        }
    </tbody>
</table>

