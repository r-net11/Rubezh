<Window x:Class="DeviceEditor.Views.View" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:DeviceEditor" xmlns:Views="clr-namespace:DeviceEditor.Views" Title="Графическая библиотека устройств" Height="600" Width="800" DataContext="ViewModel">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ScrollViewer Style="{DynamicResource LeftScrollViewer}" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Visible">
            <Grid Grid.Column="0">
                <Grid.Background>
                    <ImageBrush ImageSource="Style/2.jpg" Viewport="0,0,15,15" ViewportUnits="Absolute" TileMode="Tile" />
                </Grid.Background>
                <Views:DevicesView />
            </Grid>
        </ScrollViewer>
        <Grid Grid.Column="1">
            <TabControl TabStripPlacement="Bottom" DataContext="{Binding SelectedStateViewModel}" ItemsSource="{Binding FrameViewModels}" SelectedItem="{Binding SelectedFrameViewModel}">
                <TabControl.Background>
                    <ImageBrush ImageSource="Style/1.jpg" Viewport="0,0,15,15" ViewportUnits="Absolute" TileMode="Tile" />
                </TabControl.Background>
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <TextBlock Text="Кадр " />
                            <TextBlock Text="{Binding Id}" />
                        </DockPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <Grid Margin="-4">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <Grid.Background>
                                    <ImageBrush ImageSource="Style/2.jpg" Viewport="0,0,15,15" ViewportUnits="Absolute" TileMode="Tile" />
                                </Grid.Background>
                                <TextBox Background="Transparent" Foreground="Yellow" CaretBrush="White" Text="{Binding Image, UpdateSourceTrigger=PropertyChanged}" Name="textBox2" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" AcceptsReturn="True" AcceptsTab="True" />
                            </Grid>
                            <Grid Grid.Row="1">
                                <Grid.Background>
                                    <ImageBrush ImageSource="Style/3.jpg" Viewport="0,0,15,15" TileMode="Tile" Stretch="None" ViewportUnits="Absolute" />
                                </Grid.Background>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <ItemsControl VerticalAlignment="Bottom" HorizontalAlignment="Left" ItemsSource="{Binding Path=Picture, UpdateSourceTrigger=PropertyChanged}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Canvas Width="500" Height="500">
                                                </Canvas>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.LayoutTransform>
                                            <ScaleTransform ScaleX="0.4" ScaleY="0.4" />
                                        </ItemsControl.LayoutTransform>
                                    </ItemsControl>
                                </Grid>
                                <Grid Grid.Column="1" Height="120" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                        <StackPanel VerticalAlignment="Bottom">
                                            <DockPanel HorizontalAlignment="Left">
                                                <Label Content="Слой:" FontSize="14" FontWeight="ExtraBold" />
                                                <TextBox Background="Transparent" BorderBrush="Black" HorizontalContentAlignment="Center" Width="43" Height="20" FontSize="13" FontWeight="Bold" Text="{Binding Layer, UpdateSourceTrigger=PropertyChanged}" />
                                            </DockPanel>
                                            <Label Background="Transparent"  Content="Длительность: " FontWeight="ExtraBold" FontSize="11"  Height="28" Name="label1" Margin="-2,0,0,-5" />
                                            <TextBox Background="Transparent" Name="textBoxDuration" BorderBrush="Black"  HorizontalContentAlignment="Right" FontWeight="Bold" Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" AcceptsTab="True" AcceptsReturn="True" IsManipulationEnabled="True" IsTabStop="False" />
                                            <Button Background="Transparent" Content="Добавить кадр" FontWeight="ExtraBold" BorderThickness="3" Command="{Binding AddFrameCommand}" />
                                            <Button Background="Transparent" Content="Удалить кадр" FontWeight="ExtraBold" BorderThickness="3" Command="{Binding RemoveFrameCommand}" />
                                        </StackPanel>
                                </Grid>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
            <Grid>
                <ContentControl DataContext="{Binding SelectedStateViewModel.ParentDevice}" HorizontalAlignment="Right">
                    <Grid>
                        <ItemsControl ItemsSource="{Binding DeviceControl.StateCanvases}" VerticalAlignment="Center" Margin="0,64,12,0" HorizontalAlignment="Left" HorizontalContentAlignment="Left" Width="500">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas Width="500" Height="500" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.LayoutTransform>
                                <ScaleTransform ScaleX="0.15" ScaleY="0.15" />
                            </ItemsControl.LayoutTransform>
                        </ItemsControl>
                    </Grid>
                </ContentControl>
            </Grid>
            <Grid VerticalAlignment="Bottom" HorizontalAlignment="Right" Panel.ZIndex="1">
                <DockPanel VerticalAlignment="Bottom">
                    <Button Width="80" Height="22" Command="{Binding SaveCommand}">Сохранить</Button>
                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>