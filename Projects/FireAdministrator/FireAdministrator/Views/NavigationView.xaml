<UserControl x:Class="FireAdministrator.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,3,0">
            <TabControl DockPanel.Dock="Top" Template="{StaticResource OutlookTab}" VerticalAlignment="Top" ItemsSource="{Binding Navigation}">
                <TabControl.ItemTemplate>
                    <!--<DataTemplate>
                        <DataTemplate.Resources>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="FontSize" Value="11" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="Margin" Value="10, 0, 0, 0" />
                                <Setter Property="Foreground" Value="White" />
                            </Style>
                            <Style TargetType="{x:Type Image}">
                                <Setter Property="Height" Value="24" />
                                <Setter Property="Width" Value="24" />
                            </Style>
                        </DataTemplate.Resources>
                        <StackPanel Orientation="Horizontal">
                            <Image/>-->
                    <!--<Image Source="{Binding Icon}"  />-->
                    <!--<TextBlock Text="{Binding Title}" />
                        </StackPanel>
                    </DataTemplate>-->
                    <HierarchicalDataTemplate ItemsSource="{Binding Childs}">
                        <StackPanel Orientation="Horizontal">
                            <Image/>
                            <TextBlock Text="{Binding Title}" />
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TabControl.ItemTemplate>
            </TabControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>