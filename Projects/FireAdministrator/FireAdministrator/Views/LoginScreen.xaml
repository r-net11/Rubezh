<Window x:Class="FireAdministrator.LoginScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:Controls;assembly=Controls"
        Title="Ввод имени пользователя и пароля" Height="180" Width="350"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="2">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Пользователь" Margin="2" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль" Margin="2" />
            <TextBox Grid.Row="0" Grid.Column="1" Name="_userName" Margin="2" Text="adm" />
            <Controls:PasswordBoxControl Grid.Row="1" Grid.Column="1" Name="_password" Margin="2" />
        </Grid>

        <TextBlock Grid.Row="1" Name="_info" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="2" HorizontalAlignment="Right">
            <Controls:XButton IsDefault="True" Content="Ok" Click="OnConnect" FontSize="14" Width="100" Height="30" Margin="2" Background="DarkGreen" />
            <Controls:XButton Content="Отмена" Click="OnCancel" FontSize="14" Width="100" Height="30" Margin="2" Background="DarkRed" />
        </StackPanel>
    </Grid>
</Window>