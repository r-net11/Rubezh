<UserControl x:Class="GKModule.Views.XDeviceDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:Controls;assembly=Controls" MinWidth="600" MinHeight="600">
    <ListView ItemsSource="{Binding XDevices}" SelectedItem="{Binding SelectedXDevice}" SelectionMode="Single">
        <ListView.View>
            <GridView>
                <GridView.Columns>
                    <GridViewColumn Header="Обозначение" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5, 1, 0, 0">
                                    <Image Width="14" Margin="5,0" Source="{Binding XDriver.ImageSource}" />
                                    <TextBlock Text="{Binding XDriver.ShortName}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Наименование" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding XDriver.Name}" ToolTip="{Binding XDriver.Name}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView.Columns>
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>