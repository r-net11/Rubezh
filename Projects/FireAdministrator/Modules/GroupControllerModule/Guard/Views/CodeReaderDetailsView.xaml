<UserControl x:Class="GKModule.Views.CodeReaderDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Действие при вооде *КОД#" Margin="2" />
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding AttentionActionTypes}" SelectedIndex="{Binding SelectedAttentionActionType}" Margin="2">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" Margin="2" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Действие при вооде *1*КОД#" Margin="2" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Действие при вооде *2*КОД#" Margin="2" />
    </Grid>
</UserControl>