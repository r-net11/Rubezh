<UserControl x:Class="GKModule.Views.HolidaysView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <XDataGrid ItemsSource="{Binding Holidays}" SelectedItem="{Binding SelectedHoliday}" CanUserSortColumns="True">
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="Название" SortMemberPath="Holiday.Name" Width="Auto" CanUserResize="False">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Controls;component/Images/Holiday.png" Margin="2 0" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding Holiday.Name}" Margin="2 0 5 0" />
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Дата" SortMemberPath="Holiday.Date" Width="Auto" CanUserResize="False">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock VerticalAlignment="Center" Text="{Binding Holiday.Date}" Margin="2 0 5 0" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Тип" SortMemberPath="Holiday.HolidayType" Width="Auto" CanUserResize="False">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Holiday.HolidayType, Converter={StaticResource EnumToDescriptionConverter}}" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Величина сокращения" SortMemberPath="ReductionTime" Width="Auto" CanUserResize="False">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ReductionTime}" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Дата переноса" SortMemberPath="TransitionDate" Width="Auto" CanUserResize="False">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding TransitionDate}" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Width="*" Binding="{Binding Holiday.Name}" Header="Наименование" />
            <DataGridTextColumn Width="*" Binding="{Binding Holiday.Description}" Header="Примечание" />
        </DataGrid.Columns>
    </XDataGrid>
</UserControl>