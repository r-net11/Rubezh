<UserControl x:Class="GKModule.Views.OPCView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataGrid ItemsSource="{Binding Objects}" SelectedItem="{Binding SelectedObject}" CanUserSortColumns="True">
		<DataGrid.Columns>
            <DataGridTemplateColumn Header="№" Width="*">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding VisualizationState, Converter={StaticResource VisualizationStateToImageSourceConverter}}" />
                            <Image Width="16" Height="16" HorizontalAlignment="Left" Source="{Binding Object.ImageSource}" />
                            <TextBlock Margin="2 0 5 0" VerticalAlignment="Center" Text="{Binding  Object.No}" />
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
			<DataGridTextColumn Header="Наименование" Binding="{Binding Object.Name}" Width="*" />
            <DataGridTextColumn Header="Примечание" Binding="{Binding Object.Description}" Width="*" />
		</DataGrid.Columns>
	</DataGrid>
</UserControl>