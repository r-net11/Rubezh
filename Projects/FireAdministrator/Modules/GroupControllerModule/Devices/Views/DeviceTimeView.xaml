<UserControl x:Class="GKModule.Views.DeviceTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="150">

    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Время" Foreground="White" Margin="2" />
            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Time}" Margin="2" />
        </Grid>
        <StackPanel Orientation="Horizontal" Margin="2">
            <Button Content="Считать" Command="{Binding ReadCommand}" Width="100" Margin="2" />
            <Button Content="Записать" Command="{Binding WriteCommand}" Width="100" Margin="2" />
        </StackPanel>
    </StackPanel>
</UserControl>