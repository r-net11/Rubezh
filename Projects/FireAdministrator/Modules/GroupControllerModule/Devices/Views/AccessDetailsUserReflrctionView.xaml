<UserControl x:Class="GKModule.Views.AccessDetailsUserReflectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:views="clr-namespace:GKModule.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinHeight="400" MinWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColor_0}" Text="Имя" Margin="2" />
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Name}" Margin="2"/>
		<TextBlock Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColor_0}" Text="Пароль" Margin="2" />
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Password}" Margin="2"/>
		<TextBlock Grid.Column="0" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColor_0}" Text="Тип" Margin="2" />
        <ComboBox  Grid.Column="1" Grid.Row="2" ItemsSource="{Binding GKCardTypes}" SelectedItem="{Binding SelectedGKCardType}" Margin="2">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Grid.Column="0" Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Top" Foreground="White" Text="Дата действия" Margin="2" />
            <StackPanel Grid.Column="1" Grid.Row="3" Orientation="Horizontal">
                <StackPanel >
                <Calendar Margin="2" VerticalAlignment="Top" BorderBrush="#66ff9829" BorderThickness="2" DisplayDate="{Binding DateEndAccess}" IsTodayHighlighted="False" SelectedDate="{Binding DateEndAccess}" SelectionMode="SingleDate" />
                </StackPanel>
           </StackPanel>
    </Grid>
</UserControl>
