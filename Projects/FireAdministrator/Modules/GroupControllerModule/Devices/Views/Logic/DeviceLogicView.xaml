<UserControl x:Class="GKModule.Views.DeviceLogicView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:Controls="clr-namespace:Controls;assembly=Controls"
		xmlns:Converters="clr-namespace:GKModule.Converters"
		xmlns:Views="clr-namespace:GKModule.Views"
		MinWidth="800" MinHeight="500">

	<TabControl>
		<TabItem Header="Условие включения">
			<ScrollViewer VerticalScrollBarVisibility="Auto">
				<StackPanel>
                    <Views:ClausesView DataContext="{Binding ClausesGroup}" />
				</StackPanel>
			</ScrollViewer>
		</TabItem>
		<TabItem Header="{Binding OffClauseName}" Visibility="{Binding HasOffClause, Converter={StaticResource BoolToVisibilityConverter}}">
			<ScrollViewer VerticalScrollBarVisibility="Auto">
                <Views:ClausesView DataContext="{Binding OffClausesGroup}" />
			</ScrollViewer>
		</TabItem>
	</TabControl>
</UserControl>