<UserControl x:Class="GKModule.Views.ConvertationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
            <DataGrid Grid.Row="0" ItemsSource="{Binding FSShleifs}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Шлейф" Binding="{Binding ShleifNo}" />
                <DataGridTextColumn Header="КАУ" Binding="{Binding KAUName}" Width="*" />
                <DataGridTextColumn Header="Шлейф КАУ" Binding="{Binding KAUShleifNo}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>