<UserControl x:Class="GKModule.Views.DeviceConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:GKModule.Converters"
             MinWidth="800" MinHeight="600">
    
    <UserControl.Resources>
        <Converters:BytesToStringConverter x:Key="BytesToStringConverter" />
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Margin" Value="1" />
            <Setter Property="Foreground" Value="White" />
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding BinObjects}" SelectedItem="{Binding SelectedBinObject}" Margin="2">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Устройство" MinWidth="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="{Binding Level, Converter={StaticResource LevelToMarginConverter}}">
                                    <Image Source="{Binding ImageSource}" Width="16" Height="16" Margin="5, 0, 5, 0" />
                                    <TextBlock Text="{Binding Name}" HorizontalAlignment="Stretch" Margin="5, 0, 5, 0" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <DataGrid Grid.Column="1" ItemsSource="{Binding DBViewModels}" SelectedItem="{Binding SelectedDBViewModel}" Margin="2">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Устройство" MinWidth="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel DataContext="{Binding Device}" Orientation="Horizontal" Margin="{Binding Level, Converter={StaticResource LevelToMarginConverter}}">
                                    <Image Source="{Binding ImageSource}" Width="16" Height="16" Margin="5, 0, 5, 0" />
                                    <TextBlock Text="{Binding Name}" HorizontalAlignment="Stretch" Margin="5, 0, 5, 0" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Адрес" Binding="{Binding Device.Address}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        
        <Grid Grid.Row="1" DataContext="{Binding SelectedBinObject}" Margin="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Border CornerRadius="5" Padding="5" Background="{DynamicResource BackgroundBrush}">
                    <Grid DataContext="{Binding BinaryObject}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Тип" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Адрес" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Смещение" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Количество входных зависимостей" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Входные зависимости" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Количество выходных зависимостей" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="Выходные зависимости" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Логика" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="8" Grid.Column="0" Text="" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="9" Grid.Column="0" Text="Количества параметров" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="10" Grid.Column="0" Text="Значения параметра" Margin="0, 0, 5, 0" />
                        <TextBlock Grid.Row="11" Grid.Column="0" Text="Все" Margin="0, 0, 5, 0" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DeviceType, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Address, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Offset, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding InputDependensesCount, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding InputDependenses, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding OutputDependensesCount, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding OutputDependenses, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Formula, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <ItemsControl Grid.Row="7" Grid.Column="1" ItemsSource="{Binding FormulaOperations}" Margin="1">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding FormulaOperationType}" Foreground="White" Width="60" />
                                        <TextBlock Text="{Binding FirstOperand}" Foreground="White" Width="15" />
                                        <TextBlock Text="{Binding SecondOperand}" Foreground="White" Width="15" />
                                        <TextBlock Text="{Binding Comment}" Foreground="White" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding ParametersCount, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding Parameters, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                        <TextBox Grid.Row="11" Grid.Column="1" Text="{Binding AllBytes, Converter={StaticResource BytesToStringConverter}, Mode=OneWay}" Margin="1" />
                    </Grid>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>