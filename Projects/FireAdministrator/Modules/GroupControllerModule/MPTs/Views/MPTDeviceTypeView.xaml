<UserControl x:Class="GKModule.Views.MPTDeviceTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="400">

    <DataGrid ItemsSource="{Binding AvailableMPTDeviceTypes}" SelectedItem="{Binding SelectedMPTDeviceType}" x:Name="dataGrid1" SelectionChanged="OnSelectionChanged">
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="Тип" Width="*">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource MPTDeviceTypeToStringConverter}}" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>