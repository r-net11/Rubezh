<UserControl x:Class="GKModule.Views.MPTsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls">
    <Grid>
        <Controls:XDataGrid ItemsSource="{Binding MPTs}" SelectedItem="{Binding SelectedMPT}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Width="1*" Binding="{Binding MPT.Name}" />
                <DataGridTextColumn Header="Описание" Width="1*" Binding="{Binding MPT.Description}" />
            </DataGrid.Columns>
        </Controls:XDataGrid>
    </Grid>
</UserControl>