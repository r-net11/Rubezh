<UserControl x:Class="GKModule.Views.PmfUserDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
			 xmlns:ExtToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
             Height="300" Width="400">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="2*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Grid.Column="0" Text="Прибор" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<ComboBox Grid.Row="0" Grid.Column="1" Margin="2" ItemsSource="{Binding GkDevices}" SelectedItem="{Binding GkDevice}" >
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding PresentationName}" Margin="1" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>

		<TextBlock Grid.Row="1" Grid.Column="0" Text="Номер" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<ExtToolkit:IntegerUpDown Grid.Row="1" Grid.Column="1" Margin="2" HorizontalAlignment="Left" Minimum="1"
								  Value="{Binding GkNo, UpdateSourceTrigger=PropertyChanged}" />
		
		<TextBlock Grid.Row="2" Grid.Column="0" Text="ФИО" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<TextBox Grid.Row="2" Grid.Column="1" Margin="2"
							  Text="{Binding Fio, UpdateSourceTrigger=PropertyChanged}" />

		<TextBlock Grid.Row="3" Grid.Column="0" Text="Пароль" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<ExtToolkit:IntegerUpDown Grid.Row="3" Grid.Column="1" Margin="2" HorizontalAlignment="Left" Minimum="0"
								  Value="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />

		<TextBlock Grid.Row="4" Grid.Column="0" Text="Тип" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<ComboBox Grid.Row="4" Grid.Column="1" Margin="2" ItemsSource="{Binding UserTypes}" SelectedItem="{Binding UserType}" >
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" Margin="1" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>

		<TextBlock Grid.Row="5" Grid.Column="0" Text="Срок действия" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" >
			<DatePicker VerticalAlignment="Center" Width="150" Focusable="False" Margin="2" SelectedDate="{Binding ExpirationDate.Date}" />
			<Controls:TimePicker Margin="2" TimeSpan="{Binding ExpirationDate.Time}" />
		</StackPanel>

		
	</Grid>
</UserControl>
