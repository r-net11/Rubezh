<UserControl x:Class="GKModule.Views.InstructionsMenuView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls">
	
	<StackPanel Orientation="Horizontal" DataContext="{Binding Context}">
		<ToolBarButton Command="{Binding AddCommand}" ToolTip="Добавить (Ctrl+N)" Background="{DynamicResource Add}"  />
		<Controls:ToolBarButton Command="{Binding EditCommand}" ToolTip="Редактировать (Ctrl+E)" Background="{DynamicResource Edit}"  />
		<Controls:ToolBarButton Command="{Binding DeleteCommand}" ToolTip="Удалить (Ctrl+Del)" Background="{DynamicResource Delete}"  />
		<Controls:ToolBarButton Command="{Binding DeleteAllCommand}" ToolTip="Удалить все инструкции" Background="{DynamicResource Delete}"  />
	</StackPanel>
</UserControl>