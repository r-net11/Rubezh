<UserControl x:Class="FiltersModule.Views.FilterDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:FiltersModule.Converters"
             xmlns:ExtToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
             MinWidth="500" MinHeight="400">
    <UserControl.Resources>
        <Converters:StateTypeToEventNameConverter x:Key="StateTypeToEventNameConverter" />
        <Converters:CategoryTypeToCategoryNameConverter x:Key="CategoryTypeToCategoryNameConverter" />
        <SolidColorBrush x:Key="headerBrush">OrangeRed</SolidColorBrush>
        <Style x:Key="textBoxInError" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <DockPanel>
        <WrapPanel Margin="5"
                   HorizontalAlignment="Right"
                   DockPanel.Dock="Bottom">
            <Button Width="100"
                    Margin="2"
                    Command="{Binding OkCommand}"
                    Content="Ok"
                    IsDefault="True" />
            <Button Width="100"
                    Margin="2"
                    Command="{Binding CancelCommand}"
                    Content="Отмена" />
        </WrapPanel>
        <StackPanel>
            <TextBlock Margin="0,0,0,5" Text="Название представления:" />
            <TextBox Margin="0,0,0,15"
                     Style="{StaticResource textBoxInError}"
                     Text="{Binding FilterName,
                                    ValidatesOnDataErrors=True,
                                    UpdateSourceTrigger=PropertyChanged}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <GroupBox Margin="0,0,5,0" Padding="5">
                        <GroupBox.Header>
                            <TextBlock FontWeight="Heavy"
                                       Foreground="{StaticResource headerBrush}"
                                       Text="Классы событий" />
                        </GroupBox.Header>
                        <ItemsControl ItemsSource="{Binding EventViewModels}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Id, Converter={StaticResource StateTypeToEventNameConverter}}" IsChecked="{Binding IsChecked}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </GroupBox>
                    <GroupBox Margin="0,5,5,0"
                              HorizontalContentAlignment="Center"
                              Header="количество записей"
                              Padding="5">
                        <WrapPanel>
                            <ExtToolkit:IntegerUpDown Maximum="{Binding RecordsMaxCount}"
                                                      Minimum="1"
                                                      Value="{Binding JournalFilter.LastRecordsCount}" />
                            <TextBlock Margin="5,5,0,5" Text="последних дней" />
                        </WrapPanel>
                    </GroupBox>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <GroupBox Padding="5">
                        <GroupBox.Header>
                            <TextBlock FontWeight="Heavy"
                                       Foreground="{StaticResource headerBrush}"
                                       Text="Категории устройств" />
                        </GroupBox.Header>
                        <ItemsControl ItemsSource="{Binding CategoryViewModels}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Id, Converter={StaticResource CategoryTypeToCategoryNameConverter}}" IsChecked="{Binding IsChecked}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </GroupBox>
                    <GroupBox Margin="0,5,0,0" Padding="5">
                        <GroupBox.Header>
                            <CheckBox Content="дополнительный фильтр" IsChecked="{Binding JournalFilter.IsLastDaysCountActive, UpdateSourceTrigger=PropertyChanged}" />
                        </GroupBox.Header>
                        <WrapPanel>
                            <TextBlock Margin="5,0,5,5" Text="записи за последние" />
                            <ExtToolkit:IntegerUpDown IsEnabled="{Binding JournalFilter.IsLastDaysCountActive}"
                                                      Minimum="1"
                                                      Value="{Binding JournalFilter.LastDaysCount}" />
                            <TextBlock Margin="5,5,0,5" Text="дн." />
                        </WrapPanel>
                    </GroupBox>
                </StackPanel>
            </Grid>
        </StackPanel>
    </DockPanel>
</UserControl>