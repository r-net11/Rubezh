<UserControl x:Class="FiltersModule.Views.FilterDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:FiltersModule.Converters"
             xmlns:ExtToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
             MinWidth="550" MinHeight="350">
    <UserControl.Resources>
        <Converters:StateTypeToEventNameConverter x:Key="StateTypeToEventNameConverter" />
        <Converters:CategoryTypeToCategoryNameConverter x:Key="CategoryTypeToCategoryNameConverter" />
    </UserControl.Resources>


    <Border CornerRadius="5" Padding="5" Background="{DynamicResource BackgroundBrush}">
        <StackPanel>
            <Grid Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Наименование:" Margin="2" />
                <TextBox Grid.Column="1" Margin="2" Text="{Binding FilterName, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>

            <Grid Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <Label FontWeight="Heavy" Content="Классы событий" />
                    <ItemsControl ItemsSource="{Binding EventViewModels}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Id, Converter={StaticResource StateTypeToEventNameConverter}}" IsChecked="{Binding IsChecked}" Foreground="White" Margin="1" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <Label FontWeight="Heavy" Content="Категории устройств" />
                    <ItemsControl ItemsSource="{Binding CategoryViewModels}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Id, Converter={StaticResource CategoryTypeToCategoryNameConverter}}" IsChecked="{Binding IsChecked}" Foreground="White" Margin="1" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="2">
                <Label Content="Выбрать " VerticalAlignment="Center" />
                <ExtToolkit:IntegerUpDown Maximum="{Binding RecordsMaxCount}" Minimum="1" Value="{Binding JournalFilter.LastRecordsCount}" Height="25" Width="50" />
                <Label Margin="5,5,0,5" Content="последних записей" />
            </StackPanel>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center" Margin="2">
                <CheckBox HorizontalAlignment="Center" Content="дополнительный фильтр" IsChecked="{Binding JournalFilter.IsLastDaysCountActive, UpdateSourceTrigger=PropertyChanged}" Foreground="White" />
                <StackPanel Orientation="Horizontal" Visibility="{Binding JournalFilter.IsLastDaysCountActive, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Label Margin="5,0,5,5" Content="Выбрать записи за последние" VerticalAlignment="Center" />
                    <ExtToolkit:IntegerUpDown IsEnabled="{Binding JournalFilter.IsLastDaysCountActive}" Minimum="1" Value="{Binding JournalFilter.LastDaysCount}" Height="25" Width="50" />
                    <Label Margin="5,5,0,5" Content="дн." VerticalAlignment="Center" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>