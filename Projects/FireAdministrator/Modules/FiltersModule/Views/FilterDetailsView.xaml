<UserControl x:Class="FiltersModule.Views.FilterDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:FiltersModule.Converters"
             xmlns:ExtToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
             MinWidth="500" MinHeight="400">
    <UserControl.Resources>
        <Converters:StateTypeToEventNameConverter x:Key="StateTypeToEventNameConverter" />
        <Converters:CategoryTypeToCategoryNameConverter x:Key="CategoryTypeToCategoryNameConverter" />
        <SolidColorBrush x:Key="headerBrush">OrangeRed</SolidColorBrush>
        <Style x:Key="textBoxInError" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Margin="0,0,0,5" Content="Название представления:" />
            <TextBox Margin="0,0,0,15" Style="{StaticResource textBoxInError}" Text="{Binding FilterName, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Label FontWeight="Heavy" Content="Классы событий" />
                <ItemsControl ItemsSource="{Binding EventViewModels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Id, Converter={StaticResource StateTypeToEventNameConverter}}" IsChecked="{Binding IsChecked}" Foreground="White" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Label Content="количество записей" />
                <StackPanel Orientation="Horizontal">
                    <ExtToolkit:IntegerUpDown Maximum="{Binding RecordsMaxCount}" Minimum="1" Value="{Binding JournalFilter.LastRecordsCount}" Height="25" />
                    <Label Margin="5,5,0,5" Content="последних дней" />
                </StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <Label FontWeight="Heavy" Content="Категории устройств" />
                <ItemsControl ItemsSource="{Binding CategoryViewModels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Id, Converter={StaticResource CategoryTypeToCategoryNameConverter}}" IsChecked="{Binding IsChecked}" Foreground="White" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <CheckBox Content="дополнительный фильтр" IsChecked="{Binding JournalFilter.IsLastDaysCountActive, UpdateSourceTrigger=PropertyChanged}" Foreground="White" />
                <StackPanel Orientation="Horizontal">
                    <Label Margin="5,0,5,5" Content="записи за последние" />
                    <ExtToolkit:IntegerUpDown IsEnabled="{Binding JournalFilter.IsLastDaysCountActive}" Minimum="1" Value="{Binding JournalFilter.LastDaysCount}" Height="25" />
                    <Label Margin="5,5,0,5" Content="дн." />
                </StackPanel>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>