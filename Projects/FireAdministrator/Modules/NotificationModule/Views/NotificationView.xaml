<UserControl x:Class="NotificationModule.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls">
    <Controls:XDataGrid ItemsSource="{Binding Emails}" SelectedItem="{Binding SelectedEmail}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Адрес" Width="200" Binding="{Binding Email.Address}" />
            <DataGridTextColumn Header="Имя" Width="200" Binding="{Binding Email.FirstName}" />
            <DataGridTextColumn Header="Фамилия" Width="200" Binding="{Binding Email.LastName}" />
            <DataGridTextColumn Header="Отсылаемые события" Width="*" Binding="{Binding PresenrationStates}" />
        </DataGrid.Columns>
    </Controls:XDataGrid>
</UserControl>