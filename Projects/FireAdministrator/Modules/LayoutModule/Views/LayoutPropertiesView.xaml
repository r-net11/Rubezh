<UserControl x:Class="LayoutModule.Views.LayoutPropertiesView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended" xmlns:xc="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
             xmlns:lang="clr-namespace:Localization.Layout.Views;assembly=Localization"
             xmlns:langCommon="clr-namespace:Localization.Layout.Common;assembly=Localization"
             MinWidth="400" MinHeight="400">
	<TabControl Background="{DynamicResource BackgroundBrush}">
        <TabItem Header="{x:Static lang:CommonViews.Main}">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.Title}" />
                <Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.Description}" />
                <Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.ShowMenu}" />
				<TextBox Grid.Row="0" Grid.Column="1" Margin="2" Text="{Binding Caption, UpdateSourceTrigger=PropertyChanged}" />
				<TextBox Grid.Row="1" Grid.Column="1" Margin="2" AcceptsReturn="True" AcceptsTab="True" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
				<CheckBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding IsRibbonEnabled}" />
			</Grid>
		</TabItem>
        <TabItem Header="{x:Static lang:CommonViews.Users}">
			<ContentPresenter Margin="-5" Content="{Binding LayoutUsersViewModel}" />
		</TabItem>
        <TabItem Header="{x:Static lang:CommonViews.IpFilter}">
			<Grid Margin="-5" DataContext="{Binding IPFilterViewModel}">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>

				<Grid Grid.Row="1">
					<Grid.RowDefinitions>
						<RowDefinition Height="*" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<DataGrid Grid.Row="0" ItemsSource="{Binding HostNameOrAddressList}" SelectedItem="{Binding SelectedHostNameOrAddress}">
						<DataGrid.Columns>
                            <DataGridTextColumn Width="*" Binding="{Binding .}" Header="{x:Static lang:CommonViews.IpAddressOrCompName}" />
						</DataGrid.Columns>
					</DataGrid>
					<StackPanel Grid.Row="1" HorizontalAlignment="Left" Orientation="Horizontal">
                        <ToolBarButton Command="{Binding AddCommand}" ImageSource="Add"  ToolTip="{x:Static lang:CommonViews.Add}" />
                        <ToolBarButton Command="{Binding RemoveCommand}" ImageSource="Delete"  ToolTip="{x:Static langCommon:CommonResources.Delete}" />
					</StackPanel>
				</Grid>
			</Grid>
		</TabItem>
        <TabItem Header="{x:Static lang:CommonViews.Style}">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.Borderwidth}" />
                <Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.BorderColor}" />
                <Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.BackgroungColor}" />
                <Label Grid.Row="3" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.Indent}" />

                <Label Grid.Row="4" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.SeparatorWidth}" />
                <Label Grid.Row="5" Grid.Column="0" HorizontalAlignment="Right" Content="{x:Static lang:CommonViews.SeparatorColor}" />

				<extToolkit:IntegerUpDown Grid.Row="0" Grid.Column="1" Margin="2" Maximum="100" Minimum="0" Value="{Binding BorderThickness, UpdateSourceTrigger=PropertyChanged}" />
				<xc:ColorPicker Grid.Row="1" Grid.Column="1" Margin="2" SelectedColor="{Binding BorderColor, UpdateSourceTrigger=PropertyChanged}" />
				<xc:ColorPicker Grid.Row="2" Grid.Column="1" Margin="2" SelectedColor="{Binding BackgroundColor, UpdateSourceTrigger=PropertyChanged}" />
				<extToolkit:IntegerUpDown Grid.Row="3" Grid.Column="1" Margin="2" Maximum="100" Minimum="1" Value="{Binding Padding, UpdateSourceTrigger=PropertyChanged}" />
				<extToolkit:IntegerUpDown Grid.Row="4" Grid.Column="1" Margin="2" Maximum="100" Minimum="1" Value="{Binding SplitterSize, UpdateSourceTrigger=PropertyChanged}" />
				<xc:ColorPicker Grid.Row="5" Grid.Column="1" Margin="2" SelectedColor="{Binding SplitterColor, UpdateSourceTrigger=PropertyChanged}" />
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>