<UserControl x:Class="LayoutModule.Views.LayoutElementsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls">
	<tree:TreeList SelectedTreeNode="{Binding SelectedLayoutPart}" Source="{Binding LayoutParts}">
		<tree:TreeList.ItemContainerStyle>
			<Style BasedOn="{StaticResource {x:Type tree:TreeListItem}}" TargetType="{x:Type tree:TreeListItem}">
				<!--  <Setter Property="ContextMenu" Value="{StaticResource contextMenu}" />  -->
				<!--  <Setter Property="Controls:Attach.InputBindings" Value="{StaticResource inputCollection}" />  -->
				<Setter Property="Focusable" Value="True" />
			</Style>
		</tree:TreeList.ItemContainerStyle>
		<tree:TreeList.View>
			<GridView>
				<GridViewColumn Header="Элементы">
					<GridViewColumn.CellTemplate>
						<DataTemplate>
							<Grid>
								<Grid.ToolTip>
									<ToolTip>
										<StackPanel Orientation="Vertical">
											<StackPanel Orientation="Horizontal">
												<Image Width="16" Height="16" Margin="2, 3, 5, 0" VerticalAlignment="Center" Source="{Binding ImageSource, Converter={StaticResource NullToImageConverter}}" Visibility="{Binding ImageSource, Converter={StaticResource NullToVisibilityConverter}}" />
												<TextBlock VerticalAlignment="Center" FontWeight="Bold" Text="{Binding Name}" />
											</StackPanel>
											<TextBlock VerticalAlignment="Center" Text="{Binding Description}" />
										</StackPanel>
									</ToolTip>
								</Grid.ToolTip>
								<Image Width="16" Height="16" Margin="5, 0, 5, 0" HorizontalAlignment="Left" Source="{Binding ImageSource, Converter={StaticResource NullToImageConverter}}" />
								<TextBlock x:Name="txt" Width="200" Margin="31 0 0 0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding Name}" />
							</Grid>
							<DataTemplate.Triggers>
								<DataTrigger Binding="{Binding IsPresented}" Value="False">
									<Setter Property="FontWeight" Value="Bold" TargetName="txt" />
								</DataTrigger>
							</DataTemplate.Triggers>
						</DataTemplate>
					</GridViewColumn.CellTemplate>
				</GridViewColumn>
			</GridView>
		</tree:TreeList.View>
	</tree:TreeList>
</UserControl>
