<UserControl x:Class="LayoutModule.Views.LayoutPartImageView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource BaseWindowBackgroundBrush}" BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="1">
                <Image Source="{Binding ImageSource}" Stretch="{Binding Stretch}" />
            </Border>
            <StackPanel Grid.Column="1">
                <Button Margin="2" Command="{Binding SelectPictureCommand}" Content="Выбрать картинку" />
                <Button Margin="2" Command="{Binding RemovePictureCommand}" Content="Очистить картинку" />
                <DockPanel>
                    <TextBlock Margin="2" Text="Расположение:" Foreground="White" DockPanel.Dock="Left" VerticalAlignment="Center" />
                    <ComboBox Margin="2" ItemsSource="{Binding StretchTypes}" SelectedItem="{Binding Stretch, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
