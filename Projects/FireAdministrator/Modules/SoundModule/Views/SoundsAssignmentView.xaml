<UserControl 
	x:Class="SoundsModule.Views.SoundsAssignmentView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lang="clr-namespace:Localization.Sounds.Views;assembly=Localization"
    xmlns:langCommon="clr-namespace:Localization.Sounds.Common;assembly=Localization"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">

	<DataGrid ItemsSource="{Binding Sounds}" SelectedItem="{Binding SelectedSound}" IsReadOnly="False" CanUserSortColumns="True">
		<DataGrid.Columns>
			<DataGridTemplateColumn
				SortMemberPath="JournalEventNameType"
				Header="{x:Static lang:CommonViews.Event}"
				Width="*">
				<DataGridTemplateColumn.CellTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<Image Width="16" Height="16" VerticalAlignment="Center" Source="{Binding Path=JournalEventNameType, Converter={StaticResource JournalEventNameTypeToIconConverter}}" Margin="2 0" />
							<TextBlock Text="{Binding Path=JournalEventNameType, Converter={StaticResource EnumToDescriptionConverter}}" Margin="2 0" />
						</StackPanel>
					</DataTemplate>
				</DataGridTemplateColumn.CellTemplate>
			</DataGridTemplateColumn>
			<DataGridTemplateColumn Header="{x:Static lang:CommonViews.Sound}" Width="1*">
				<DataGridTemplateColumn.CellTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding SelectedSoundFileViewModel.Name, FallbackValue={x:Static langCommon:CommonResources.None}}" />
					</DataTemplate>
				</DataGridTemplateColumn.CellTemplate>
				<DataGridTemplateColumn.CellEditingTemplate>
					<DataTemplate>
                        <FeaturedComboBox HorizontalAlignment="Stretch" Width="Auto" ItemsSource="{Binding AvailableSoundFileViewModels}" SelectedItem="{Binding SelectedSoundFileViewModel, UpdateSourceTrigger=PropertyChanged}" NullValueText="{x:Static langCommon:CommonResources.None}" CanSelectNullItem="True" >
							<FeaturedComboBox.ItemTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding Name}" Foreground="{DynamicResource ForegroundColorDark}" />
								</DataTemplate>
							</FeaturedComboBox.ItemTemplate>
						</FeaturedComboBox>
					</DataTemplate>
				</DataGridTemplateColumn.CellEditingTemplate>
			</DataGridTemplateColumn>
			<DataGridTemplateColumn Header="{x:Static lang:CommonViews.Continuously}" Width="1*">
				<DataGridTemplateColumn.CellTemplate>
					<DataTemplate>
						<CheckBox IsChecked="{Binding IsContinious, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
					</DataTemplate>
				</DataGridTemplateColumn.CellTemplate>
			</DataGridTemplateColumn>
		</DataGrid.Columns>
	</DataGrid>
</UserControl>