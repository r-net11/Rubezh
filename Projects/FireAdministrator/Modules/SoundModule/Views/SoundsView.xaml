<UserControl x:Class="SoundsModule.Views.SoundsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:SoundsModule.Converters">
    <UserControl.Resources>
        <Converters:BoolToStringConverter x:Key="BoolToStringConverter" />
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <DataGrid Margin="5" Grid.Column="0" ItemsSource="{Binding States}" SelectedItem="{Binding SelectedState}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}" Style="{StaticResource DataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Состояние" Width="1*" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Звук" Width="1*" Binding="{Binding Sound}" />
                    <DataGridTextColumn  Header="PC Speaker" Width="1*" Binding="{Binding Speaker}" />
                    <DataGridTextColumn Header="Непрерывно" Width="1*" Binding="{Binding IsContinious, Converter={StaticResource BoolToStringConverter}}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Grid.Row="1" DataContext="{Binding SelectedState}">
            <StackPanel>
                <TextBlock Text="Звук" Margin="2" />
                <ComboBox ItemsSource="{Binding AvailableSounds}" SelectedItem="{Binding Sound}" Margin="2" />
                <TextBlock Text="Встроенный динамик (PC Speaker)" Margin="2" />
                <ComboBox ItemsSource="{Binding AvailableSpeakers}" SelectedItem="{Binding Speaker}" Margin="2" />
                <CheckBox IsChecked="{Binding IsContinious}" Content="Воспроизводить звук непрерывно" Margin="2" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Row="0" Grid.Column="0" Content="Воспроизвести" Name="btnPlay" Width="100"/>
            <Button Grid.Row="0" Grid.Column="2" Content="Сохранить" Name="btnSave" Width="100"/>
            <Button Grid.Row="0" Grid.Column="3" Content="Отмена" Name="btnCansel" Width="100"/>
        </Grid>
    </Grid>
</UserControl>
