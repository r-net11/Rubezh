<UserControl x:Class="SoundsModule.Views.SoundsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:views="clr-namespace:SoundsModule.Views"
			 xmlns:lang="clr-namespace:Localization.Sounds.Views;assembly=Localization"
			 mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<TabControl
			SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="{x:Static lang:CommonViews.SoundAttach}">
				<views:SoundsAssignmentView DataContext="{Binding SoundsAssignmentViewModel}" />
			</TabItem>
            <TabItem Header="{x:Static lang:CommonViews.SoundFiles}">
				<views:SoundFilesView DataContext="{Binding SoundFilesViewModel}" />
			</TabItem>
		</TabControl>
    </Grid>
</UserControl>
