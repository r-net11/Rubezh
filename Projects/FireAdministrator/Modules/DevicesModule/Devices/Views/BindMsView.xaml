<UserControl x:Class="DevicesModule.Views.BindMsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="400">
    
<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Текущий серийный номер" Margin="2" Foreground="White" />
            <TextBlock Text="{Binding CurrentSerial, Mode=OneWay}" Margin="2" Foreground="White" />
            <TextBlock Text="Подключенные устройства" Margin="2" Foreground="White" />
        </StackPanel>

            <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding Serials}" SelectedItem="{Binding SelectedSerial}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Серийный номер" Width="*" Binding="{Binding .}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="Ок" Command="{Binding SaveCommand}" Width="100" Margin="2" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="2" />
        </StackPanel>
    </Grid>
</UserControl>
