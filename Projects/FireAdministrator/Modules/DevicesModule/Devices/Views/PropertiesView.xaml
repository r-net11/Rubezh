<UserControl x:Class="DevicesModule.Views.PropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:Controls;assembly=Controls" xmlns:Converters="clr-namespace:DevicesModule.Converters">

    <UserControl.Resources>
        <Style TargetType="Label" x:Key="PropertyCaptionStyle">
            <Setter Property="Content" Value="{Binding Caption}" />
            <Setter Property="ToolTip" Value="{Binding ToolTip}" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="White" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAUParameter}" Value="True">
                    <DataTrigger.Setters>
                        <Setter Property="Label.Foreground" Value="Gray" />
                    </DataTrigger.Setters>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />                
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Grid.Column="0">
                <StackPanel Visibility="{Binding AUParameterVis, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="group1" Width="600" />
                            <ColumnDefinition SharedSizeGroup="group2" Width="400" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                    <Grid Visibility="{Binding Device.Driver.CanMonitoringDisable, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="group1" />
                            <ColumnDefinition SharedSizeGroup="group2" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="Отключить мониторинг устройства" ToolTip="Не использовать устройство при периодическом опросе" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        <CheckBox Grid.Column="1" IsChecked="{Binding IsMonitoringDisabled}" Margin="2" VerticalAlignment="Center" />
                    </Grid>
                    <ItemsControl ItemsSource="{Binding StringProperties}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition SharedSizeGroup="group1" />
                                        <ColumnDefinition SharedSizeGroup="group2" />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Style="{StaticResource PropertyCaptionStyle}" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <TextBox Grid.Column="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Margin="2" VerticalAlignment="Center" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ItemsControl ItemsSource="{Binding BoolProperties}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition SharedSizeGroup="group1" />
                                        <ColumnDefinition SharedSizeGroup="group2" />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Style="{StaticResource PropertyCaptionStyle}" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <CheckBox Grid.Column="1" IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}" Margin="2" VerticalAlignment="Center" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ItemsControl ItemsSource="{Binding EnumProperties}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition SharedSizeGroup="group1" />
                                        <ColumnDefinition SharedSizeGroup="group2" />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Style="{StaticResource PropertyCaptionStyle}" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <ComboBox Grid.Column="1" ItemsSource="{Binding Parameters}" SelectedItem="{Binding SelectedParameter, UpdateSourceTrigger=PropertyChanged}" Margin="2" VerticalAlignment="Center" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                    <StackPanel Visibility="{Binding AUParameterVis, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition SharedSizeGroup="group1" Width="600" />
                                <ColumnDefinition SharedSizeGroup="group2" Width="400" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                        <Grid Visibility="{Binding Device.Driver.CanMonitoringDisable, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition SharedSizeGroup="group1" />
                                <ColumnDefinition SharedSizeGroup="group2" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Отключить мониторинг устройства" ToolTip="Не использовать устройство при периодическом опросе" HorizontalAlignment="Right" VerticalAlignment="Center" />
                            <CheckBox Grid.Column="1" IsChecked="{Binding IsMonitoringDisabled}" Margin="2" VerticalAlignment="Center" />
                        </Grid>
                        <ItemsControl ItemsSource="{Binding StringProperties}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition SharedSizeGroup="group1" />
                                            <ColumnDefinition SharedSizeGroup="group2" />
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Column="0" Style="{StaticResource PropertyCaptionStyle}" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}" />
                                        <TextBox Grid.Column="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Margin="2" VerticalAlignment="Center" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl ItemsSource="{Binding BoolProperties}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition SharedSizeGroup="group1" />
                                            <ColumnDefinition SharedSizeGroup="group2" />
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Column="0" Style="{StaticResource PropertyCaptionStyle}" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}" />
                                        <CheckBox Grid.Column="1" IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}" Margin="2" VerticalAlignment="Center" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl ItemsSource="{Binding EnumProperties}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition SharedSizeGroup="group1" />
                                            <ColumnDefinition SharedSizeGroup="group2" />
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Column="0" Style="{StaticResource PropertyCaptionStyle}" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}" />
                                        <ComboBox Grid.Column="1" ItemsSource="{Binding Parameters}" SelectedItem="{Binding SelectedParameter, UpdateSourceTrigger=PropertyChanged}" Margin="2" VerticalAlignment="Center" Visibility="{Binding IsAUParameter, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name}" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
            </Grid>
        </ScrollViewer>
        <Grid Column="1">
            <StackPanel>
                <Controls:ToggleButton Visibility="{Binding ChoiseAUVis, Converter={StaticResource BoolToVisibilityConverter}}" ToolTip="Параметры устройства" Command="{Binding OneCommand}" IsChecked="{Binding AUParameterVis}" ImageSource="/Controls;component/Images/devparameters.png"/>
                <Controls:ToggleButton Visibility="{Binding ChoiseBaseVis, Converter={StaticResource BoolToVisibilityConverter}}" ToolTip="Параметры адресного устройства" Command="{Binding TwoCommand}" IsChecked="{Binding AUParameterVis, Converter={StaticResource InverseBooleanConverter}}" ImageSource="/Controls;component/Images/adrparameters.png"/>
            </StackPanel>
        </Grid>
        <Grid Column="0" Row="1" Visibility="{Binding AUParameterVis, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" DataContext="{Binding Context.DeviceCommandsViewModel}">
                <Controls:ToolBarButton ToolTip="Считать параметры устройства"  Command="{Binding GetConfigurationParametersCommand}" ImageSource="/Controls;component/Images/readconfig.png"/>
                <Controls:ToolBarButton ToolTip="Записать параметры на устройство" Command="{Binding SetConfigurationParametersCommand}" ImageSource="/Controls;component/Images/writeconfig.png"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>