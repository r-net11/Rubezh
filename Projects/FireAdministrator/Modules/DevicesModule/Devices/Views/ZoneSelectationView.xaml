<UserControl x:Class="DevicesModule.Views.ZoneSelectationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        MinWidth="400" MinHeight="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" ItemsSource="{Binding Zones}" SelectedItem="{Binding SelectedZone}" SelectionChanged="OnSelectionChanged" MouseDoubleClick="OnMouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Zone.No}" Header="№" />
                    <GridViewColumn DisplayMemberBinding="{Binding Zone.Name}" Header="Наименование" />
                    <GridViewColumn DisplayMemberBinding="{Binding Zone.Description}" Header="Примечание" />
                    <GridViewColumn DisplayMemberBinding="{Binding DetectorCount}" Header="Число ИП для перехода в пожар" />
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding EditCommand}" Header="Редактировать" />
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding SaveCommand}" />
            </ListView.InputBindings>
        </ListView>
        
        <Button Grid.Row="1" Width="100" Margin="0, 2, 0, 2" HorizontalAlignment="Right" Command="{Binding CreateCommand}" Content="Создать" />
    </Grid>
</UserControl>