<UserControl x:Class="DevicesModule.Views.ZoneDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
             xmlns:Converters="clr-namespace:DevicesModule.Converters"
             MinWidth="600" MinHeight="400">

    <UserControl.Resources>
        <Converters:ZoneTypeToStringConverter x:Key="ZoneTypeToStringConverter" />
        <Converters:GuardZoneTypeToStringConverter x:Key="GuardZoneTypeToStringConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TabControl>

                <TabItem Header="Основные">
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер" Margin="2" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Наименование" Margin="2" />
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Примечание" Margin="2" />

                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding No}" Margin="2" IsEnabled="False" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}" Margin="2" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Description}" Margin="2" />
                    </Grid>
                </TabItem>

                <TabItem Header="Настройки зоны">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Назначение зоны" Margin="2" />
                        <ComboBox Grid.Row="1" ItemsSource="{Binding AvailableZoneTypes}" SelectedItem="{Binding ZoneType}" Margin="2">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ., Converter={StaticResource ZoneTypeToStringConverter}}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <Grid Grid.Row="2">

                            <Grid Visibility="{Binding IsFireZone, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="Число датчиков для формирования сигнала Пожар" Margin="2" />
                                    <extToolkit:IntegerUpDown Value="{Binding DetectorCount}" Minimum="1" Margin="2" />
                                    <TextBlock Text="Время эвакуации, сек(если указано, то при получении события о" Margin="2" />
                                    <TextBlock Text="включении МПТ или ППУ в зоне, ОЗ перейдет в режим эвакуации)" Margin="2" />
                                    <extToolkit:IntegerUpDown Value="{Binding EvacuationTime}" Minimum="0" Margin="2" />
                                </StackPanel>
                            </Grid>

                            <Grid Visibility="{Binding IsGuardZone, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="Вид зоны" Margin="2" />

                                    <ComboBox ItemsSource="{Binding AvailableGuardZoneTypes}" SelectedItem="{Binding GuardZoneType}" Margin="2">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding ., Converter={StaticResource GuardZoneTypeToStringConverter}}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <CheckBox Content="Тихая тревога" IsChecked="{Binding Skipped}" Margin="2" />
                                    <TextBlock Text="Входная/выходная задержка, сек" Margin="2" />
                                    <extToolkit:IntegerUpDown Value="{Binding Delay}" IsEnabled="{Binding CanAutoSet}" Minimum="0" Increment="10" Margin="2" />
                                    <TextBlock Text="Автоперевзятие, сек" Margin="2" />
                                    <extToolkit:IntegerUpDown Value="{Binding AutoSet}" Minimum="0" Margin="2" />
                                </StackPanel>
                            </Grid>

                        </Grid>

                    </Grid>
                </TabItem>

            </TabControl>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="OK" Command="{Binding SaveCommand}" Width="100" Margin="2" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="2" />
        </StackPanel>
    </Grid>
</UserControl>