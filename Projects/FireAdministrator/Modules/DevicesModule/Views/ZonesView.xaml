<UserControl x:Class="DevicesModule.Views.ZonesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:WpfToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Background="{StaticResource HeaderBackgroundBrush}">
        <WpfToolkit:DataGrid Margin="5" ItemsSource="{Binding Zones}" SelectedItem="{Binding SelectedZone}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                            AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column"
                            IsReadOnly="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            HorizontalGridLinesBrush="WhiteSmoke" VerticalGridLinesBrush="WhiteSmoke"
                            Background="White" BorderBrush="Black" BorderThickness="0"
                            Height="500" RowHeight="20">
            <WpfToolkit:DataGrid.Columns>
                <WpfToolkit:DataGridTextColumn Header="№" Width="1*" Binding="{Binding No}" />
                <WpfToolkit:DataGridTextColumn Header="Наименование" Width="2*" Binding="{Binding Name}" />
                <WpfToolkit:DataGridTextColumn  Header="Примечание" Width="2*" Binding="{Binding Description}" />
                <WpfToolkit:DataGridTextColumn Header="Число ИП для перехода в пожар" Width="2*" Binding="{Binding DetectorCount}" />
                <WpfToolkit:DataGridTextColumn Header="Время эвакуации" Width="2*" Binding="{Binding EvacuationTime}" />
            </WpfToolkit:DataGrid.Columns>
            <WpfToolkit:DataGrid.CellStyle>
                <Style TargetType="{x:Type WpfToolkit:DataGridCell}">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource RowSelectionBrush}" />
                            <Setter Property="Foreground" Value="Red" />
                        </Trigger>
                    </Style.Triggers>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type WpfToolkit:DataGridCell}">
                                <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                    <ContentPresenter VerticalAlignment="Center"></ContentPresenter>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </WpfToolkit:DataGrid.CellStyle>

        </WpfToolkit:DataGrid>

        <StackPanel DataContext="{Binding SelectedZone}" Width="500" HorizontalAlignment="Left">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Наименование" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Примечание" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Число ИП для перехода в пожар" />
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Время эвакуации" />

                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding No}" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Description}" />
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding DetectorCount}" />
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding EvacuationTime}" />
            </Grid>
        </StackPanel>

        <Button Content="Добавить зону" Command="{Binding AddZoneCommand}" HorizontalAlignment="Left" Height="40" />

    </StackPanel>
</UserControl>
