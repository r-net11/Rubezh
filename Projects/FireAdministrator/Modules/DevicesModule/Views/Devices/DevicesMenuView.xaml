<UserControl x:Class="DevicesModule.Views.DevicesMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <Button Content="Копировать" Command="{Binding CopyCommand}" Width="100" Margin="2" />
        <Button Content="Вырезать" Command="{Binding CutCommand}" Width="100" Margin="2" />
        <Button Content="Вставить" Command="{Binding PasteCommand}" Width="100" Margin="2" />
    </StackPanel>
</UserControl>
