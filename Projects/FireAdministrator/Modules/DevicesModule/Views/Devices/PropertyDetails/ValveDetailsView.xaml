<UserControl x:Class="DevicesModule.Views.ValveDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
             xmlns:XButtons="clr-namespace:Buttons;assembly=Buttons"
             xmlns:Converters="clr-namespace:DevicesModule.Converters"
             MinWidth="300" MinHeight="300">

    <UserControl.Resources>
        <Converters:ValveActionConverter x:Key="ValveActionConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Действие при событии в зоне:" Margin="2" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Задержка запуска, с:" Margin="2" />

            <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Actions}" SelectedItem="{Binding SelectiedAction}" Margin="2">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource ValveActionConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <extToolkit:IntegerUpDown Grid.Row="1" Grid.Column="1" Value="{Binding Timeout}" Minimum="0" Margin="2" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <XButtons:XButton Content="Ок" Command="{Binding SaveCommand}" Width="100" Height="30" Margin="2" Background="Red" />
            <XButtons:XButton Content="Отмена" Command="{Binding CancelCommand}" Width="100" Height="30" Margin="2" Background="YellowGreen" />
        </StackPanel>

    </Grid>
</UserControl>