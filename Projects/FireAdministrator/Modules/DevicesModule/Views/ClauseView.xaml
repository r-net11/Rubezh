<UserControl x:Class="DevicesModule.Views.ClauseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:DevicesModule.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <Converters:ClauseOperationToStringConverter x:Key="ClauseOperationToStringConverter" />
        <Converters:ClauseStateToStringConverter x:Key="ClauseStateToStringConverter" />
    </UserControl.Resources>
    <Grid Height="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="состояние" />

        <ComboBox Grid.Column="1" ItemsSource="{Binding States}" SelectedItem="{Binding SelectedState}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ., Converter={StaticResource ClauseStateToStringConverter}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <ComboBox Grid.Column="2" ItemsSource="{Binding Operations}" SelectedItem="{Binding SelectedOperation}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ., Converter={StaticResource ClauseOperationToStringConverter}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Button Grid.Column="3" Command="{Binding ShowZonesCommand}">
            <TextBlock Text="{Binding PresenrationZones}" />
        </Button>
    </Grid>
</UserControl>
