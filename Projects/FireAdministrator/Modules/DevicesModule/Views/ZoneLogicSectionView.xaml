<UserControl x:Class="DevicesModule.Views.ZoneLogicSectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:WpfToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
        MinHeight="300" MinWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <WpfToolkit:DataGrid ItemsSource="{Binding TargetZones}" SelectedItem="{Binding SelectedTargetZone}"
                         ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                         AlternatingRowBackground="{StaticResource AlternateRowBackgroundBrush}"
                         AutoGenerateColumns="False" SelectionMode="Single"
                         CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                         IsReadOnly="True" SelectionUnit="FullRow" HorizontalGridLinesBrush="Black" VerticalGridLinesBrush="Black"
                         Background="White" BorderBrush="Black" BorderThickness="0"
                         RowHeaderWidth="0" RowHeight="25" HorizontalAlignment="Stretch">
                    <WpfToolkit:DataGrid.Columns>
                        <WpfToolkit:DataGridTextColumn Header="№" Binding="{Binding No}" Width="auto" />
                        <WpfToolkit:DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*" />
                        <WpfToolkit:DataGridTextColumn Header="Примечание" Binding="{Binding Description}" Width="*" />
                    </WpfToolkit:DataGrid.Columns>
                </WpfToolkit:DataGrid>
            </Grid>

            <StackPanel Grid.Column="1">
                <Button Content="+" Command="{Binding AddOneCommand}" />
                <Button Content="-" Command="{Binding RemoveOneCommand}" />
                <Button Content="++" Command="{Binding AddAllCommand}" />
                <Button Content="--" Command="{Binding RemoveAllCommand}" />
            </StackPanel>

            <Grid Grid.Column="2">
                <WpfToolkit:DataGrid ItemsSource="{Binding SourceZones}" SelectedItem="{Binding SelectedSourceZone}"
                         ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                         AlternatingRowBackground="{StaticResource AlternateRowBackgroundBrush}"
                         AutoGenerateColumns="False" SelectionMode="Single"
                         CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                         IsReadOnly="True" SelectionUnit="FullRow" HorizontalGridLinesBrush="Black" VerticalGridLinesBrush="Black"
                         Background="White" BorderBrush="Black" BorderThickness="0"
                         RowHeaderWidth="0" RowHeight="25" HorizontalAlignment="Stretch">
                    <WpfToolkit:DataGrid.Columns>
                        <WpfToolkit:DataGridTextColumn Header="№" Binding="{Binding No}" Width="auto" />
                        <WpfToolkit:DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*" />
                        <WpfToolkit:DataGridTextColumn Header="Примечание" Binding="{Binding Description}" Width="*" />
                    </WpfToolkit:DataGrid.Columns>
                </WpfToolkit:DataGrid>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Button Content="Ok" Command="{Binding SaveCommand}" />
        </Grid>
        
    </Grid>
</UserControl>
