<UserControl x:Class="DevicesModule.Views.ZoneLogicView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        MinHeight="600" MinWidth="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="2">
            <Button Content="Удалить" Command="{Binding RemoveCommand}" Width="100" />
            <Button Content="Добавть" Command="{Binding AddCommand}" Width="100" />
        </StackPanel>
        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding Clauses}" SelectedItem="{Binding SelectedClause}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                            AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column"
                            IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            HorizontalGridLinesBrush="WhiteSmoke" VerticalGridLinesBrush="WhiteSmoke"
                            Background="White" BorderBrush="Black" BorderThickness="0"
                            RowHeight="20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Состояние" Binding="{Binding SelectedState, Converter={StaticResource ClauseStateToStringConverter}}" />
                    <DataGridTextColumn Header="Операция" Binding="{Binding SelectedOperation, Converter={StaticResource ClauseOperationToStringConverter}}" />
                    <DataGridTextColumn Header="Зоны" Binding="{Binding PresenrationZones}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Row="2" DataContext="{Binding SelectedClause}" Orientation="Horizontal" Margin="2">
            <ComboBox ItemsSource="{Binding States}" SelectedItem="{Binding SelectedState}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource ClauseStateToStringConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <ComboBox ItemsSource="{Binding Operations}" SelectedItem="{Binding SelectedOperation}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource ClauseOperationToStringConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Button Command="{Binding ShowZonesCommand}" MinWidth="100">
                <TextBlock Text="{Binding PresenrationZones}" />
            </Button>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ok" Command="{Binding SaveCommand}" Width="100" Margin="2" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="2" />
        </StackPanel>
    </Grid>
</UserControl>