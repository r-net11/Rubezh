<UserControl x:Class="DevicesModule.Views.DirectionsMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <Button Content="Добавить" Command="{Binding AddCommand}" Width="100" Margin="2" />
        <Button Content="Удалить" Command="{Binding DeleteCommand}" Width="100" Margin="2" />
        <Button Content="Редактировать" Command="{Binding EditCommand}" Width="100" Margin="2" />
    </StackPanel>
</UserControl>
