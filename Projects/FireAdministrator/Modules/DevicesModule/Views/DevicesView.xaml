<UserControl x:Class="DevicesModule.Views.DevicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:WpfToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
             xmlns:Converters="clr-namespace:DevicesModule.Converters"
             xmlns:Views="clr-namespace:DevicesModule.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="600">
    <UserControl.Resources>
        <Converters:LevelToMarginConverter x:Key="LevelToMarginConverter" />
        <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Converters:BoolToVisibilityRevertConverter x:Key="BoolToVisibilityRevertConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <WpfToolkit:DataGrid ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                            AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column"
                            IsReadOnly="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            HorizontalGridLinesBrush="WhiteSmoke" VerticalGridLinesBrush="WhiteSmoke"
                            Background="White" BorderBrush="Black" BorderThickness="0"
                            RowHeight="20">
                <WpfToolkit:DataGrid.Columns>
                    <WpfToolkit:DataGridTemplateColumn Header="Тип Устройства" Width="250">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="{Binding Level, Converter={StaticResource LevelToMarginConverter}}">
                                        <StackPanel Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <CheckBox Style="{StaticResource TreeExpanderStyle}" IsChecked="{Binding IsExpanded, Mode=TwoWay}"/>
                                        </StackPanel>
                                        <StackPanel Visibility="{Binding HasImage, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <Image Source="{Binding ImageSource}" Width="16" Height="16" Margin="5, 0, 5, 0" />
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding ShortDriverName}">
                                        <TextBlock.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Добавить" Command="{Binding AddCommand}" Visibility="{Binding CanAddChildren, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                <MenuItem Header="Добавить несколько" Command="{Binding AddManyCommand}" Visibility="{Binding CanAddChildren, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                <MenuItem Header="Удалить" Command="{Binding RemoveCommand}" />
                                            </ContextMenu>
                                        </TextBlock.ContextMenu>
                                        </TextBlock>
                                    </StackPanel>

                                </Grid>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                    <WpfToolkit:DataGridTemplateColumn Header="Адрес" Width="100">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Address}" />
                                </StackPanel>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                        <WpfToolkit:DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <StackPanel Visibility="{Binding CanEditAddress, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBox Text="{Binding Address}" BorderThickness="0" Name="addressTextBox" />
                                    </StackPanel>
                                    <StackPanel Visibility="{Binding CanEditAddress, Converter={StaticResource BoolToVisibilityRevertConverter}}">
                                        <TextBlock Text="{Binding Address}" />
                                    </StackPanel>
                                </StackPanel>
                                <DataTemplate.Triggers>
                                    <Trigger SourceName="addressTextBox" Property="IsVisible" Value="True">
                                        <Setter TargetName="addressTextBox" Property="FocusManager.FocusedElement" Value="{Binding ElementName=addressTextBox}"/>
                                    </Trigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellEditingTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                    <WpfToolkit:DataGridTemplateColumn Header="Зона" Width="100">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" DataContext="{Binding Zone}">
                                    <TextBlock Text="{Binding PresentationName}" />
                                    <StackPanel.Style>
                                        <Style>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                                    <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                </StackPanel>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                        <WpfToolkit:DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <StackPanel Visibility="{Binding IsZoneDevice, Converter={StaticResource BoolToVisibilityConverter}}" VerticalAlignment="Stretch">
                                        <ComboBox ItemsSource="{Binding Zones}" SelectedItem="{Binding Zone}" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" Height="19">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding PresentationName}" VerticalAlignment="Stretch" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Visibility="{Binding IsZoneLogicDevice, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Button Content="..." Command="{Binding ShowZoneLogicCommand}" Height="19"/>
                                    </StackPanel>
                                    <StackPanel Visibility="{Binding IsIndicatorDevice, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Button Content="..." Command="{Binding ShowIndicatorLogicCommand}" Height="19"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellEditingTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                    <WpfToolkit:DataGridTemplateColumn Header="Примечание" Width="*">
                        <WpfToolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Description}" />
                                </StackPanel>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellTemplate>
                        <WpfToolkit:DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <StackPanel HorizontalAlignment="Stretch" Background="Red">
                                    <TextBox Text="{Binding Description}" BorderThickness="0" HorizontalAlignment="Stretch" Name="descriptionTextBox" />
                                </StackPanel>
                                <DataTemplate.Triggers>
                                    <Trigger SourceName="descriptionTextBox" Property="IsVisible" Value="True">
                                        <Setter TargetName="descriptionTextBox" Property="FocusManager.FocusedElement" Value="{Binding ElementName=descriptionTextBox}"/>
                                    </Trigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </WpfToolkit:DataGridTemplateColumn.CellEditingTemplate>
                    </WpfToolkit:DataGridTemplateColumn>

                </WpfToolkit:DataGrid.Columns>

            </WpfToolkit:DataGrid>
        </Grid>

        <Grid Grid.Row="1">
            <StackPanel DataContext="{Binding SelectedDevice}" Width="400" HorizontalAlignment="Left">

                <TextBlock Text="Устройство" FontWeight="Bold" Foreground="DarkOrange" />
                <StackPanel Orientation="Horizontal">
                    <Image Source="{Binding ImageSource}" Width="16" />
                    <TextBlock Text="{Binding DriverName}" />
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Подключено к" FontWeight="Bold" Foreground="YellowGreen" />
                    <TextBlock Text="{Binding ConnectedTo}" />
                    <StackPanel.Style>
                        <Style>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectedTo}" Value="{x:Null}" >
                                    <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                </StackPanel>

                <StackPanel DataContext="{Binding Zone}">
                    <TextBlock Text="Зона" FontWeight="Bold" Foreground="YellowGreen" />
                    <TextBlock Text="{Binding PresentationName}" />
                    <StackPanel.Style>
                        <Style>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                    <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                </StackPanel>

                <Views:PropertiesView DataContext="{Binding PropertiesViewModel}" />
                
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
