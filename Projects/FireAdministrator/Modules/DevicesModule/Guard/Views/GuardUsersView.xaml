<UserControl x:Class="DevicesModule.Views.GuardUsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid Margin="5" Grid.Column="0" ItemsSource="{Binding GuardUsers}" SelectedItem="{Binding SelectedGuardUser}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}" Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Width="1*" Binding="{Binding GuardUser.Id}" />
                <DataGridTextColumn Header="Наименование" Width="1*" Binding="{Binding GuardUser.Name}" />
                <DataGridTextColumn Header="ФИО" Width="1*" Binding="{Binding GuardUser.FIO}" />
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Setter Property="DataContext" Value="{Binding RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Добавить" Command="{Binding AddCommand}" />
                                <MenuItem Header="Удалить" Command="{Binding DeleteCommand}" />
                                <MenuItem Header="Редактировать" Command="{Binding EditCommand}" />
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</UserControl>
