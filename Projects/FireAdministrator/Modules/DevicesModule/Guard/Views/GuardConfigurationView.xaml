<UserControl x:Class="DevicesModule.Views.GuardConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             MinWidth="500" MinHeight="400">
    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="30"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition MaxHeight="30"/>
        </Grid.RowDefinitions> 
                
        <DataGrid DataContext="Список охранных пользователей прибора" Grid.Row="0" Margin ="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Foreground="White" FontSize="14"/>
        <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" Grid.Row="1" Margin="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Width="50" Binding="{Binding GuardUser.Id}"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding GuardUser.Name}" />
                <DataGridTextColumn Header="Пароль" Binding="{Binding GuardUser.Password}" />
                <DataGridCheckBoxColumn Header="Постановка на охрану" Binding="{Binding GuardUser.CanSetZone}" />
                <DataGridCheckBoxColumn Header="Снятие с охраны" Binding="{Binding GuardUser.CanUnSetZone}" />
                <DataGridTextColumn Header="Ключ" Binding="{Binding GuardUser.KeyTM}" Width="1*" />
            </DataGrid.Columns>
        </DataGrid>        
        <DataGrid ItemsSource="{Binding UserZones}" Grid.Row="2" Margin="1" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Список охраняемых зон" Binding="{Binding PresentationName}" Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="3" Margin="1">
            <DockPanel>
                <Button Content=" Применить " Command="{Binding AcceptCommand}" HorizontalAlignment="Right" Margin="3" />
                <Button Content=" Отмена " Command="{Binding CancelCommand}" HorizontalAlignment="Right" Margin="3" />
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
