<UserControl x:Class="DevicesModule.Views.LevelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid Grid.Column="0" ItemsSource="{Binding Levels}" SelectedItem="{Binding SelectedLevel}" SelectionChanged="DataGrid_SelectionChanged" PreviewMouseDown="DataGrid_PreviewMouseDown" MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding GuardLevel.Name}" />
                <DataGridTextColumn Header="Зоны" Binding="{Binding Zones}" Width="*" />
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Добавить" Command="{Binding AddCommand}" />
                    <MenuItem Header="Удалить" Command="{Binding DeleteCommand}" />
                    <MenuItem Header="Редактировать" Command="{Binding EditCommand}" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</UserControl>