<UserControl x:Class="DevicesModule.Plans.Views.ZonePropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             MinWidth="400" MinHeight="400">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
        <DataGrid Grid.Row="0" ItemsSource="{Binding Zones}" SelectedItem="{Binding SelectedZone}" SelectionChanged="OnSelectionChanged" CanUserSortColumns="True" Name="ZonesDataGrid">
            <DataGrid.Resources>
                <InputBindingCollection x:Key="inputCollection" x:Shared="False">
                    <MouseBinding Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" MouseAction="LeftDoubleClick" />
                </InputBindingCollection>
            </DataGrid.Resources>
            <DataGrid.ItemContainerStyle>
                <Style BasedOn="{StaticResource {x:Type DataGridRow}}" TargetType="{x:Type DataGridRow}">
                    <Setter Property="Controls:Attach.InputBindings" Value="{StaticResource inputCollection}" />
                </Style>
            </DataGrid.ItemContainerStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Zone.No}" Header="№" />
                <DataGridTextColumn Binding="{Binding Zone.Name}" Header="Наименование" />
                <DataGridTextColumn Binding="{Binding Zone.Description}" Header="Примечание" Width="*" />
                </DataGrid.Columns>
        </DataGrid>
		<CheckBox Grid.Row="1" VerticalAlignment="Center" IsChecked="{Binding IsHidden, UpdateSourceTrigger=PropertyChanged}" Content="Невидимая зона"/>
		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="100" Command="{Binding EditCommand}" Content="Редактировать" Margin="2" />
            <Button Width="100" Command="{Binding CreateCommand}" Content="Создать" Margin="2" />
        </StackPanel>
	</Grid>
</UserControl>