<UserControl x:Class="InstructionsModule.Views.InstructionDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:InstructionsModule.Converters" 
             xmlns:XButtons="clr-namespace:Controls;assembly=Controls"
             MinWidth="400" MinHeight="200" >
    <UserControl.Resources>
        <Converters:InstructionTypeToStringConverter  x:Key="InstructionTypeToStringConverter" />
    </UserControl.Resources>
    <Grid Margin="0" Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" TextBlock.FontSize="14" Margin="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Название" Margin="2"/>
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Текст" Margin="2"/>
            <TextBlock Grid.Column="0" Grid.Row="2" Text="Состояние" Margin="2"/>
            <TextBlock Grid.Column="0" Grid.Row="3" Text="Тип инструкции" Margin="2"/>
            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Name}" />
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Text}" />
            <ComboBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding AvailableStates}"
                      SelectedItem="{Binding StateType, Converter={StaticResource StateTypeToStringConverter}, UpdateSourceTrigger=PropertyChanged}"
                      Text="{Binding StateType}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource StateTypeToStringConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox Grid.Column="1" Grid.Row="3" ItemsSource="{Binding AvailableInstructionsType}"
                      SelectedItem="{Binding InstructionType, Converter={StaticResource InstructionTypeToStringConverter}, UpdateSourceTrigger=PropertyChanged}"
                      Text="{Binding InstructionType}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource InstructionTypeToStringConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <XButtons:XButton Grid.Column="0" Name="btnSelectZone" FontWeight="DemiBold" Background="Black" 
                              Width="115" Height="30" VerticalAlignment="Center" FontSize="10"
                              Command="{Binding SelectZoneCommand}" Content="Выбор зон" />
            <XButtons:XButton Grid.Column="1" Name="btnSelectDevice" FontWeight="DemiBold" Background="Black"
                              Width="115" Height="30" VerticalAlignment="Center" FontSize="10"
                              Command="{Binding SelectDeviceCommand}" Content="Выбор устройств" />
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <XButtons:XButton Grid.Column="1" Name="btnOk" FontWeight="Black" Background="Black"
                              Width="100" Height="30" VerticalAlignment="Center" 
                              Command="{Binding SaveCommand}" Content="OK"/>
            <XButtons:XButton Grid.Column="1" Name="btnCancel" FontWeight="Black" Background="Black"
                              Width="100" Height="30" VerticalAlignment="Center" 
                              Command="{Binding CancelCommand}" Content="Отмена" />
        </StackPanel>
        
    </Grid>
</UserControl>
