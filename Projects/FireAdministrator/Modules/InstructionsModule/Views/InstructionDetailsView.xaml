<UserControl x:Class="InstructionsModule.Views.InstructionDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:InstructionsModule.Converters" 
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             MinWidth="400" MinHeight="200" >
    <Grid Margin="00" Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" TextBlock.FontSize="14" Margin="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Номер" Margin="2" />
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Текст" Margin="2" />
            <TextBlock Grid.Column="0" Grid.Row="2" Text="Состояние" Margin="2" />
            <TextBlock Grid.Column="0" Grid.Row="3" Text="Тип инструкции" Margin="2" />
            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding InstructionNo}" HorizontalAlignment="Center" />
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox Grid.Column="1" Grid.Row="2" Margin="2" ItemsSource="{Binding AvailableStates}"
                      SelectedItem="{Binding StateType, UpdateSourceTrigger=PropertyChanged}"
                      Text="{Binding StateType}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource StateTypeToStringConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox Grid.Column="1" Grid.Row="3" Margin="2" ItemsSource="{Binding AvailableInstructionsType}"
                      SelectedItem="{Binding InstructionType, UpdateSourceTrigger=PropertyChanged}"
                      Text="{Binding InstructionType}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ., Converter={StaticResource InstructionTypeToStringConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                <Controls:XButton Grid.Column="0" Margin="2" Name="btnSelectZone" FontWeight="DemiBold" Background="Black" 
                              Height="30" VerticalAlignment="Center" FontSize="10"
                              Command="{Binding SelectZoneCommand}" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding SelectZones}" Margin="2" />
                        <TextBlock Margin="2">
                            <TextBlock.Style>
                                <Style>
                                    <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectZones}" Value="{x:Null}">
                                        <Setter Property="TextBlock.Text" Value="Нажмите для выбора зон" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectZones}" Value="">
                                        <Setter Property="TextBlock.Text" Value="Нажмите для выбора зон" />
                                    </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Controls:XButton>
                <Controls:XButton Grid.Column="1" Margin="2" Name="btnSelectDevice" FontWeight="DemiBold" Background="Black"
                              Height="30" VerticalAlignment="Center" FontSize="10"
                              Command="{Binding SelectDeviceCommand}" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding SelectDevices}" Margin="2" />
                        <TextBlock Margin="2">
                            <TextBlock.Style>
                                <Style>
                                    <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectDevices}" Value="{x:Null}">
                                        <Setter Property="TextBlock.Text" Value="Нажмите для выбора устройств" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectDevices}" Value="">
                                        <Setter Property="TextBlock.Text" Value="Нажмите для выбора устройств" />
                                    </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Controls:XButton>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Controls:XButton Grid.Column="1" Name="btnOk" FontWeight="Black" Background="Black"
                              Width="100" Height="30" Margin="2" VerticalAlignment="Center" 
                              Command="{Binding SaveCommand}" Content="OK"/>
            <Controls:XButton Grid.Column="1" Name="btnCancel" FontWeight="Black" Background="Black"
                              Width="100" Height="30" Margin="2" VerticalAlignment="Center" 
                              Command="{Binding CancelCommand}" Content="Отмена" />
        </StackPanel>
        
    </Grid>
</UserControl>
