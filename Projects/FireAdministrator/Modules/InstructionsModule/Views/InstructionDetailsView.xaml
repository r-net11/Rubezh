<UserControl x:Class="InstructionsModule.Views.InstructionDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="400">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Название" />
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Текст" />
            <TextBlock Grid.Column="0" Grid.Row="2" Text="Состояние" />
            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Name}" />
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Text}" />
            <ComboBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding AvailableStates}" Text="{Binding StateType}" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Button Grid.Column="0" Content="Настройка зон" Command="{Binding SetZoneCommand}" Width="115" Margin="2" />
            <Button Grid.Column="1" Content="Выбор устройств" Width="115" Margin="2" />
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Ok" Command="{Binding SaveCommand}" Width="100" Margin="2" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="2" />
        </StackPanel>
        
    </Grid>
</UserControl>
