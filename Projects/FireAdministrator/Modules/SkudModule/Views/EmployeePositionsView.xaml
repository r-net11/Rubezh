<UserControl x:Class="SKDModule.Views.EmployeePositionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             xmlns:Converters="clr-namespace:Controls.Converters;assembly=Controls">

    <Controls:XDataGrid ItemsSource="{Binding Dictionary}" SelectedItem="{Binding SelectedItem}" CanUserSortColumns="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Значение" Width="*" MinWidth="100" Binding="{Binding Item.Value}"/>
        </DataGrid.Columns>
    </Controls:XDataGrid>
    
</UserControl>
