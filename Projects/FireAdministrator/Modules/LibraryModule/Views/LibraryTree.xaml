<UserControl x:Class="LibraryModule.Views.LibraryTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:LibraryModule.ViewModels"
             xmlns:Converters="clr-namespace:LibraryModule.Converters"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Converters:CollectionViewFactoryConverter x:Key="CollectionViewFactoryConverter" />              
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Visible">
        <TreeView Name="DevicesTree" 
                  ItemsSource="{Binding DeviceViewModels, 
                                        Converter={StaticResource CollectionViewFactoryConverter}, 
                                        ConverterParameter=Driver.Name}"
                  SelectedItemChanged="LibraryTree_SelectedItemChanged">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding StateViewModels,
                                        Converter={StaticResource CollectionViewFactoryConverter}, 
                                        ConverterParameter=Name}">
                    <WrapPanel Margin="0,3">
                        <Image Source="{Binding Driver.ImageSource}" Width="14" Margin="5,0" />
                        <TextBlock Text="{Binding Driver.DriverName}" />
                    </WrapPanel>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <CheckBox IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"
                                      Visibility="{Binding IsAdditional, 
                                      Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <TextBlock Text="{Binding Name}" 
                                       ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
                            </WrapPanel>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </ScrollViewer>
</UserControl>
