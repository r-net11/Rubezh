<UserControl x:Class="LibraryModule.Views.LibraryTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:LibraryModule.ViewModels"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        
        <CollectionViewSource x:Key="SortedDevices" Source="{Binding Devices}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="Name" Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
        
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="{Binding ElementName=tree, Path=SelectedItem.Name}" Background="Yellow" />
        <TreeView Name="DevicesTree" ItemsSource="{Binding Source={StaticResource SortedDevices}}"
                  SelectedItemChanged="LibraryTree_SelectedItemChanged">
        <!--<TreeView.Resources>
            <CollectionViewSource x:Key="SortedStates" Source="{Binding States}">
                <CollectionViewSource.SortDescriptions>
                    <scm:SortDescription PropertyName="Name" Direction="Ascending" />
                </CollectionViewSource.SortDescriptions>
            </CollectionViewSource>
        </TreeView.Resources>-->
        <TreeView.ItemTemplate>
            <HierarchicalDataTemplate ItemsSource = "{Binding States}">
                <WrapPanel Margin="0,3">
                    <Image Source="{Binding IconPath}" Width="14" Margin="5,0" />
                    <TextBlock Text="{Binding Name}" />
                </WrapPanel>
                <HierarchicalDataTemplate.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel>
                            <CheckBox IsChecked="{Binding IsChecked}"
                                      Visibility="{Binding IsAdditional, 
                                      Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <TextBlock Text="{Binding Name}" 
                                       ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
                        </WrapPanel>
                    </DataTemplate>
                </HierarchicalDataTemplate.ItemTemplate>
            </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
    </StackPanel>
</UserControl>
