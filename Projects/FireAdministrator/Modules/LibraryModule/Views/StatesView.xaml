<UserControl x:Class="LibraryModule.Views.StatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase">
    <UserControl.Resources>
        <CollectionViewSource x:Key="SortedStates" Source="{Binding StateViewModels}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="Name" Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <ListBox Name="lb" ItemsSource="{Binding Source={StaticResource SortedStates}}" SelectedItem="{Binding SelectedStateViewModel}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <WrapPanel Margin="40,0,0,0">
                    <CheckBox IsChecked="{Binding IsChecked}" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked"
                              Visibility="{Binding IsAdditional, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock Text="{Binding Name}"
                               ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
                </WrapPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>

        <ListBox.ItemContainerStyle>
            <Style TargetType="{x:Type ListBoxItem}">
                <Style.Setters>
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Удалить состояние" Command="{Binding RemoveStateCommand}" />
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style.Setters>
            </Style>
        </ListBox.ItemContainerStyle>
    </ListBox>
</UserControl>