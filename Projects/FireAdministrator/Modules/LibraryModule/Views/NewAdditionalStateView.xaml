<UserControl x:Class="LibraryModule.Views.NewAdditionalStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:WpfToolkit="http://schemas.microsoft.com/wpf/2008/toolkit"
             mc:Ignorable="d" MinWidth="300" MinHeight="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <WpfToolkit:DataGrid Margin="5" Grid.Column="0" ItemsSource="{Binding States}" SelectedItem="{Binding SelectedState}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                            AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column"
                            IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            HorizontalGridLinesBrush="WhiteSmoke" VerticalGridLinesBrush="WhiteSmoke"
                            Background="White" BorderBrush="Black" BorderThickness="0"
                            RowHeight="20">
                <WpfToolkit:DataGrid.Columns>
                    <WpfToolkit:DataGridTextColumn Header="Состояние" Binding="{Binding Name}" Width="Auto" />
                    <WpfToolkit:DataGridTextColumn Header="Класс состояния" Binding="{Binding ClassName}" Width="*" />
                </WpfToolkit:DataGrid.Columns>
            </WpfToolkit:DataGrid>
        </Grid>
        <Grid Grid.Row="1">
            <DockPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="ОК" Width="100" HorizontalAlignment="Right" Command="{Binding AddCommand}" IsEnabled="{Binding IsEnabled}" />
                <Button Content="Отмена" Width="100" HorizontalAlignment="Right" Command="{Binding CancelCommand}" />
            </DockPanel>
        </Grid >
    </Grid>
</UserControl>
