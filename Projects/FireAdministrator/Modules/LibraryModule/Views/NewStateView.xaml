<UserControl x:Class="LibraryModule.Views.NewStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinWidth="300" MinHeight="300">
    <Grid Margin="10">
        <ListBox ItemsSource="{Binding States}" SelectedItem="{Binding SelectedState}" SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <DockPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="ОК" Width="100" HorizontalAlignment="Right" Command="{Binding AddCommand}" IsEnabled="{Binding IsEnabled}" />
            <Button Content="Отмена" Width="100" HorizontalAlignment="Right" Command="{Binding CancelCommand}" />
        </DockPanel>
    </Grid>
</UserControl>
