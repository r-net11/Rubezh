<UserControl x:Class="LibraryModule.Views.NewDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid Grid.Row="0">
                <ListBox Background="Transparent" ItemsSource="{Binding Items}" BorderThickness="0" SelectedItem="{Binding SelectedItem}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <Image Source="{Binding IconPath}" Width="12" Margin="0,0,4,4" />
                                <TextBlock Text="{Binding Name}" />
                            </DockPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </ScrollViewer>
        <Grid Grid.Row="1">
            <DockPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="ОК" HorizontalAlignment="Right" Width="100" Command="{Binding AddCommand}" />
                <Button Content="Отмена" HorizontalAlignment="Right" Width="100" Command="{Binding CancelCommand}" />
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
