<UserControl x:Class="LibraryModule.Views.StatesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinWidth="500" MinHeight="500">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding States}" SelectedItem="{Binding SelectedState}" SelectionMode="Single" CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Состояние" Binding="{Binding Name}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel VerticalAlignment="Bottom">
            <Button Content="Добавить состояние" Command="{Binding AddCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
