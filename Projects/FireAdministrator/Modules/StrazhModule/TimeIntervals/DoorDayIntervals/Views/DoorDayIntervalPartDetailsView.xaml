<UserControl x:Class="StrazhModule.Views.DoorDayIntervalPartDetailsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
			 Width="500" Height="200">
	<Grid Background="{StaticResource BackgroundBrush}" >
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<TextBlock Grid.Row="0" Grid.Column="0" Margin="5 2 2 2" Foreground="{DynamicResource ForegroundColorLight}" Text="Начало" />
			<Controls:TimePicker Grid.Row="0" Grid.Column="1" Margin="2 2 5 2" TimeSpan="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="1" Grid.Column="0" Margin="5 2 2 2" Foreground="{DynamicResource ForegroundColorLight}" Text="Окончание" />
			<Controls:TimePicker Grid.Row="1" Grid.Column="1" Margin="2 2 5 2" TimeSpan="{Binding EndTime, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="2" Grid.Column="0" Margin="5 2 2 2" Foreground="{DynamicResource ForegroundColorLight}" Text="Метод открытия" />
			<ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding AvailableDoorOpenMethods}" SelectedItem="{Binding SelectedDoorOpenMethod}" Margin="2 2 5 2">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
		</Grid>
		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Image Grid.Column="0" Source="/Controls;component/Images/Alarm_main_2_Attention.png" Margin="5 2 2 2" VerticalAlignment="Center"/>
			<TextBlock Grid.Column="1" Margin="2 2 5 2" TextWrapping="Wrap" Foreground="{DynamicResource ForegroundColorLight}" Text="Метод открытия, заданный в интервале, действует до момента начала следующего интервала" VerticalAlignment="Center"/>
		</Grid>
	</Grid>
</UserControl>