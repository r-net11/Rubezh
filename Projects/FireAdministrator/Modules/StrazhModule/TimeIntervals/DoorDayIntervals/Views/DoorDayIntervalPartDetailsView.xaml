<UserControl
	x:Class="StrazhModule.Views.DoorDayIntervalPartDetailsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:Controls="clr-namespace:Controls;assembly=Controls"
	xmlns:lang="clr-namespace:Localization.Strazh.Views;assembly=Localization"
	Width="500"
	Height="200">
	<Border Background="{DynamicResource BackgroundBrush}">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>
			<Grid Grid.Row="0" Margin="10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Margin="2" Foreground="{DynamicResource ForegroundColorLight}" Text="{x:Static lang:CommonViews.Start}" />
				<Controls:TimePicker Grid.Row="0" Grid.Column="1" Margin="2" TimeSpan="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}" MinTimeSpan="{Binding MinStartTime, Mode=OneWay}" MaxTimeSpan="{Binding MaxEndTime, Mode=OneWay}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="2" Foreground="{DynamicResource ForegroundColorLight}" Text="{x:Static lang:CommonViews.End}" />
				<Controls:TimePicker Grid.Row="1" Grid.Column="1" Margin="2" TimeSpan="{Binding EndTime, UpdateSourceTrigger=PropertyChanged}" MinTimeSpan="{Binding MinStartTime, Mode=OneWay}" MaxTimeSpan="{Binding MaxEndTime, Mode=OneWay}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="2" Foreground="{DynamicResource ForegroundColorLight}" Text="{x:Static lang:CommonViews.OpenMethod}" />
				<ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding AvailableDoorOpenMethods}" SelectedItem="{Binding SelectedDoorOpenMethod}" Margin="2">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
			</Grid>
			<Grid Grid.Row="1">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>
				<Image Grid.Column="0" Source="/Controls;component/Images/Alarm_main_2_Attention.png" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{x:Static lang:CommonViews.OpenMethodRange}" Foreground="{DynamicResource ForegroundColorLight}" TextWrapping="Wrap" VerticalAlignment="Center"/>
			</Grid>
		</Grid>
	</Border>
</UserControl>