<UserControl
	x:Class="StrazhModule.Views.DoorDayIntervalDetailsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:lang="clr-namespace:Localization.Strazh.Views;assembly=Localization"
	Width="500"
	Height="200">
	<Border Background="{DynamicResource BackgroundBrush}">
		<Grid Margin="10">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static lang:CommonViews.Name}" Foreground="{DynamicResource ForegroundColorLight}" Margin="2" />
			<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="2" MaxLength="50" />

			<TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static lang:CommonViews.Description}" Foreground="{DynamicResource ForegroundColorLight}" Margin="2" />
			<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="2" />
		</Grid>
	</Border>
</UserControl>