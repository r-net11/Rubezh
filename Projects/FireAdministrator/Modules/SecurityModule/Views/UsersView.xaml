<UserControl x:Class="SecurityModule.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="5">
        <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                  ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}" Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Width="1*" Binding="{Binding User.Name}" />
                <DataGridTextColumn Header="Полное имя" Width="1*" Binding="{Binding User.FullName}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>