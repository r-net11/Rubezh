<UserControl x:Class="SecurityModule.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" PreviewMouseDown="DataGrid_PreviewMouseDown" MouseDoubleClick="DataGrid_MouseDoubleClick">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Логин" Width="1*" Binding="{Binding User.Login}" />
            <DataGridTextColumn Header="Имя" Width="1*" Binding="{Binding User.Name}" />
            <DataGridTextColumn Header="Роль" Width="1*" Binding="{Binding RoleName}" />
        </DataGrid.Columns>
        <DataGrid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Добавить" Command="{Binding AddCommand}" />
                <MenuItem Header="Удалить" Command="{Binding DeleteCommand}" />
                <MenuItem Header="Редактировать" Command="{Binding EditCommand}" />
            </ContextMenu>
        </DataGrid.ContextMenu>
    </DataGrid>
</UserControl>