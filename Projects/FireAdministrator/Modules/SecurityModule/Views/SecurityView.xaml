<UserControl x:Class="SecurityModule.Views.SecurityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Views="clr-namespace:SecurityModule.Views">

    <!--<Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Пользователи" />
        <Grid Grid.Row="1">
            <DataGrid Margin="5" Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                            AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column"
                            IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            HorizontalGridLinesBrush="WhiteSmoke" VerticalGridLinesBrush="WhiteSmoke"
                            Background="White" BorderBrush="Black" BorderThickness="0"
                            RowHeight="20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Width="1*" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Полное имя" Width="1*" Binding="{Binding FullName}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <TextBlock Grid.Row="2" Text="Группы" />
        <Grid Grid.Row="3">
            <DataGrid Margin="5" Grid.Column="0" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                            AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column"
                            IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            HorizontalGridLinesBrush="WhiteSmoke" VerticalGridLinesBrush="WhiteSmoke"
                            Background="White" BorderBrush="Black" BorderThickness="0"
                            RowHeight="20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Width="1*" Binding="{Binding Name}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>-->
	<Grid Margin="10" x:Name="OuterGrid">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Border Grid.Row="0" Grid.Column="1" Background="AntiqueWhite" BorderThickness="2">
		<StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Left" >
				<TabControl>
					<TabItem Header="Пользователи">
						<Views:UsersView/>
					</TabItem>
					<TabItem Header="Группы">
						<Views:Groups/>
					</TabItem>
				</TabControl>
		</StackPanel>
		</Border>
	</Grid>
</UserControl>
