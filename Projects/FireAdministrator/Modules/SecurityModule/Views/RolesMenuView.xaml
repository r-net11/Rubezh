<UserControl x:Class="SecurityModule.Views.RolesMenuView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:lang="clr-namespace:Localization.Security.Common;assembly=Localization">

	<StackPanel Orientation="Horizontal" DataContext="{Binding Context}">
        <ToolBarButton Command="{Binding AddCommand}" ToolTip="{x:Static lang:CommonResources.Add}" ImageSource="Add"  />
        <ToolBarButton Command="{Binding EditCommand}" ToolTip="{x:Static lang:CommonResources.Edit}" ImageSource="Edit"  />
        <ToolBarButton Command="{Binding DeleteCommand}" ToolTip="{x:Static lang:CommonResources.Delete}" ImageSource="Delete"  />
	</StackPanel>
</UserControl>