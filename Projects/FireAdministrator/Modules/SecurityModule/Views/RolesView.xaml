<UserControl x:Class="SecurityModule.Views.RolesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataGrid ItemsSource="{Binding Roles}" SelectedItem="{Binding SelectedRole}" PreviewMouseDown="DataGrid_PreviewMouseDown" MouseDoubleClick="DataGrid_MouseDoubleClick">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Название" Width="1*" Binding="{Binding Role.Name}" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>