<UserControl x:Class="SecurityModule.Views.RolesView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
			 xmlns:lang="clr-namespace:Localization.Security.Views;assembly=Localization">

	<Controls:XDataGrid
		CanUserSortColumns="True"
		ItemsSource="{Binding Roles}" 
		SelectedItem="{Binding SelectedRole}">
		<DataGrid.Columns>
			<DataGridTemplateColumn 
				Header="{x:Static lang:CommonViews.Title}"
				SortMemberPath="Role.Name"
				Width="*" >
				<DataGridTemplateColumn.CellTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<Image Margin="1 0 0 0" VerticalAlignment="Center" Source="/Controls;component/Images/AccessTemplate.png" />
							<TextBlock Margin="2" VerticalAlignment="Center" Text="{Binding Role.Name}" />
						</StackPanel>
					</DataTemplate>
				</DataGridTemplateColumn.CellTemplate>
			</DataGridTemplateColumn>
		</DataGrid.Columns>
	</Controls:XDataGrid>
</UserControl>