<UserControl x:Class="SKDModule.Views.PasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="300" MinHeight="150">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Пароль" Margin="5 2" />
                <TextBox Text="{Binding Password}" Margin="2" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="Считать" Command="{Binding GetPasswordCommand}" Margin="2" />
                <Button Content="Записать" Command="{Binding SetPasswordCommand}" Margin="2" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>