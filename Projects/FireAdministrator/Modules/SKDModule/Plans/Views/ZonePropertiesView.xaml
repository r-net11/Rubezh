<UserControl x:Class="SKDModule.Plans.Views.ZonePropertiesView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:Controls;assembly=Controls" xmlns:viewModel="clr-namespace:SKDModule.ViewModels" MinWidth="600" MinHeight="400">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TreeList Grid.Column="0" Focusable="True" SelectedTreeNode="{Binding SelectedZone}" SortBehavior.CanUserSortColumns="False" Source="{Binding RootZones}">
			<TreeList.Resources>
				<Style x:Key="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
					<Setter Property="MinWidth" Value="0" />
					<Setter Property="MinHeight" Value="0" />
				</Style>
				<InputBindingCollection x:Key="inputCollection" x:Shared="False">
					<MouseBinding Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" MouseAction="LeftDoubleClick" />
				</InputBindingCollection>
				<viewModel:ZoneViewModelNameComparer x:Key="ZoneViewModelNameComparer" />
				<viewModel:ZoneViewModelDescriptionComparer x:Key="ZoneViewModelDescriptionComparer" />
			</TreeList.Resources>
			<TreeList.ItemContainerStyle>
				<Style BasedOn="{StaticResource {x:Type TreeListItem}}" TargetType="{x:Type TreeListItem}">
					<Setter Property="Controls:Attach.InputBindings" Value="{StaticResource inputCollection}" />
				</Style>
			</TreeList.ItemContainerStyle>
			<TreeList.View>
				<GridView>
					<GridViewColumn Width="250" Header="Зона" SortBehavior.SortComparer="{StaticResource ZoneViewModelNameComparer}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image Margin="1 0 2 0" Source="{Binding VisualizationState, Converter={StaticResource VisualizationStateToImageSourceConverter}}" />
									<Image Margin="1 0 0 0" VerticalAlignment="Center" Source="/Controls;component/Images/zone.png" />
									<TextBlock Margin="2" VerticalAlignment="Center" Text="{Binding Name}" />
								</StackPanel>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					<GridViewColumn Width="100" DisplayMemberBinding="{Binding Description}" Header="Примечание" SortBehavior.SortComparer="{StaticResource ZoneViewModelDescriptionComparer}" />
				</GridView>
			</TreeList.View>
		</TreeList>
		<CheckBox Grid.Row="1" Margin="5" VerticalAlignment="Center" Content="Невидимая зона" Foreground="White" IsChecked="{Binding IsHiddenZone, UpdateSourceTrigger=PropertyChanged}" />
		<StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal">
			<Button Width="100" Margin="2" Command="{Binding EditCommand}" Content="Редактировать" />
			<Button Width="100" Margin="2" Command="{Binding CreateCommand}" Content="Создать" />
		</StackPanel>
	</Grid>
</UserControl>