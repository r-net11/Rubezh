<UserControl x:Class="AutomationModule.Views.OpcDaClientReadWriteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 MinHeight="100" MinWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<DataGrid Grid.Row="0" ItemsSource="{Binding TagValues}" AutoGenerateColumns="True">
		</DataGrid>
		<Button Grid.Row="1" Content="Прочитать" Command="{Binding ReadTagCommand}" />
	</Grid>
</UserControl>