<UserControl x:Class="AutomationModule.Views.VariablesMenuView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:AutomationModule.Resources.Language.Procedures.Views">
	<StackPanel Orientation="Horizontal" DataContext="{Binding Context}">
		<ToolBarButton Command="{Binding AddCommand}" ToolTip="{x:Static res:VariablesMenuView.Add}" ImageSource="Add"  />
        <ToolBarButton Command="{Binding EditCommand}" ToolTip="{x:Static res:VariablesMenuView.Edit}" ImageSource="Edit"  />
        <ToolBarButton Command="{Binding DeleteCommand}" ToolTip="{x:Static res:VariablesMenuView.Delete}" ImageSource="Delete"  />
	</StackPanel>
</UserControl>