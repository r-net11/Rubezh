<UserControl x:Class="AutomationModule.Views.VariableDetailsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 xmlns:Views="clr-namespace:AutomationModule.Views"
			 MinWidth="650" MinHeight="400">
	<TabControl>
		<TabItem Header="Основные">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition Width="5" />
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>

				<DataGrid Grid.Column="0" Grid.RowSpan="2" ItemsSource="{Binding ExplicitTypes}" SelectedItem="{Binding SelectedExplicitType}">
					<DataGrid.Columns>
						<DataGridTextColumn Header="Тип" Binding="{Binding Name}" Width="*" />
					</DataGrid.Columns>
				</DataGrid>

				<StackPanel Orientation="Vertical" Grid.Column="2" Grid.Row="0" HorizontalAlignment="Stretch">

					<StackPanel Orientation="Horizontal" Margin="2" HorizontalAlignment="Right" Height="28">
						<Label Content="Имя" />
						<TextBox Text= "{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="2" Width="200"/>
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" HorizontalAlignment="Right" Height="28">
						<Label Content="Список" HorizontalAlignment="Left" />
						<CheckBox Width="202" IsChecked="{Binding IsList}" IsEnabled="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}" HorizontalAlignment="Right" Margin="2, 7, 0, 0" />
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Path=SelectedExplicitType.ExplicitType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static automation:ExplicitType.Object}}" HorizontalAlignment="Right" Height="28">
						<Label Content="Тип" />
						<ComboBox ItemsSource="{Binding ObjectTypes}" SelectedItem="{Binding SelectedObjectType}" Width="200" Margin="2" IsEnabled="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}">
							<ComboBox.ItemTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
								</DataTemplate>
							</ComboBox.ItemTemplate>
						</ComboBox>
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Path=SelectedExplicitType.ExplicitType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static automation:ExplicitType.Enum}}" HorizontalAlignment="Right" Height="28">
						<Label Content="Тип" />
						<ComboBox ItemsSource="{Binding EnumTypes}" SelectedItem="{Binding SelectedEnumType}" Width="200" Margin="2" IsEnabled="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}">
							<ComboBox.ItemTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
								</DataTemplate>
							</ComboBox.ItemTemplate>
						</ComboBox>
					</StackPanel>
				</StackPanel>

				<Views:ExplicitValuesView Grid.Column="2" Grid.Row="1" DataContext="{Binding ExplicitValuesViewModel}" />
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>