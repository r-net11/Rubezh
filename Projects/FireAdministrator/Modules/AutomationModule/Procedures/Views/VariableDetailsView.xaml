<UserControl x:Class="AutomationModule.Views.VariableDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:converters="clr-namespace:AutomationModule.Converters"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 MinWidth="650" MinHeight="400">
	<UserControl.Resources>
		<converters:VariableTypeToVisibilityConverter x:Key="VariableTypeToVisibilityConverter" />
	</UserControl.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>

		<DataGrid Grid.Column="0" ItemsSource="{Binding Variables}" SelectedItem="{Binding SelectedVariable}" IsReadOnly="False">
			<DataGrid.Columns>
				<DataGridTemplateColumn Header="Тип" Width="1*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<TextBlock Text="{Binding VariableType, Converter={StaticResource VariableTypeToStringConverter}}" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="2 0" />
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<GridSplitter Grid.Column="1" Width="5" Background="Transparent" />

		<Grid Grid.Column="2" DataContext="{Binding SelectedVariable}">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="2*"/>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<Label Grid.Row="0" Grid.Column="0" Content="Имя" HorizontalAlignment="Right" />
			<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="2" />

			<Label Grid.Row="1" Grid.Column="0" Content="Значение" HorizontalAlignment="Right"/>
			
			<CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding BoolValue, UpdateSourceTrigger=PropertyChanged}" Margin="2,7,0,0" 
					 Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter}, 
				ConverterParameter={x:Static automation:VariableType.Boolean}}"/>

			<extToolkit:DateTimePicker Grid.Row="1" Grid.Column="1" Value = "{Binding DateTimeValue, UpdateSourceTrigger=PropertyChanged}" Margin="2" 
					 Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter}, 
				ConverterParameter={x:Static automation:VariableType.DateTime}}">
			</extToolkit:DateTimePicker>

			<extToolkit:IntegerUpDown Grid.Row="1" Grid.Column="1" Value="{Binding IntValue, UpdateSourceTrigger=PropertyChanged}" Margin="2" 
					 Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter}, 
				ConverterParameter={x:Static automation:VariableType.Integer}}"/>

			<ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding ObjectValues}" SelectedItem="{Binding ObjectValue}" Margin="2"
					  Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter},
				ConverterParameter={x:Static automation:VariableType.Object}}">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding ., Converter={StaticResource ObjectTypeToStringConverter}}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>

			<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding StringValue, UpdateSourceTrigger=PropertyChanged}" Margin="2" 
					 Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter}, 
				ConverterParameter={x:Static automation:VariableType.String}}"/>
		</Grid>
	</Grid>
</UserControl>
