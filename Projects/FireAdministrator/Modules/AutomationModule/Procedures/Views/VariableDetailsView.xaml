<UserControl x:Class="AutomationModule.Views.VariableDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:converters="clr-namespace:AutomationModule.Converters"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 MinWidth="650" MinHeight="400">
	<UserControl.Resources>
		<converters:VariableTypeToVisibilityConverter x:Key="VariableTypeToVisibilityConverter" />
	</UserControl.Resources>
	<TabControl>
		<TabItem Header="Основные">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition Width="5" />
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>

				<DataGrid Grid.Column="0" ItemsSource="{Binding Variables}" SelectedItem="{Binding SelectedVariable}" IsReadOnly="False">
					<DataGrid.Columns>
						<DataGridTemplateColumn Header="Тип" Width="*">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<Grid>
										<TextBlock Text="{Binding VariableType, Converter={StaticResource VariableTypeToStringConverter}}" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="2 0" />
                                        <CheckBox Content="Список" IsChecked="{Binding IsList, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right" Margin="5 0" />
									</Grid>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
					</DataGrid.Columns>
				</DataGrid>

				<StackPanel Orientation="Vertical" Grid.Column="2" DataContext="{Binding SelectedVariable}" HorizontalAlignment="Right" >
					
					<StackPanel Orientation="Horizontal" Margin="2" HorizontalAlignment="Right">
						<Label Content="Имя" HorizontalAlignment="Right" />
						<TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="2" Width="200"/>
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter},
						ConverterParameter={x:Static automation:VariableType.Integer}}">
						<Label Content="Значение" HorizontalAlignment="Right"/>
						<extToolkit:IntegerUpDown Width="200" Value="{Binding IntValue, UpdateSourceTrigger=PropertyChanged}" Margin="2" />
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter}, 
						ConverterParameter={x:Static automation:VariableType.Boolean}}">
						<Label Content="Значение" HorizontalAlignment="Right" Margin="-30,0,0,0"/>
						<CheckBox IsChecked="{Binding BoolValue, UpdateSourceTrigger=PropertyChanged}" Margin="2,7,0,0" />
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter},
						ConverterParameter={x:Static automation:VariableType.String}}">
						<Label Content="Значение" HorizontalAlignment="Right"/>
						<TextBox Text="{Binding StringValue, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="2" />
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter},
						ConverterParameter={x:Static automation:VariableType.DateTime}}">
						<Label Content="Значение" HorizontalAlignment="Right"/>
						<extToolkit:DateTimePicker Value = "{Binding DateTimeValue, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="2" />
					</StackPanel>

					<StackPanel Orientation="Horizontal" Margin="2" Visibility="{Binding Path=VariableType, Converter={StaticResource VariableTypeToVisibilityConverter},
						ConverterParameter={x:Static automation:VariableType.Object}}">
						<Label Content="Тип" HorizontalAlignment="Right"/>
						<ComboBox ItemsSource="{Binding ObjectTypes}" SelectedItem="{Binding ObjectType}" Width="200" Margin="2" >
							<ComboBox.ItemTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding ., Converter={StaticResource ObjectTypeToStringConverter}}" />
								</DataTemplate>
							</ComboBox.ItemTemplate>
						</ComboBox>
					</StackPanel>
					
				</StackPanel>
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>