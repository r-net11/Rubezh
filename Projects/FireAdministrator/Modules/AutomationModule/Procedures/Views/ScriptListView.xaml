<UserControl
	x:Class="AutomationModule.Views.ScriptListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:customControls="clr-namespace:Controls.CustomControls;assembly=Controls"
	xmlns:converters="clr-namespace:Controls.Converters;assembly=Controls"
    mc:Ignorable="d"
    MinHeight="300"
	MinWidth="650"
	MaxHeight="300"
	MaxWidth="650">
	<UserControl.Resources>
		<converters:InversedBoolToVisibilityConverter x:Key="InversedBoolToVisibilityConverter" />
		<converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
	</UserControl.Resources>
	<Grid>
		<DataGrid
			x:Name="Grid"
			CanUserSortColumns="True"
			CanUserReorderColumns="False"
			ItemsSource="{Binding Scripts, IsAsync=True}"
			SelectedItem="{Binding SelectedScript}"
			IsEnabled="{Binding IsBisy, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource InverseBooleanConverter}}">
			<DataGrid.Columns>
				<DataGridTemplateColumn
					Header="№"
					Width="Auto"
					CanUserResize="False"
					SortMemberPath="Id">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal" Margin="5 0">
								<Image Source="pack://application:,,,/Controls;component/StepIcons/Control.png" />
								<TextBlock
									Text="{Binding Id}"
									VerticalAlignment="Center"
									Margin="5 0"/>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTemplateColumn
					Header="Название"
					Width="1.5*"
					SortMemberPath="Name">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								Text="{Binding Name}"
								VerticalAlignment="Center"
								Margin="5 0"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTemplateColumn
					Header="Примечание"
					Width="*"
					SortMemberPath="Description">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								Text="{Binding Description}"
								VerticalAlignment="Center"
								Margin="5 0"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTemplateColumn
					Header="Активен"
					Width="Auto"
					SortMemberPath="IsEnabled"
					CanUserResize="False">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<CheckBox
								IsHitTestVisible="False"
								IsChecked="{Binding IsEnabled}"
								VerticalAlignment="Center"
								Margin="5 0"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>
		<StackPanel
			VerticalAlignment="Center"
			Visibility="{Binding ElementName=Grid, Path=IsEnabled, Mode=TwoWay, FallbackValue=Visible, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource InversedBoolToVisibilityConverter}}">
			<customControls:BisyIndicator
				VerticalAlignment="Center"
				HorizontalAlignment="Center"
				Width="50"
				Height="50"/>
			<TextBlock
				Text="Получение данных от сервера..."
				HorizontalAlignment="Center"/>
		</StackPanel>
	</Grid>
</UserControl>
