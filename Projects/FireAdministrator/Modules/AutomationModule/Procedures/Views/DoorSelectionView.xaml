<UserControl x:Class="AutomationModule.Views.DoorSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 MinWidth="400" MinHeight="400">
	<DataGrid ItemsSource="{Binding Doors}" SelectedItem="{Binding SelectedDoor}" SelectionMode="Single" CanUserSortColumns="True">
		<DataGrid.Columns>
			<DataGridTextColumn Header="Наименование" Binding="{Binding Door.Name}" />
			<DataGridTextColumn Header="Примечание" Binding="{Binding Door.Description}" Width="*" />
		</DataGrid.Columns>
	</DataGrid>
</UserControl>