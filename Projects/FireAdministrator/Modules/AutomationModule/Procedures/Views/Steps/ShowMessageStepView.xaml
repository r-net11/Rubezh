<UserControl x:Class="AutomationModule.Views.ShowMessageStepView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 xmlns:Views="clr-namespace:AutomationModule.Views">
	<StackPanel>
		<StackPanel Orientation="Horizontal">
			<Label Content="Тип значений" Width="100" />
			<ComboBox ItemsSource="{Binding ExplicitTypes}" Margin="2" VerticalAlignment="Top" SelectedItem="{Binding ExplicitType, UpdateSourceTrigger=PropertyChanged}">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
		</StackPanel>
		<StackPanel Orientation="Horizontal">
			<Label Content="Сообщение" Width="100" />
			<Views:ArithmeticParameterView DataContext="{Binding MessageParameter}" />
		</StackPanel>
	</StackPanel>
</UserControl>