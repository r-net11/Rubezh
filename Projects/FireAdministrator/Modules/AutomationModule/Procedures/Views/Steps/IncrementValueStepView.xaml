<UserControl x:Class="AutomationModule.Views.IncrementValueStepView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 xmlns:Views="clr-namespace:AutomationModule.Views">
	<StackPanel>
		<StackPanel Orientation="Horizontal">
			<Label Content="Переменная" HorizontalAlignment="Right" />
			<Views:ArithmeticParameterView DataContext="{Binding Variable1}" />
		</StackPanel>
		<StackPanel Orientation="Horizontal">
			<Label Grid.Row="1" Grid.Column="0" Content="Значение" HorizontalAlignment="Right" />
			<ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding IncrementTypes}" Margin="2"
				  SelectedItem="{Binding SelectedIncrementType, UpdateSourceTrigger=PropertyChanged}">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
		</StackPanel>
	</StackPanel>
</UserControl>
