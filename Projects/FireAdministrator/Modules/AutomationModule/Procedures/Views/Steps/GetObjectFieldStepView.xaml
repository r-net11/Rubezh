<UserControl x:Class="AutomationModule.Views.GetObjectFieldStepView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 xmlns:Views="clr-namespace:AutomationModule.Views">
	<Grid>
		<StackPanel>
			<StackPanel Orientation="Horizontal">
				<Label Content="Объект" Margin="2" Width="100" />
				<Views:ArithmeticParameterView DataContext="{Binding Variable1}" />
			</StackPanel>

			<StackPanel Orientation="Horizontal">
				<Label Content="Поле" Margin="2" Width="100" />
				<ComboBox ItemsSource="{Binding Properties}" Margin="2"
								  SelectedItem="{Binding SelectedProperty, UpdateSourceTrigger=PropertyChanged}" MinWidth="163" MaxWidth="163">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
			</StackPanel>

			<StackPanel Orientation="Horizontal">
				<Label Content="Результат" Margin="2" Width="100" />
				<Views:ArithmeticParameterView DataContext="{Binding Result}" />
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>

