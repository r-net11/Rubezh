<UserControl x:Class="AutomationModule.Views.GetObjectFieldStepView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 xmlns:Views="clr-namespace:AutomationModule.Views">
	<UserControl.Resources>
		<ResourceDictionary Source="ProcedureResources.xaml" />
	</UserControl.Resources>
	<Grid>
		<StackPanel>
			<StackPanel Orientation="Horizontal">
				<Label Content="Объект" />
				<Views:ArithmeticParameterView DataContext="{Binding Variable1}" />
			</StackPanel>

			<StackPanel Orientation="Horizontal">
				<Label Content="Свойство" />
				<ComboBox ItemsSource="{Binding Properties}" SelectedItem="{Binding SelectedProperty, UpdateSourceTrigger=PropertyChanged}">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
			</StackPanel>

			<StackPanel Orientation="Horizontal">
				<Label Content="Результат" />
				<Views:ArithmeticParameterView DataContext="{Binding Result}" />
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>

