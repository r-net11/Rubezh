<UserControl x:Class="AutomationModule.Views.FindObjectStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:AutomationModule.Converters"
             xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
             xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended">
	<UserControl.Resources>
		<converters:ObjectTypeToVisibilityConverter x:Key="ObjectTypeToVisibilityConverter" />
		<converters:ConditionTypeToStringConverter x:Key="ConditionTypeToStringConverter" />
		<converters:PropertyTypeToVisibilityConverter x:Key="PropertyTypeToVisibilityConverter" />
		<converters:DevicePropertyTypeToStringConverter x:Key="DevicePropertyTypeToStringConverter" />
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<StackPanel Orientation="Horizontal" Grid.Row="0">
			<ComboBox ItemsSource="{Binding Variables}" Margin="2"
					  SelectedItem="{Binding SelectedVariable, UpdateSourceTrigger=PropertyChanged}" MinWidth="163" MaxWidth="163">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding Name}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
			<ToolBarButton HorizontalAlignment="Left" Command="{Binding AddCommand}" ImageSource="/Controls;component/Images/Add.png" ToolTip="Добавить условие" Height="32" Width="32" />
			<ToolBarButton HorizontalAlignment="Left" Width="50" Height="30" Foreground="White" Command="{Binding ChangeJoinOperatorCommand}">
				<TextBlock Text="{Binding JoinOperator, Converter={StaticResource JoinOperatorToStringConverter}}" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" />
			</ToolBarButton>
		</StackPanel>
		<ItemsControl Grid.Row="1" ItemsSource="{Binding FindObjectConditions}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<ToolBarButton ToolTip="Удалить условие" Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}},
							Path=DataContext.RemoveCommand}" CommandParameter="{Binding}" ImageSource="/Controls;component/Images/deleteCross.png" Height="32" Width="32" Background="DarkGray" />

						<ComboBox ItemsSource="{Binding DevicePropertyTypes}" Margin="2" SelectedItem="{Binding SelectedDevicePropertyType, UpdateSourceTrigger=PropertyChanged}"
								  Visibility="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}},
							Path=DataContext.SelectedVariable.ObjectType, Converter={StaticResource ObjectTypeToVisibilityConverter},
							ConverterParameter={x:Static automation:ObjectType.Device}}" MinWidth="163" MaxWidth="163">
							<ComboBox.ItemTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding ., Converter={StaticResource DevicePropertyTypeToStringConverter}}" />
								</DataTemplate>
							</ComboBox.ItemTemplate>
						</ComboBox>

						<ComboBox ItemsSource="{Binding ConditionTypes}" Margin="2"
								  SelectedItem="{Binding SelectedConditionType, UpdateSourceTrigger=PropertyChanged}" MinWidth="87" MaxWidth="87">
							<ComboBox.ItemTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding ., Converter={StaticResource ConditionTypeToStringConverter}}" />
								</DataTemplate>
							</ComboBox.ItemTemplate>
						</ComboBox>

						<extToolkit:IntegerUpDown Value="{Binding IntValue}" Margin="2"
							Visibility="{Binding Path=PropertyType, Converter={StaticResource PropertyTypeToVisibilityConverter},
							ConverterParameter={x:Static automation:PropertyType.Integer}}" MinWidth="163" MaxWidth="163"/>

						<TextBox Text="{Binding StringValue}" Margin="2"
							Visibility="{Binding Path=PropertyType, Converter={StaticResource PropertyTypeToVisibilityConverter},
							ConverterParameter={x:Static automation:PropertyType.String}}" MinWidth="163" MaxWidth="163"/> 
						
					</StackPanel>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</Grid>
</UserControl>
