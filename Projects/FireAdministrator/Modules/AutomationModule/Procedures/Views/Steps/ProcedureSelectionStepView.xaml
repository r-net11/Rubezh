<UserControl x:Class="AutomationModule.Views.ProcedureSelectionStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:AutomationModule.Views" >
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="40"/>
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
			<ToolBarButton Command="{Binding AddCommand}" ToolTip="Добавить процедуру" ImageSource="/Controls;component/Images/Add.png" />
			<ToolBarButton Command="{Binding DeleteCommand}" ToolTip="Удалить процедуру" ImageSource="/Controls;component/Images/Delete.png" />
		</StackPanel>
		<views:ScheduleProceduresView Grid.Column="0" Grid.RowSpan="2" Margin="2" />
		<Border CornerRadius="5" Background="White" Margin="2" Grid.Row="1" Grid.Column="1" Visibility="{Binding ., Converter={StaticResource NullToVisibilityConverter}}" DataContext="{Binding SelectedScheduleProcedure}">
			<views:ArgumentsView />
		</Border>
	</Grid>
</UserControl>
