<UserControl x:Class="AutomationModule.Views.ProcedureSelectionStepView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:views="clr-namespace:AutomationModule.Views">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="240" MinWidth="240" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<StackPanel Grid.Column="0" Orientation="Horizontal">
			<Label Content="Процедура" Margin="2" />
			<ComboBox VerticalAlignment="Top" ItemsSource="{Binding ScheduleProcedures}" SelectedItem="{Binding SelectedScheduleProcedure}" Width="163">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text ="{Binding Name}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
		</StackPanel>
		<views:ArgumentsView Grid.Column="1" DataContext="{Binding SelectedScheduleProcedure}" Visibility="{Binding ., Converter={StaticResource NullToVisibilityConverter}}" />
	</Grid>
</UserControl>