<UserControl x:Class="AutomationModule.Views.SendEmailStepView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:automation="clr-namespace:FiresecAPI.Automation;assembly=FiresecAPI"
			 xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
			 xmlns:Views="clr-namespace:AutomationModule.Views">
	<UserControl.Resources>
		<ResourceDictionary Source="ProcedureResources.xaml" />
	</UserControl.Resources>
	<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
		<Grid>
			<StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Адрес" />
					<Views:ArithmeticParameterView DataContext="{Binding EMailAddress}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Заголовок" />
					<Views:ArithmeticParameterView DataContext="{Binding EMailTitle}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Текст" />
					<Views:ArithmeticParameterView DataContext="{Binding EMailContent}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Хост" />
					<Views:ArithmeticParameterView DataContext="{Binding Host}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Порт" />
					<Views:ArithmeticParameterView DataContext="{Binding Port}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Логин" />
					<Views:ArithmeticParameterView DataContext="{Binding Login}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Пароль" />
					<Views:ArithmeticParameterView DataContext="{Binding Password}" />
				</StackPanel>
			</StackPanel>
		</Grid>
	</ScrollViewer>
</UserControl>