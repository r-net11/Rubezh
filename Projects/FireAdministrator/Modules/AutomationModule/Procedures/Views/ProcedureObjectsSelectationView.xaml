<UserControl x:Class="AutomationModule.Views.ProcedureObjectsSelectationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:AutomationModule.Converters"
             xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls"
             MinWidth="300" MinHeight="300">
    <UserControl.Resources>
        <Converters:ObjectTypeToStringConverter x:Key="ObjectTypeToStringConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ComboBox Grid.Row="0" ItemsSource="{Binding AvailableProcedureObjectTypes}" SelectedItem="{Binding SelectedProcedureObjectType}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ., Converter={StaticResource ObjectTypeToStringConverter}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Grid Grid.Row="1" Visibility="{Binding IsXDeviceSelected, Converter={StaticResource BoolToVisibilityConverter}}" Margin="2">
            <Grid DataContext="{Binding DeviceSelectationViewModel}">
                <tree:TreeList Focusable="True" SelectedTreeNode="{Binding SelectedDevice}" Source="{Binding RootDevices}">
                    <tree:TreeList.View>
                        <GridView>
                            <GridViewColumn Width="250" Header="Устройство">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Image Width="16" Height="16" Margin="5, 0, 5, 0" HorizontalAlignment="Left" Source="{Binding Device.Driver.ImageSource}" />
                                            <TextBlock Width="200" Margin="31 0 0 0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding Device.ShortName}" />
                                        </Grid>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="100" Header="Адрес">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Device.PresentationAddress}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Width="100" Header="Примечание">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Device.Description}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </tree:TreeList.View>
                </tree:TreeList>
            </Grid>
        </Grid>
        <Grid Grid.Row="1" Visibility="{Binding IsCameraSelected, Converter={StaticResource BoolToVisibilityConverter}}" Margin="2">
            <Grid DataContext="{Binding CameraSelectationViewModel}">
                <TreeList Focusable="True" SelectedTreeNode="{Binding SelectedCamera}" Source="{Binding RootCameras}">
                    <TreeList.View>
                        <GridView>
                            <GridView.Columns>
                                <GridViewColumn Width="250" Header="Камера">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Image Width="16" Height="16" Margin="5, 0, 5, 0" HorizontalAlignment="Left" Source="{Binding Camera.ImageSource}" />
                                                <TextBlock Text="{Binding  Camera.Name, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="100" Header="Адрес">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PresentationAddress}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="100" Header="Порт">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Camera.Port}" Visibility="{Binding IsDvr, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView.Columns>
                        </GridView>
                    </TreeList.View>
                </TreeList>
            </Grid>
        </Grid>
    </Grid>
</UserControl>