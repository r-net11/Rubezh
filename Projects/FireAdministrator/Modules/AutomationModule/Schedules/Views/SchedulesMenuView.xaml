<UserControl x:Class="AutomationModule.Views.SchedulesMenuView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             xmlns:res="clr-namespace:AutomationModule.Resources.Language.Schedules.Views">

	<StackPanel Orientation="Horizontal" DataContext="{Binding Context}">
		<ToolBarButton Command="{Binding AddCommand}" ToolTip="{x:Static res:SchedulesMenuView.Add}" ImageSource="Add"  />
        <ToolBarButton Command="{Binding EditCommand}" ToolTip="{x:Static res:SchedulesMenuView.Edit}" ImageSource="Edit"  />
        <ToolBarButton Command="{Binding DeleteCommand}" ToolTip="{x:Static res:SchedulesMenuView.Delete}" ImageSource="Delete"  />
	</StackPanel>
</UserControl>