<UserControl
		x:Class="AutomationModule.Views.EmployeeSelectionView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
		xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls"
		xmlns:ctr="clr-namespace:Controls.ListViewLayoutManager;assembly=Controls"
        xmlns:res="clr-namespace:AutomationModule.Resources.Language.ObjectSelection.Views"
		mc:Ignorable="d" 
		d:DesignHeight="300" d:DesignWidth="300"
		MinWidth="400" MinHeight="400">
    <Grid>
		<tree:TreeList
				Focusable="True"
				ctr:ListViewLayoutManager.Enabled="True"
				SelectedTreeNode="{Binding SelectedEmployee}"
				Source="{Binding Employees}"
				tree:SortBehavior.CanUserSortColumns="True">
			<tree:TreeList.View>
				<GridView>
					<GridViewColumn
							Width="300"
							Header="{x:Static res:EmployeeSelectionView.Employee}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image
											Width="16"
											Margin="1 0"
											VerticalAlignment="Center"
											Source="/Controls;component/Images/Organisation.png"
											Visibility="{Binding IsEmployee, Converter={StaticResource InversedBoolToVisibilityConverter}}" />
									<Image
											Width="16"
											Margin="1 0"
											VerticalAlignment="Center"
											Source="/Controls;component/Images/Employee.png"
											Visibility="{Binding IsEmployee, Converter={StaticResource BoolToVisibilityConverter}}" />
									<TextBlock Text="{Binding Name}" />
								</StackPanel>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					<GridViewColumn
							Width="150"
							Header="{x:Static res:EmployeeSelectionView.Number}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding TabelNo}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</tree:TreeList.View>
		</tree:TreeList>
	</Grid>
</UserControl>
