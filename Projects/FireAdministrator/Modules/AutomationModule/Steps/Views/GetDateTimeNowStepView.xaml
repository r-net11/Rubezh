<UserControl
	x:Class="AutomationModule.Views.GetDateTimeNowStepView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:views="clr-namespace:AutomationModule.Views"
    xmlns:enums="clr-namespace:FiresecAPI.Enums;assembly=FiresecAPI"
    xmlns:lang="clr-namespace:Localization.Automation.Views;assembly=Localization"
    mc:Ignorable="d"
    d:DesignHeight="300"
	d:DesignWidth="300">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock
			Text="{x:Static lang:CommonViews.Result}"
			VerticalAlignment="Center"
			Foreground="White"
			Margin="5" />
		<views:ArgumentView
			Grid.Column="1"
			DataContext="{Binding Result}"
			Margin="3 5" />
		<TextBlock
			Grid.Row="1"
			Text="{x:Static lang:CommonViews.Round}"
			Foreground="White"
			Margin="5" />
		<ComboBox
			Grid.Row="1"
			Grid.Column="1"
			ItemsSource="{Binding Source={x:Type enums:RoundingType}, Converter={StaticResource EnumToItemsSourceConverter}}"
			ItemTemplate="{StaticResource EnumDescriptionTemplate}"
			SelectedItem="{Binding SelectedRoundingType}"
			HorizontalAlignment="Left"
			Width="367"
			Margin="5" />
    </Grid>
</UserControl>
