<UserControl
	x:Class="AutomationModule.Views.SendOPCCommandStepView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enums="clr-namespace:StrazhAPI.Enums;assembly=StrazhAPI"
    mc:Ignorable="d"
    d:DesignHeight="100"
	d:DesignWidth="300">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock
			Text="Команда"
			HorizontalAlignment="Left"
			VerticalAlignment="Center"
			Foreground="White"
			Margin="5"/>
		<ComboBox
			Grid.Column="1"
			VerticalAlignment="Center"
			ItemsSource="{Binding Source={x:Type enums:OPCCommandType}, Converter={StaticResource EnumToItemsSourceConverter}}"
			ItemTemplate="{StaticResource EnumDescriptionTemplate}"
			SelectedItem="{Binding SelectedCommandType}"
			HorizontalAlignment="Left"
			Margin="5"
			Width="200"/>
    </Grid>
</UserControl>
