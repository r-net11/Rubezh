<UserControl x:Class="JournalModule.Views.NewJournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Название представления" />
        <TextBox Grid.Row="1" Text="{Binding Name}" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Классы событий" />
            <TextBlock Grid.Column="1" Grid.Row="0" Text="Категории устройств" />
            <Grid Grid.Column="0" Grid.Row="1">
                <ItemsControl ItemsSource="{Binding Events}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsEnabled}" />
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="1">
                <ItemsControl ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsEnabled}" />
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
        
        <Grid Grid.Row="3">
            <StackPanel>
                <TextBlock Text="Количество записей" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Count}" />
                    <TextBlock Text="Последних дней" />
                </StackPanel>
                <CheckBox Content="Дополнительный фильтр" IsChecked="{Binding IsFiltered}" Name="_checkBox" />
                <StackPanel Orientation="Horizontal" IsEnabled="{Binding IsChecked, ElementName=_checkBox}">
                    <TextBlock Text="записи за последние" />
                    <TextBox Text="{Binding LastDays}" />
                    <TextBlock Text="дн." />
                </StackPanel>
            </StackPanel>
        </Grid>
        
        <Grid Grid.Row="4">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Ok" Command="{Binding SaveCommand}" Width="100" />
                <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" />
            </StackPanel>
        </Grid>
        
    </Grid>
</UserControl>
