<UserControl x:Class="PlansModule.Views.PlansTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer HorizontalScrollBarVisibility="Visible">
        <TreeView >
            <TreeView.Resources>

                <HierarchicalDataTemplate ItemsSource="{Binding Plans}" DataType="{x:Type }">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.Resources>

            <TreeView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Добавить план" Command="{Binding AddCommand}"
                              />
                    <MenuItem Header="Удалить план" 
                              />
                    <MenuItem Header="Редактировать план" 
                              />
                </ContextMenu>
            </TreeView.ContextMenu>
        </TreeView>
    </ScrollViewer>
</UserControl>
