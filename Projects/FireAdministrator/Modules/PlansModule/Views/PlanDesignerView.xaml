<UserControl x:Class="PlansModule.Views.PlanDesignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Name="scrollViewer" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
            <Grid Name="grid" RenderTransformOrigin="0.5,0.5">
                <ContentControl Name="_contentControl" Background="White" HorizontalAlignment="Left" Content="{Binding DesignerCanvas}" />
            </Grid>
        </ScrollViewer>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="OnZoomOut" ToolTip="Уменьшить" Style="{StaticResource ToolarButtonStyle}">
                <Image Source="/Controls;component/Images/ZoomOut.png" Height="24" />
            </Button>
            <Slider Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Minimum="1" Maximum="10" Value="1" x:Name="slider"/>
            <Button Grid.Column="2" Click="OnZoomIn" ToolTip="Увеличить" Style="{StaticResource ToolarButtonStyle}">
                <Image Source="/Controls;component/Images/ZoomIn.png" Height="24" />
            </Button>
        </Grid>
    </Grid>
</UserControl>
