<UserControl
	x:Class="PlansModule.Views.LayoutPartPropertyPlansPageView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:lang="clr-namespace:Localization.Plans.Views;assembly=Localization"
	xmlns:langCommon="clr-namespace:Localization.Plans.Common;assembly=Localization"
	d:DesignHeight="300"
	d:DesignWidth="300"
	mc:Ignorable="d">
	<UserControl.Resources>
		<SwitchConverter x:Key="TypeConverter" Else="{x:Null}">
			<SwitchCase Then="{x:Static lang:CommonViews.AllPlansAvailable}" When="All" />
            <SwitchCase Then="{x:Static lang:CommonViews.SelectedPlansAvailable}" When="Selected" />
            <SwitchCase Then="{x:Static lang:CommonViews.OnePlanAvailable}" When="Single" />
		</SwitchConverter>
		<Style BasedOn="{StaticResource {x:Type ComboBox}}" TargetType="{x:Type ComboBox}">
			<Setter Property="ItemTemplate">
				<Setter.Value>
					<DataTemplate>
						<TextBlock Text="{Binding Converter={StaticResource TypeConverter}}" />
					</DataTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>
	<DockPanel>
		<ComboBox
			Margin="4"
			DockPanel.Dock="Top"
			ItemsSource="{Binding Types}"
			SelectedItem="{Binding Type,
		                                 UpdateSourceTrigger=PropertyChanged}" />
		<TreeList
			IsEnabled="{Binding IsTreeEnabled}"
			SelectedTreeNode="{Binding SelectedPlan}"
			Source="{Binding Plans}">
			<TreeList.ItemContainerStyle>
				<Style BasedOn="{StaticResource {x:Type TreeListItem}}" TargetType="{x:Type TreeListItem}">
					<Setter Property="Focusable" Value="{Binding DataContext.ShowCheckboxes, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBooleanConverter}}" />
				</Style>
			</TreeList.ItemContainerStyle>
			<TreeList.View>
				<GridView>
                    <GridViewColumn Width="250" Header="{x:Static langCommon:CommonResources.Plans}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<Border Background="Transparent">
									<StackPanel Orientation="Horizontal">
										<CheckBox
											Margin="2"
											VerticalAlignment="Center"
											IsChecked="{Binding IsChecked}"
											Visibility="{Binding DataContext.ShowCheckboxes,
										                               RelativeSource={RelativeSource AncestorType=UserControl},
										                               Converter={StaticResource BoolToVisibilityConverter}}" />
										<Image
											Name="Image"
											Margin="2"
											VerticalAlignment="Center"
											Source="/Controls;component/Images/CMap.png" />
										<TextBlock VerticalAlignment="Center" Text="{Binding Caption}" />
									</StackPanel>
								</Border>
								<DataTemplate.Triggers>
									<DataTrigger Binding="{Binding IsFolder}" Value="True">
										<Setter TargetName="Image" Property="Image.Source" Value="/Controls;component/Images/CFolder.png" />
									</DataTrigger>
								</DataTemplate.Triggers>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</TreeList.View>
		</TreeList>
	</DockPanel>
</UserControl>