<UserControl x:Class="PlansModule.Views.PlanDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400" MinHeight="200">

    <Border Grid.Column="0" CornerRadius="5" Padding="5" Background="{DynamicResource BackgroundBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Content="Название" HorizontalAlignment="Right" />
            <Label Grid.Column="0" Grid.Row="1" Content="Описание" HorizontalAlignment="Right" />
            <Label Grid.Column="0" Grid.Row="2" Content="Ширина" HorizontalAlignment="Right" />
            <Label Grid.Column="0" Grid.Row="3" Content="Высота" HorizontalAlignment="Right" />
            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Caption}" Margin="2" />
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Description}" Margin="2" />
            <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding Width}" Margin="2" />
            <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding Height}" Margin="2" />
        </Grid>
    </Border>
</UserControl>