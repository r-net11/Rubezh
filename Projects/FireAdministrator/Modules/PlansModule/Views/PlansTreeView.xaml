<UserControl x:Class="PlansModule.Views.PlansTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid ItemsSource="{Binding Plans}" SelectedItem="{Binding SelectedPlan}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Plan.Name}" Header="Название" />
                <DataGridTextColumn Binding="{Binding Plan.Description}" Header="Описание" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
