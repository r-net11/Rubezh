<UserControl
	x:Class="PlansModule.Views.DesignerPropertiesView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
	xmlns:xc="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
	xmlns:lang="clr-namespace:Localization.Plans.Views;assembly=Localization"
	MinWidth="400"
	MinHeight="400">
	<TabControl>
		<TabItem Header="{x:Static lang:CommonViews.Main}">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Label
					Grid.Row="0"
					Grid.Column="0"
					HorizontalAlignment="Right"
					Content="{x:Static lang:CommonViews.Name}" />
				<TextBox
					Grid.Row="0"
					Grid.Column="1"
					Margin="2"
					Text="{Binding Caption, UpdateSourceTrigger=PropertyChanged}"
					MaxLength="50" />
				<Label
					Grid.Row="1"
					Grid.Column="0"
					HorizontalAlignment="Right"
					Content="{x:Static lang:CommonViews.Description}" />
				<TextBox
					Grid.Row="1"
					Grid.Column="1"
					Margin="2"
					Text="{Binding Description,
										UpdateSourceTrigger=PropertyChanged}" />
				<Label
					Grid.Row="2"
					Grid.Column="0"
					HorizontalAlignment="Right"
					Content="{x:Static lang:CommonViews.Width}" />
				<extToolkit:IntegerUpDown
					Grid.Row="2"
					Grid.Column="1"
					Margin="2"
					Minimum="10"
					Value="{Binding Width,
														  UpdateSourceTrigger=PropertyChanged}" />
				<Label
					Grid.Row="3"
					Grid.Column="0"
					HorizontalAlignment="Right"
					Content="{x:Static lang:CommonViews.Height}" />
				<extToolkit:IntegerUpDown
					Grid.Row="3"
					Grid.Column="1"
					Margin="2"
					Minimum="10"
					Value="{Binding Height,
														  UpdateSourceTrigger=PropertyChanged}" />
				<Label
					Grid.Row="4"
					Grid.Column="0"
					HorizontalAlignment="Right"
					Content="{x:Static lang:CommonViews.BackgroundColor}" />
				<xc:ColorPicker
					Grid.Row="4"
					Grid.Column="1"
					Margin="2"
					SelectedColor="{Binding BackgroundColor,
														UpdateSourceTrigger=PropertyChanged}" />
			</Grid>
		</TabItem>
		<TabItem Header="{x:Static lang:CommonViews.Picture}">
			<Grid DataContext="{Binding ImagePropertiesViewModel}">
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Border Grid.Column="0" Background="{Binding ImageBrush}" />
				<StackPanel Grid.Column="1">
					<Button
						Margin="2"
						Command="{Binding SelectPictureCommand}"
						Content="{x:Static lang:CommonViews.SelectPicture}" />
					<Button
						Margin="2"
						Command="{Binding RemovePictureCommand}"
						Content="{x:Static lang:CommonViews.DeletePicture}" />
				</StackPanel>
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>