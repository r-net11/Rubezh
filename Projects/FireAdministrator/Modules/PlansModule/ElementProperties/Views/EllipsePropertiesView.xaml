<UserControl x:Class="PlansModule.Views.EllipsePropertiesView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended" xmlns:xc="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit" MinWidth="400" MinHeight="400">

	<TabControl Background="{DynamicResource AlternativeBackgroundBrush}">
		<TabItem Header="Основные">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Content="Цвет фона" />
				<Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Content="Цвет границы" />
				<Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" Content="Ширина границы" />

				<xc:ColorPicker Grid.Row="0" Grid.Column="1" Margin="2" SelectedColor="{Binding BackgroundColor, UpdateSourceTrigger=PropertyChanged}" />
				<xc:ColorPicker Grid.Row="1" Grid.Column="1" Margin="2" SelectedColor="{Binding BorderColor, UpdateSourceTrigger=PropertyChanged}" />
				<extToolkit:IntegerUpDown Grid.Row="2" Grid.Column="1" Margin="2" Minimum="0" Value="{Binding StrokeThickness, UpdateSourceTrigger=PropertyChanged}" Maximum="10" />
			</Grid>
		</TabItem>
		<TabItem Header="Изображение">
			<Grid DataContext="{Binding ImagePropertiesViewModel}">
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<ContentControl Grid.Column="0" Content="{Binding Image}" />
				<StackPanel Grid.Column="1">
					<Button Margin="2" Command="{Binding SelectPictureCommand}" Content="Выбрать картинку" />
					<Button Margin="2" Command="{Binding RemovePictureCommand}" Content="Очистить картинку" />
				</StackPanel>
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>
