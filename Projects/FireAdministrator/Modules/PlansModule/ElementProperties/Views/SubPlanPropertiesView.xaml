<UserControl x:Class="PlansModule.Views.SubPlanPropertiesView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:Controls;assembly=Controls" MinWidth="400" MinHeight="400">
	<Controls:TreeListView ItemsSource="{Binding Plans}" SelectedObject="{Binding SelectedPlan}">
		<Controls:TreeListView.Resources>
			<InputBindingCollection x:Key="inputCollection" x:Shared="False">
				<MouseBinding Command="{Binding DataContext.SaveCommand,RelativeSource={RelativeSource AncestorType={x:Type Controls:TreeListView}}}" MouseAction="LeftDoubleClick" />
			</InputBindingCollection>
		</Controls:TreeListView.Resources>
		<Controls:TreeListView.ItemContainerStyle>
			<Style BasedOn="{StaticResource {x:Type Controls:TreeListViewItem}}" TargetType="{x:Type Controls:TreeListViewItem}">
				<Setter Property="Controls:Attach.InputBindings" Value="{StaticResource inputCollection}" />
			</Style>
		</Controls:TreeListView.ItemContainerStyle>
		<Controls:TreeListView.Columns>
			<GridViewColumn Width="250" DisplayMemberBinding="{Binding Plan.Caption}" Header="Название" />
			<GridViewColumn DisplayMemberBinding="{Binding Plan.Description}" Header="Описание" />
		</Controls:TreeListView.Columns>
	</Controls:TreeListView>
</UserControl>