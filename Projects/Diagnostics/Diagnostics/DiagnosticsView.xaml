<Window x:Class="Diagnostics.DiagnosticsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Менеджер логов" MaxHeight="300" MaxWidth="250" MinHeight="300" MinWidth="260" mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="300" d:DesignWidth="220" SizeToContent="WidthAndHeight">
    <StackPanel Background="LightBlue">
        <Button Background="#C0C0C0" Content="Собрать логи" Command="{Binding GetLogsCommand}" Height="30" Width="250" VerticalAlignment="Top" FontSize="15" Margin="0 5"/>
        <Button Background="#C0C0C0" Content="Удалить логи" Command="{Binding RemLogsCommand}" Height="30" Width="250" VerticalAlignment="Top" FontSize="15" />
        <CheckBox Content="Автозапуск сервера при старте системы" IsChecked="{Binding IsServerAuto}" Margin="5 5" />
        <CheckBox Content="Автозапуск opc-сервера при старте системы" IsChecked="{Binding IsOpcServerAuto}" Margin="5 0"/>
    </StackPanel>
</Window>