<Window x:Class="ComServerEmulator.ComServerEmulatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ComServerEmulator"
    Title="Com Server Emulator" Height="800" Width="800" Loaded="Window_Loaded" >
    
    <ScrollViewer>
        <StackPanel>
            <StackPanel  Orientation="Horizontal">
                <Button Content="Get State" Command="{Binding GetStateCommand}" />
                <Button Content="GetCoreState" Command="{Binding GetCoreStateCommand}" />
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <local:DeviceTreeView Grid.Column="0" />
                <!--<StackPanel>
                    <TreeView Name="treeView" ItemsSource="{Binding Devices}" SelectedItemChanged="treeView_SelectedItemChanged" Height="500">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                                <TextBlock Text="{Binding DeviceName}" />
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </StackPanel>-->

                <local:DeviceDetailsView Grid.Column="1" />
                
                <!--<StackPanel Grid.Column="1">
                    <StackPanel DataContext="{Binding SelectedDevice}">
                        <TextBox Text="{Binding DeviceName}" />
                        <TextBox Text="{Binding Address}" />
                        <ItemsControl ItemsSource="{Binding States}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto" />
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsActive}" />
                                        <TextBox Grid.Column="1" Text="{Binding Name}" />
                                        <TextBox Grid.Column="2" Text="{Binding Id}" />
                                        <TextBox Grid.Column="3" Text="{Binding Priority}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>-->
            </Grid>

            <TextBox Text="{Binding StateMessage}" />

        </StackPanel>
    </ScrollViewer>
    
</Window>
