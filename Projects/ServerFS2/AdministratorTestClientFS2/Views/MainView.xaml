<Window x:Class="AdministratorTestClientFS2.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Converters="clr-namespace:Controls.Converters;assembly=Controls"
        xmlns:Views="clr-namespace:AdministratorTestClientFS2.Views" xmlns:Controls="clr-namespace:Controls;assembly=Controls" xmlns:Converters1="clr-namespace:AdministratorTestClientFS2.Converters" WindowStartupLocation="CenterScreen"
        Title="Администратор Firesec-2"
        Height="800" Width="1000">

    <Window.Resources>
        <Converters:NullToVisibilityConverter x:Key="NullObjectToVisibiltyConverter"/>
        <Converters1:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Views:DevicesView Grid.Column="0" DataContext="{Binding DevicesViewModel}" Margin="2"/>
            <StackPanel Grid.Column="1">
                <Button Content="Автопоиск устройств" Command = "{Binding AutoDetectDeviceCommand}" Width="300" Margin="2" />
                <Button Content="Считать конфигурацию из прибора" Command = "{Binding ReadConfigurationCommand}" Width="300" Margin="2" />
                <Button Content="Считать журнал событий" Command = "{Binding ReadJournalCommand}" Width="300" Margin="2" />
                <Button Content="Информация об устройстве" Command = "{Binding GetInformationCommand}" Width="300" Margin="2" />
                <Button Content="Синхронизация времени" Command = "{Binding SynchronizeTimeCommand}" Width="300" Margin="2" />
                <Button Content="Обновление ПО" Command = "{Binding UpdateFirmwhareCommand}" Width="300" Margin="2" />
                <Button Content="Задать пароль" Command = "{Binding SetPasswordCommand}" Width="300" Margin="2" />
                <Button Content="Установить режим глухой панели" Command = "{Binding SetPanelRegimeCommand}" Width="300" Margin="2" />
                <Button Content="Снять режим глухой панели" Command = "{Binding UnsetPanelRegimeCommand}" Width="300" Margin="2" />
                <Button Content="Записать конфигурацию в прибор" Command = "{Binding WriteConfigurationCommand}" Width="300" Margin="2" />
                <Button Content="Получить статус прибора" Command = "{Binding GetDeviceStatusCommand}" Width="300" Margin="2" />
                <Button Content="Добавить в список обхода" Command = "{Binding AddDeviceToCheckListCommand}" Width="300" Margin="2" />
                <Button Content="Удалить из списка обхода" Command = "{Binding RemoveDeviceFromCheckListCommand}" Width="300" Margin="2" />
                <TextBlock Text="{Binding StatusString}" Width="300" Margin="2" FontSize = "14"/>
                <CheckBox Content="Прибор подключен по USB" IsChecked="{Binding IsUsbDevice}" Margin="2"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Visibility="{Binding DevicesViewModel.SelectedDevice, Converter={StaticResource NullObjectToVisibiltyConverter}}" >
            <Grid  Margin="0, 5, 0, 0" DataContext="{Binding DevicesViewModel.SelectedDevice}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{DynamicResource BackgroundBrush}" CornerRadius="5,0,0,5">
                    <Grid>
                        <Views:PropertiesView Visibility="{Binding PropertiesViewModel.ParameterVis, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}" MaxHeight ="200" MinHeight="200"/>
                        <Views:ParametersView DataContext="{Binding PropertiesViewModel}" Visibility="{Binding ParameterVis, Converter={StaticResource BoolToVisibilityConverter}}" MaxHeight ="200" MinHeight="200"/>
                    </Grid>
                </Border>
                <Border Grid.Column="1" CornerRadius="0,5,5,0" Background="{DynamicResource BackgroundBrush}">
                    <StackPanel DataContext="{Binding PropertiesViewModel}">
                        <Controls:ToggleButton Command="{Binding PropVisCommand}" ImageSource="/Controls;component/Images/devparameters.png" IsChecked="{Binding ParameterVis, Converter={StaticResource InverseBooleanConverter}}" Style="{StaticResource ToggleButtonImageStyle}" ToolTip="Информация об устройстве" Visibility="{Binding Choise, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <Controls:ToggleButton Command="{Binding ParamVisCommand}" ImageSource="/Controls;component/Images/adrparameters.png" IsChecked="{Binding ParameterVis}" Style="{StaticResource ToggleButtonImageStyle}" ToolTip="Параметры устройства" Visibility="{Binding Choise, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <StackPanel IsEnabled="{Binding IsAuParametersReady}" Visibility="{Binding ParameterVis, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Controls:ToolBarButton Width="36" Height="36" Margin="0,0,0,-8" Command="{Binding Context.GetParametersCommand}" ImageSource="/Controls;component/Images/readconfig.png" ToolTip="Считать параметры устройства" Visibility="{Binding Device.Driver.HasConfigurationProperties, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <Controls:ToolBarButton Width="36" Height="36" Margin="0,0,0,-8" Command="{Binding Context.SetParametersCommand}" ImageSource="/Controls;component/Images/writeconfig.png" ToolTip="Записать параметры на устройство" Visibility="{Binding Device.Driver.HasConfigurationProperties, Converter={StaticResource BoolToVisibilityConverter}}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
        <Grid Grid.Row="2" Margin="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Запрос:" Style="{x:Null}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding TextBoxRequest}" Margin="2" Style="{x:Null}"/>
                <Button Grid.Row="1" Grid.ColumnSpan="2" Content="Послать запрос" Command = "{Binding SendRequestCommand}" Margin="2" />
                <Label Grid.Row="2" Grid.Column="0" Content="Ответ:" Style="{x:Null}"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TextBoxResponse}" Style="{x:Null}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>