<UserControl x:Class="AdministratorTestClientFS2.Views.InformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" MinWidth="300" MinHeight="100" MaxWidth="300" MaxHeight="100">
    <Grid>
        <ListBox ItemsSource="{Binding Properties}">
                <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="13" />
                        <TextBlock Text = ": " FontSize="13"/>
                        <TextBlock Grid.Column="1" Text="{Binding Value}" FontSize="13" />
                    </StackPanel>
                </DataTemplate>
                </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
