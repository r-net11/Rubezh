<Window x:Class="MonitorClientFS2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:Controls;assembly=Controls"
        xmlns:Views="clr-namespace:MonitorClientFS2.Views"
        Title="Оперативная Задача Firesec-2"
        Height="600" Width="600"
        Closing="Window_Closing">
    <Grid>
        <TabControl>
            <TabItem Header="Журнал">
                <StackPanel Orientation="Vertical">
                    <DataGrid ItemsSource="{Binding DevicesViewModel}" SelectedItem="{Binding SelectedDeviceViewModel}" Margin="2" AutoGenerateColumns="False" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Название" Binding="{Binding Device.PresentationName}" />
                            <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" />
                            <DataGridTextColumn Header="USB канал" Binding="{Binding UsbChannel}" />
                            <DataGridTextColumn Header="Серийный номер" Binding="{Binding SerialNo}" />
                            <DataGridTextColumn Header="Версия" Binding="{Binding Version}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
            <Button Command="{Binding StartMonitoringCommand}" Content="StartMonitoring"/>
            <Button Command="{Binding StopMonitoringCommand}" Content="StopMonitoring"/>
            <DataGrid ItemsSource="{Binding JournalItems}" AutoGenerateColumns="False" Height="300" MinRowHeight="20">
                        <DataGrid.Columns>
                    <DataGridTextColumn Header="No" Binding="{Binding No}" />
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" />
                    <DataGridTextColumn Header="EventName" Binding="{Binding EventName}" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </TabItem>
            <TabItem Header="Устройства">
                <Views:DevicesView DataContext="{Binding DevicesViewModel}" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>