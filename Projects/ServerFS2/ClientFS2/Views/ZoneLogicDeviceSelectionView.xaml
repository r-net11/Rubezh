<UserControl x:Class="ClientFS2.Views.ZoneLogicDeviceSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             MinWidth="500" MinHeight="500">

    <DataGrid Margin="5" Grid.Column="0" ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}" CanUserSortColumns="True">
        <DataGrid.Resources>
            <InputBindingCollection x:Key="inputCollection" x:Shared="False">
                <MouseBinding Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" MouseAction="LeftDoubleClick" />
            </InputBindingCollection>
        </DataGrid.Resources>
        <DataGrid.ItemContainerStyle>
            <Style BasedOn="{StaticResource {x:Type DataGridRow}}" TargetType="{x:Type DataGridRow}">
                <Setter Property="Controls:Attach.InputBindings" Value="{StaticResource inputCollection}" />
            </Style>
        </DataGrid.ItemContainerStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="Устройство" Width="1*" Binding="{Binding Driver.ShortName}" />
            <DataGridTextColumn Header="Адрес" Width="2*" Binding="{Binding DottedAddress}" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
