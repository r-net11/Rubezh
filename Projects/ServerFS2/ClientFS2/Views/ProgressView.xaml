<UserControl x:Class="ClientFS2.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" MinWidth="500" MinHeight="100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <Label Content="{Binding Description, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Center"/>
        </ScrollViewer>
        <ProgressBar Grid.Row="1" Minimum="0" Maximum="{Binding MaxValue}" Value="{Binding Value}" Height="30" Margin="2" />
    </Grid>
</UserControl>