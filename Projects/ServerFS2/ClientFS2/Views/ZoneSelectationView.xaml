<UserControl x:Class="ClientFS2.Views.ZoneSelectationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Controls="clr-namespace:Controls;assembly=Controls" mc:Ignorable="d" 
             MinWidth="400" MinHeight="400">
<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" ItemsSource="{Binding Zones}" SelectedItem="{Binding SelectedZone}" SelectionMode="Single" SelectionChanged="OnSelectionChanged">
            <ListView.Resources>
                <InputBindingCollection x:Key="inputCollection" x:Shared="False">
                    <MouseBinding Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" MouseAction="LeftDoubleClick" />
                </InputBindingCollection>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="{x:Type ListViewItem}">
                    <Setter Property="Controls:Attach.InputBindings" Value="{StaticResource inputCollection}" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Zone.No}" Header="№" />
                    <GridViewColumn DisplayMemberBinding="{Binding Zone.Name}" Header="Наименование" />
                    <GridViewColumn DisplayMemberBinding="{Binding Zone.Description}" Header="Примечание" />
                    <GridViewColumn DisplayMemberBinding="{Binding DetectorCount}" Header="Число ИП для перехода в пожар" />
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding EditCommand}" Header="Редактировать" />
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>

        <Button Grid.Row="1" Command="{Binding CreateCommand}" Content="Создать" Width="100" Margin="0, 2, 0, 2" HorizontalAlignment="Right" />
    </Grid>
</UserControl>
