<Window x:Class="PowerCalculator.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Converters="clr-namespace:PowerCalculator.Converters"
        xmlns:Controls="clr-namespace:Controls;assembly=Controls"
        Title="Рубеж глобал. Калькулятор МВП" Height="600" Width="800">
    <Window.Resources>
        <Converters:DriverTypeToStringConverter x:Key="DriverTypeToStringConverter" />
    </Window.Resources>

    <Grid Background="{DynamicResource BaseWindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="2">
            <ToolBarButton Command="{Binding CreateNewCommand}" ToolTip="Создать новый" Background="{DynamicResource New}" Margin="2" />
            <ToolBarButton Command="{Binding SaveFileCommand}" ToolTip="Сохранить файл" Background="{DynamicResource Save}" Margin="2" />
            <ToolBarButton Command="{Binding LoadFileCommand}" ToolTip="Загрузить файл" Background="{DynamicResource Load}" Margin="2" />
            <ToolBarButton Command="{Binding AddLineCommand}" ToolTip="Добавить АЛС" Background="{DynamicResource Add}" Margin="2" />
            <ToolBarButton Command="{Binding RemoveLineCommand}" ToolTip="Удалить АЛС" Background="{DynamicResource Delete}" Margin="2" />
        </StackPanel>
        <TabControl Grid.Row="1" ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLine}" Margin="2">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="2">
                            <ToolBarButton Command="{Binding AddCommand}" ToolTip="Добавить устройство" Background="{DynamicResource Add}" Margin="2" />
                            <ToolBarButton Command="{Binding RemoveCommand}" ToolTip="Удалить устройство" Background="{DynamicResource Delete}" Margin="2" />
                        </StackPanel>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}" Margin="2">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Название" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding AlsDevice.DeviceType, Converter={StaticResource EnumToDescriptionConverter}}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>