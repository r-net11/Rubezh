<Window x:Class="CustomActions.InstanceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding InstanceList}" SelectedItem="{Binding SelectedInstance}" Grid.Row="0" Height="100" Name="listBox1" VerticalAlignment="Top" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Ok" IsDefault="True" Width="100" Click="Button_Click" />
            <Button Content="Отмена" Width="100" Margin="2, 0, 0, 0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
