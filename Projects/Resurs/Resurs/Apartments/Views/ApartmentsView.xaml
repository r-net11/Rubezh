<UserControl x:Class="Resurs.Views.ApartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls"
			 xmlns:viewModel="clr-namespace:Resurs.ViewModels">
	<UserControl.Resources>
		<viewModel:ApartmentViewModelNameComparer x:Key="ApartmentViewModelNameComparer" />
		<viewModel:ApartmentViewModelAddressComparer x:Key="ApartmentViewModelAddressComparer" />
		<viewModel:ApartmentViewModelDescriptionComparer x:Key="ApartmentViewModelDescriptionComparer" />
	</UserControl.Resources>


	<Grid>
		<Grid.InputBindings>
			<KeyBinding Modifiers="Control" Key="N" Command="{Binding AddCommand}" />
			<KeyBinding Modifiers="Control" Key="E" Command="{Binding EditCommand}" />
			<KeyBinding Modifiers="Control" Key="Del" Command="{Binding RemoveCommand}" />
		</Grid.InputBindings>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Orientation="Horizontal">
			<ToolBarButton Command="{Binding AddCommand}" ToolTip="Добавить (Ctrl+N)" ImageSource="Add" />
			<ToolBarButton Command="{Binding AddFolderCommand}" ToolTip="Добавить группу (Ctrl+G)" ImageSource="AddChild" />
			<ToolBarButton Command="{Binding ChangeParentCommand}" ToolTip="Переместить в группу (Ctrl+M)" ImageSource="ArrowInto" />
			<ToolBarButton Command="{Binding EditCommand}" ToolTip="Редактировать (Ctrl+E)" ImageSource="Edit" />
			<ToolBarButton Command="{Binding RemoveCommand}" ToolTip="Удалить (Ctrl+Del)" ImageSource="Delete" />
		</StackPanel>
		<tree:TreeList Grid.Row="1" Source="{Binding RootApartments}" SelectedTreeNode="{Binding SelectedApartment}" ItemActivatedCommand="{Binding EditCommand}"
					   Focusable="True" tree:SortBehavior.CanUserSortColumns="True">
			<tree:TreeList.View>
				<GridView>
					<GridViewColumn Header="Название" ListViewLayoutManager.StarWidth="1" tree:SortBehavior.SortComparer="{StaticResource ApartmentViewModelNameComparer}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image Margin="1 0 0 0" VerticalAlignment="Center" Source="{Binding ImageSource}" />
									<TextBlock Margin="2 0" VerticalAlignment="Center" Text="{Binding Apartment.Name}" />
								</StackPanel>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					<GridViewColumn Header="Адрес" ListViewLayoutManager.StarWidth="1" tree:SortBehavior.SortComparer="{StaticResource ApartmentViewModelAddressComparer}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Margin="2 0" VerticalAlignment="Center" Text="{Binding Apartment.Address}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					<GridViewColumn Header="Примечание" ListViewLayoutManager.StarWidth="1" tree:SortBehavior.SortComparer="{StaticResource ApartmentViewModelDescriptionComparer}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Margin="2 0" VerticalAlignment="Center" Text="{Binding Apartment.Description}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</tree:TreeList.View>
		</tree:TreeList>
	</Grid>
</UserControl>