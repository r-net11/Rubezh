<!--<UserControl x:Class="Resurs.Views.TariffDetailsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 MinWidth="600" MinHeight="600">
	<StackPanel>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="150" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="auto" />
				<RowDefinition Height="auto" />
				<RowDefinition Height="auto" />
			</Grid.RowDefinitions>
			--><!--<TextBlock Grid.Column="0" Grid.Row="0" Text="Название тарифа" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource TextBlockDetailsStyle}" />
			<TextBlock Grid.Column="0" Grid.Row="1" Text="Количество тарифов" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource TextBlockDetailsStyle}" />
			<TextBlock Grid.Column="0" Grid.Row="1" Text="Тип ресурса" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource TextBlockDetailsStyle}" />

			<TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Name}" VerticalAlignment="Center" MaxLength="100" Margin="5" />
			<TextBox Grid.Column="1" Grid.Row="1" Text="{Binding TariffParts.Capacity}" VerticalAlignment="Center" MaxLength="100" Margin="5" />
			<ComboBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding TariffType}" VerticalAlignment="Center" Margin="5" />--><!--

		</Grid>
	</StackPanel>
</UserControl>-->

<UserControl x:Class="Resurs.Views.TariffDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 MinWidth="400" MinHeight="400">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" Grid.Column="0" Text="Название" HorizontalAlignment="Right" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<TextBlock Grid.Row="1" Grid.Column="0" Text="Примечание" HorizontalAlignment="Right" Foreground="{DynamicResource ForegroundColorWhite}" Margin="2" />
		<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}" Margin="2" />
		<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description}" Margin="2" />
	</Grid>
</UserControl>