<UserControl x:Class="Resurs.Views.ReportFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="750" Height="400" 
			 MinWidth="750" MinHeight="400">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<TextBlock Grid.Row="0" Grid.Column="0" Text="Дата начала" />
		<DatePicker Grid.Row="0" Grid.Column="1" 
					SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
					DisplayDateStart="{Binding MinDate, Mode=OneWay}"/>
		<TextBlock Grid.Row="1" Grid.Column="0" Text="Дата окончания" />
		<DatePicker Grid.Row="1" Grid.Column="1"
					SelectedDate="{Binding EndDate, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
					DisplayDateStart="{Binding MinDate, Mode=OneWay}"/>
		<TextBlock Grid.Row="2" Grid.Column="0" Text="Выберите счетчик" />
		<ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}">
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Name}" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>
	</Grid>
</UserControl>
