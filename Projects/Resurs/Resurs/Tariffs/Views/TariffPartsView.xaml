<UserControl x:Class="Resurs.Views.TariffPartsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 MinWidth="400" MinHeight="200">
	<StackPanel Orientation="Vertical">
		<ToolBarButton Command="{Binding AddTariffPartCommand}" ImageSource="Add"  ToolTip="Добавить тарифный интервал" Height="32" Width="32" />
		<ItemsControl ItemsSource="{Binding TariffParts}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						<ToolBarButton ToolTip="Удалить тарифный план"
									   Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, 
									   Path=DataContext.RemoveCommand}"
									   CommandParameter="{Binding}"
									   Height="32" Width="32" Margin="2" Background="DarkGray" ImageSource="RedDeleteCross" 
									   Grid.Column="0" Grid.Row="0"/>
						<TextBlock Grid.Column="0" Grid.Row="1"  Text="Цена за единицу ресурса" />
						<TextBlock Grid.Column="0" Grid.Row="2" Text="Скидка" />
						<TextBlock Grid.Column="0" Grid.Row="3" Text="Промежуток" />

						<TextBox Grid.Column="1" Grid.Row="1" Text="{Binding TariffPart.Price}" />
						<TextBox Grid.Column="1" Grid.Row="2" Text="{Binding TariffPart.Discount}" />
						<TextBox Grid.Column="1" Grid.Row="3" Text="{Binding TariffPart.StartTime}" />
					</Grid>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</StackPanel>
</UserControl>
