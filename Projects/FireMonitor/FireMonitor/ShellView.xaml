<Window x:Class="FireMonitor.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FireMonitor"
        WindowStartupLocation="CenterScreen"
        Title="Станция пожарного мониторинга" Height="800" Width="1000">
    <Grid>
        <Grid Background="Gray">

            <DockPanel>
                
                <StackPanel DockPanel.Dock="Top"  Margin="2">
                    <ContentControl Name="_alarmGroups" />
                    <ContentControl Name="_alarm" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="Состояние" />
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <local:NavigationView Grid.Column="0" />

                    <Grid Grid.Column="1" Name="centerStackPanel" Background="LightGray">
                        <ContentControl Name="_mainRegionHost" />
                    </Grid>

                </Grid>

            </DockPanel>

        </Grid>
    </Grid>
</Window>
