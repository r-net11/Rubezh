<UserControl x:Class="FireMonitor.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             MinHeight="75" MinWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Логин" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" />
            <Label Grid.Row="1" Grid.Column="0" Content="Пароль" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding UserName}" IsEnabled="{Binding CanEditUserName}" Margin="2" />
            <Controls:PasswordBoxControl Grid.Row="1" Grid.Column="1" Password="{Binding Password}" Margin="2" />
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0, 10, 0, 0" HorizontalAlignment="Right">
            <Button Content="Ok" Command="{Binding ConnectCommand}" IsDefault="True" Width="100" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="2, 0, 0, 0" />
        </StackPanel>
    </Grid>
</UserControl>
