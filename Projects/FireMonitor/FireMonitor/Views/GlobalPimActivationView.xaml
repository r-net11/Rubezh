<UserControl x:Class="FireMonitor.Views.GlobalPimActivationView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:firemonitor="clr-namespace:FireMonitor.ViewModels">

	<Button Name="Button" Command="{Binding ChangeGlobalPimActivationCommand}" ToolTip="Вкл/Выкл автоматику" Width="34" Height="34">
		<Button.Style>
			<Style BasedOn="{StaticResource ToolBarButtonStyle}" TargetType="{x:Type Button}">
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate>
							<Border Name="border"
									Margin="2"
									BorderThickness="1"
									CornerRadius="2"
									Padding="2"
									ToolTip="{TemplateBinding ToolTip}"
									ToolTipService.ShowOnDisabled="True">
								<Rectangle Name="Image" Width="20" Height="20" />
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="UIElement.IsMouseOver" Value="True">
									<Setter TargetName="border" Property="Border.Background" Value="{DynamicResource HighlightedBackgoundBrush}" />
									<Setter TargetName="border" Property="Border.BorderBrush" Value="{DynamicResource PressedBackgroundBrush}" />
								</Trigger>
								<Trigger Property="Button.IsPressed" Value="True">
									<Setter TargetName="border" Property="Border.Background" Value="{DynamicResource PressedBackgroundBrush}" />
								</Trigger>
								<DataTrigger Binding="{Binding GlobalPimStatusType}" Value="{x:Static firemonitor:GlobalPimStatusType.On}">
									<DataTrigger.Setters>
										<Setter TargetName="Image" Property="Fill" Value="{DynamicResource Alarm_main_5_Info}" />
										<Setter TargetName="Image" Property="Opacity" Value="1" />
										<Setter TargetName="border" Property="ToolTip" Value="Включено" />
									</DataTrigger.Setters>
								</DataTrigger>
								<DataTrigger Binding="{Binding GlobalPimStatusType}" Value="{x:Static firemonitor:GlobalPimStatusType.Off}">
									<DataTrigger.Setters>
										<Setter TargetName="Image" Property="Fill" Value="{DynamicResource Alarm_main_5_Info}" />
										<Setter TargetName="Image" Property="Opacity" Value="0.5" />
										<Setter TargetName="border" Property="ToolTip" Value="Выключено" />
									</DataTrigger.Setters>
								</DataTrigger>
								<DataTrigger Binding="{Binding GlobalPimStatusType}" Value="{x:Static firemonitor:GlobalPimStatusType.Unknown}">
									<DataTrigger.Setters>
										<Setter TargetName="Image" Property="Fill" Value="{DynamicResource RAlarm_main_5_Info}" />
										<Setter TargetName="Image" Property="Opacity" Value="1" />
										<Setter TargetName="border" Property="ToolTip" Value="Состояние неизвестно" />
									</DataTrigger.Setters>
								</DataTrigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
		</Button.Style>
	</Button>
</UserControl>
