<UserControl x:Class="FireMonitor.Views.MailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"             
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Button Command="{Binding PlaySoundCommand}" ToolTip="Вкл/Выкл звук" Style="{StaticResource ToolBarButtonStyle}">
        <ContentControl>
            <Image Source="/Controls;component/Images/sound.png" Opacity="0.5" />
        </ContentControl>
    </Button>
</UserControl>
