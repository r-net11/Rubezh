<UserControl x:Class="FireMonitor.SoundView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:XButtons="clr-namespace:Buttons;assembly=Buttons"
             xmlns:Converters="clr-namespace:FireMonitor.Views.Converters"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!--<Image x:Key="XImage" Source="Images\sound.png" />
        <Style TargetType="Image">
            <Style.Setters>
                <Setter Property="Image.Source" Value="Images\sound.png" />
            </Style.Setters>
        </Style>
        <Style x:Key="XButton"
               TargetType="XButtons:XButton"
               BasedOn="{StaticResource {x:Type XButtons:XButton}}">
            <Style.Setters>
                <Setter Property="XButtons:XButton.Content">
                    <Setter.Value>
                        <Image Name="MyImage" Source="Images\sound.png" />
                    </Setter.Value>
                </Setter>
            </Style.Setters>
            <Style.Triggers>
                <DataTrigger
                    Binding="{Binding IsSoundOn}" Value="false">
                    <Setter Property="Image.Source"
                            Value="Images\mute.png"/>
                </DataTrigger>
                <DataTrigger
                    Binding="{Binding IsSoundOn}" Value="true">
                    <Setter TargetName="MyImage" Property="Image.Source"
                            Value="Images\sound.png"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>-->
        <Converters:BoolToBackgroundImageConverter x:Key="BoolToBackgroundImageConverter" />
    </UserControl.Resources>
    <!--Content="{Binding IsSoundOn, Converter={StaticResource BoolToBackgroundImageConverter}}"-->
    <!--<Grid>
        <XButtons:XButton Name="SoundButton" Margin="2" Padding="2"
                          
                          FontWeight="Black" Background="Black"
                          Command="{Binding PlaySoundCommand}" >
            <XButtons:XButton.Content>
                <Image Name="PlayStatusImage" Source="Images\sound.png"/>
            </XButtons:XButton.Content>
            <XButtons:XButton.Triggers>
                <DataTrigger Binding="{Binding IsSoundOn}" Value="false">
                    <Setter TargetName="PlayStatusImage" Property="Image.Source" Value="Images\mute.png"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsSoundOn}" Value="true">
                    <Setter TargetName="PlayStatusImage" Property="Image.Source" Value="Images\sound.png"/>
                </DataTrigger>
            </XButtons:XButton.Triggers>
        </XButtons:XButton>
    </Grid>-->
    <Grid>
        
        <XButtons:XButton Name="SoundButtonOn" Margin="2" Padding="2"
                          FontWeight="Black" Background="Black"
                          Command="{Binding PlaySoundCommand}"
                          Content="{Binding IsSoundOn, Converter={StaticResource BoolToBackgroundImageConverter}}" >
        </XButtons:XButton>
    </Grid>
</UserControl>