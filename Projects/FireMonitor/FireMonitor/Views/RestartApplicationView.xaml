<UserControl
	x:Class="FireMonitor.Views.RestartApplicationView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	MinWidth="400"
	MinHeight="120">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="auto" />
		</Grid.RowDefinitions>

		<DockPanel>
			<Image
				Width="50"
				Height="50"
				Margin="5"
				DockPanel.Dock="Left"
				Source="/Controls;component/Images\MessageBox\warning.png" />
			<TextBlock
				Width="320"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				Foreground="White"
				TextWrapping="Wrap">
				<Run Text="Конфигурация на сервере изменилась." />
				<LineBreak />
				<Run Text='- Нажмите "Перезапуск", чтобы перезагрузить приложение немедленно.'/>
				<LineBreak />
				<Run Text='- Нажмите "Отложить перезапуск, чтобы сохранить изменения, приложение будет автоматически перезагружено через 50 секунд!'/>
			</TextBlock>
		</DockPanel>
		<ProgressBar
			Grid.Row="1"
			Height="20"
			Margin="5"
			IsIndeterminate="False"
			Maximum="{Binding Total}"
			Minimum="0"
			Value="{Binding Counter}" />
	</Grid>
</UserControl>
