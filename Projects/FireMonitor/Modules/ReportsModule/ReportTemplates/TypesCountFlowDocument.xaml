<FlowDocument xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xrd="clr-namespace:CodeReason.Reports.Document;assembly=CodeReason.Reports"
                    xmlns:xrbc="clr-namespace:CodeReason.Reports.Document.Barcode;assembly=CodeReason.Reports"
                    PageHeight="29.7cm" PageWidth="21cm" ColumnWidth="21cm">
    <FlowDocument.Resources>
        <Style x:Key="headerFooterRowStyle" TargetType="{x:Type TableRowGroup}">
            <Setter Property="FontWeight" Value="DemiBold" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Background" Value="LightGray" />
        </Style>

        <Style x:Key="dataRowStyle" TargetType="{x:Type TableRowGroup}">
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style TargetType="{x:Type TableCell}">
            <Setter Property="Padding" Value="0.1cm" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="BorderThickness" Value="0.01cm" />
        </Style>
    </FlowDocument.Resources>
    <xrd:ReportProperties>
        <xrd:ReportProperties.ReportTitle>Количество устройств по типам</xrd:ReportProperties.ReportTitle>
    </xrd:ReportProperties>

    <xrd:SectionReportFooter PageFooterHeight="4" Padding="22,0,22,10" FontSize="12">
        <Table CellSpacing="0">
            <Table.Columns>
                <TableColumn Width="*" />
                <TableColumn Width="*" />
            </Table.Columns>
            <TableRowGroup>
                <TableRow>
                    <TableCell ColumnSpan="2">
                        <BlockUIContainer>
                            <Rectangle
                                    Fill="Black"
                                    Height="1" />
                        </BlockUIContainer>
                    </TableCell>
                </TableRow>
                <TableRow >
                    <TableCell>
                        <Paragraph TextAlignment="Left">
                            <xrd:InlineDocumentValue PropertyName="PrintDateNow" Format="dd.MM.yyyy HH:mm:ss" />
                        </Paragraph>
                    </TableCell>
                    <TableCell>
                        <Paragraph TextAlignment="Right">
                            Страница
                            <xrd:InlineContextValue PropertyName="PageNumber" FontWeight="Bold" />
                        </Paragraph>
                    </TableCell>
                </TableRow>
            </TableRowGroup>
        </Table>
    </xrd:SectionReportFooter>

    <Section Padding="20" FontSize="12" BreakPageBefore="True" >

        <Paragraph FontSize="20">
            <TextBlock HorizontalAlignment="Stretch" Text="Количество устройств по типам" />
        </Paragraph>
        <Table  CellSpacing="0" BorderBrush="Black" BorderThickness="0.02cm">

            <Table.Columns>
                <TableColumn Width="2*" />
                <TableColumn Width="1*" />
            </Table.Columns>

            <TableRowGroup Style="{StaticResource headerFooterRowStyle}">
                <xrd:TableRowForDynamicHeader TableName="Header" />
            </TableRowGroup>

            <TableRowGroup Style="{StaticResource dataRowStyle}">
                <xrd:TableRowForDynamicDataTable TableName="Data" />
            </TableRowGroup>

            <TableRowGroup Style="{StaticResource dataRowStyle}" FontWeight="Bold">
                <xrd:TableRowForDynamicDataTable TableName="Total" />
            </TableRowGroup>

        </Table>

    </Section>

</FlowDocument>