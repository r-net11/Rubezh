<UserControl x:Class="ReportsModule.Views.FilterMainView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="300" d:DesignWidth="300" mc:Ignorable="d">
	<UserControl.Resources>
		<IfConverter x:Key="AdditionalLinkText" FalseValue="Показать настройки отображения отчёта" TrueValue="Скрыть настройки отображения отчёта" />
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="2*" />
			</Grid.ColumnDefinitions>
			<TextBlock Grid.Column="0" Margin="2" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="White" Text="Фильтр" />
			<StackPanel Grid.Column="1" Orientation="Horizontal">
				<ComboBox Margin="2" DisplayMemberPath="Name" IsEditable="True" ItemsSource="{Binding Filters}" SelectedItem="{Binding SelectedFilter}" Text="{Binding FilterName}" />
				<ToolBarButton Command="{Binding SaveFilterCommand}" ImageSource="/Controls;component/Images/Save.png" ToolTip="Сохранить" />
				<ToolBarButton Command="{Binding RemoveFilterCommand}" ImageSource="/Controls;component/Images/Close.png" ToolTip="Удалить" />
			</StackPanel>
		</Grid>
		<Grid Grid.Row="1" Visibility="{Binding HasPeriod, Mode=OneTime, Converter={StaticResource BoolToVisibilityConverter}}">
			<TextBlock Text="1111111111111111111111" />
		</Grid>
		<ContentPresenter Grid.Row="2" Content="{Binding MainViewModel, Mode=OneTime}" />
		<TextBlock Grid.Row="3" Visibility="{Binding IsAdditionalExpanded, Converter={StaticResource InversedBoolToVisibilityConverter}}">
			<Hyperlink Command="{Binding ExpandAdditionalCommand}" Style="{StaticResource HyperlinkStyle}">
				<TextBlock Margin="2" Text="Показать настройки отображения отчёта" TextDecorations="Underline" />
			</Hyperlink>
		</TextBlock>
		<GroupBox Grid.Row="4" Padding="10" Visibility="{Binding IsAdditionalExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
			<GroupBox.Header>
				<TextBlock VerticalAlignment="Center">
					<Hyperlink Command="{Binding ExpandAdditionalCommand}" Style="{StaticResource HyperlinkStyle}">
						<TextBlock Margin="2" Text="Скрыть настройки отображения отчёта" TextDecorations="Underline" />
					</Hyperlink>
				</TextBlock>
			</GroupBox.Header>
			<StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2">
				<CheckBox Margin="4" Content="Отображать название фильтра" Foreground="White" IsChecked="{Binding PrintName}" />
				<CheckBox Margin="4" Content="Отображать название фильтра в заголовке" Foreground="White" IsChecked="{Binding PrintNameInHeader}" />
				<CheckBox Margin="4" Content="Отображать границы периода формирования отчёта" Foreground="White" IsChecked="{Binding PrintPeriod}" Visibility="{Binding HasPeriod, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneTime}" />
				<CheckBox Margin="4" Content="Отображать дату и время формирования отчёта" Foreground="White" IsChecked="{Binding PrintDate}" />
				<CheckBox Margin="4" Content="Отображать сведения о пользователе, сформировавшем отчёт" Foreground="White" IsChecked="{Binding PrintUser}" />
			</StackPanel>
		</GroupBox>
	</Grid>
</UserControl>
