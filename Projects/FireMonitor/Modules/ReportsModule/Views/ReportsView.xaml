<UserControl x:Class="ReportsModule.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:XButtons="clr-namespace:Buttons;assembly=Buttons">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding ReportContent}" />
        <StackPanel Grid.Row="1" HorizontalAlignment="Right">
            <XButtons:XButton Command="{Binding ShowReport1Command}" Height="30" Margin="2" Background="DarkRed" Padding="5">
                <TextBlock Text="Количество устройств по типам" FontSize="12" />
            </XButtons:XButton>
        </StackPanel>
    </Grid>
</UserControl>