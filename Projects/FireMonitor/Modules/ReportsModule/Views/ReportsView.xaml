<UserControl x:Class="ReportsModule.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:Microsoft.Reporting.WinForms;assembly=Microsoft.ReportViewer.WinForms"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--<WindowsFormsHost Height="300" HorizontalAlignment="Left" x:Name="winFormsHost" VerticalAlignment="Top" Width="300">
            <c:ReportViewer x:Name="ReportEmployee"/>
        </WindowsFormsHost>-->
        <ContentControl Grid.Row="0" Content="{Binding ReportContent}" />
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal">
            <Controls:XButton Command="{Binding ShowJournalReportCommand}" Height="30" Margin="2" Background="DarkRed" Padding="5" Width="150">
                <TextBlock Text="Журнал событий" FontSize="12" HorizontalAlignment="Center"/>
            </Controls:XButton>
            <Controls:XButton Command="{Binding ShowDriverCountReportCommand}" Height="30" Margin="2" Background="DarkRed" Padding="5" >
                <TextBlock Text="Количество устройств по типам" FontSize="12" HorizontalAlignment="Center"/>
            </Controls:XButton>
            <Controls:XButton Command="{Binding ShowDeviceParamsReportCommand}" Height="30" Margin="2" Background="DarkRed" Padding="5" Width="150">
                <TextBlock Text="Параметры устройств" FontSize="12" HorizontalAlignment="Center"/>
            </Controls:XButton>
            <Controls:XButton Command="{Binding ShowDeviceListReportCommand}" Height="30" Margin="2" Background="DarkRed" Padding="5" Width="150">
                <TextBlock Text="Список устройств" FontSize="12" HorizontalAlignment="Center"/>
            </Controls:XButton>
        </StackPanel>
        
    </Grid>
</UserControl>