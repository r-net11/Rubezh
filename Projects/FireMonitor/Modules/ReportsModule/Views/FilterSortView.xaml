<UserControl x:Class="ReportsModule.Views.FilterSortView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="300" d:DesignWidth="300" mc:Ignorable="d">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<ItemsControl Grid.Column="0" ItemsSource="{Binding Columns}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel Orientation="Vertical" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<RadioButton Margin="2" Content="{Binding Value}" Foreground="White" GroupName="SortColumn" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
		<ListBox Grid.Column="1" FrameworkElement.FocusVisualStyle="{x:Null}" ItemsSource="{Binding Columns}" ItemContainerStyle="{x:Null}">
			<ListBox.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel Orientation="Vertical" />
				</ItemsPanelTemplate>
			</ListBox.ItemsPanel>
			<ListBox.Template>
				<ControlTemplate>
					<ItemsPresenter />
				</ControlTemplate>
			</ListBox.Template>
			<ListBox.ItemTemplate>
				<DataTemplate>
					<RadioButton Margin="2" Content="{Binding Value}" Foreground="White" IsHitTestVisible="False" IsChecked="{Binding IsSelected, Mode=TwoWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBoxItem}}}" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
		<StackPanel Grid.Column="2">
			<RadioButton Margin="2" Content="По возрастанию" Foreground="White" GroupName="SortOrder" IsChecked="{Binding SortAscending}" />
			<RadioButton Margin="2" Content="По убыванию" Foreground="White" GroupName="SortOrder" IsChecked="{Binding SortAscending, Converter={StaticResource InverseBooleanConverter}}" />
		</StackPanel>
	</Grid>
</UserControl>
