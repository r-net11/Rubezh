<UserControl x:Class="DevicesModule.Views.DeviceDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Views="clr-namespace:DevicesModule.Views"
             MinHeight="300" MinWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <ContentControl Content="{Binding DeviceControlContent}" Width="50" Height="50" Background="Black" Margin="0, 0, 0, 5" />
            <TextBlock Text="{Binding DeviceState.StateType, Converter={StaticResource StateTypeToStringConverter}}" Margin="5" VerticalAlignment="Center" Foreground="White" />
        </StackPanel>
        <TabControl Grid.Row="1">
            <TabItem Header="Основные">
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Visibility="{Binding Driver.HasImage, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Image Source="{Binding Driver.ImageSource}" Width="20" Height="20" Margin="5" />
                        </StackPanel>
                        <TextBlock Text="{Binding Driver.Name}" VerticalAlignment="Center" Foreground="White" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="1">
                        <TextBlock Text="Тип устройства: " FontWeight="Bold" Foreground="Orange" />
                        <TextBlock Text="{Binding Driver.CategoryName}" Foreground="White" />
                        <TextBlock Text=", " Foreground="White" />
                        <TextBlock Text="{Binding Driver.DeviceTypeName}" Foreground="White" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="1">
                        <TextBlock Text="Подключено: " FontWeight="Bold" Foreground="Orange" />
                        <TextBlock Text="{Binding ConnectedTo}" Foreground="White" />
                    </StackPanel>
                    <StackPanel Visibility="{Binding Driver.IsZoneDevice, Converter={StaticResource BoolToVisibilityConverter}}" Orientation="Horizontal" Margin="1">
                        <TextBlock Text="Зона: " FontWeight="Bold" Foreground="Orange" />
                        <TextBlock Text="{Binding PresentationZone}" Foreground="White" />
                    </StackPanel>
                    <StackPanel Visibility="{Binding Driver.IsZoneLogicDevice, Converter={StaticResource BoolToVisibilityConverter}}" Margin="1">
                        <TextBlock Text="Логика срабатывания по состоянию зон:" FontWeight="Bold" Foreground="Orange" />
                        <TextBlock Text="{Binding PresentationZone}" Foreground="White" />
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="Состояние" TextElement.Foreground="White">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding DeviceState.ParentStringStates}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding .}" Margin="1" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ItemsControl ItemsSource="{Binding DeviceState.States}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DriverState.Name}" Margin="1" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding Parameters}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding .}" Margin="1"  />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="Управление" Visibility="{Binding Driver.CanControl, Converter={StaticResource BoolToVisibilityConverter}}"
                     IsSelected="{Binding IsValveControlSelected}">
                <Views:DeviceControlView DataContext="{Binding DeviceControlViewModel}" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>