<UserControl x:Class="DevicesModule.Views.DeviceDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             MinWidth="400" MinHeight="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" HorizontalAlignment="Left">
            <ContentControl Content="{Binding DeviceControlContent}" Margin="0, 0, 0, 5" />
        </Grid>
        
        <TabControl Grid.Row="1">
            
            <TabItem Header="Основные">
                <StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <StackPanel Visibility="{Binding HasImage, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Image Source="{Binding ImageSource}" Width="16" Height="16" Margin="5, 0, 5, 0" />
                        </StackPanel>
                        <TextBlock Text="{Binding DriverName}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="3">
                        <TextBlock Text="Тип устройства: " FontWeight="Bold" Foreground="YellowGreen" />
                        <TextBlock Text="{Binding DeviceCategory}" />
                        <TextBlock Text=", " />
                        <TextBlock Text="{Binding DeviceType}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="3">
                        <TextBlock Text="Подключено: " FontWeight="Bold" Foreground="YellowGreen" />
                        <TextBlock Text="{Binding ConnectedTo}" />
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsZoneDevice, Converter={StaticResource BoolToVisibilityConverter}}" Margin="3">
                        <TextBlock Text="Зона:" FontWeight="Bold" Foreground="YellowGreen" />
                        <TextBlock Text="{Binding PresentationZone}" />
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsZoneLogicDevice, Converter={StaticResource BoolToVisibilityConverter}}" Margin="3">
                        <TextBlock Text="Логика срабатывания по состоянию зон:" FontWeight="Bold" Foreground="YellowGreen" />
                        <TextBlock Text="{Binding PresentationZone}" />
                    </StackPanel>

                </StackPanel>
            </TabItem>
            
            <TabItem Header="Состояние">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Состояние" FontWeight="Bold" Foreground="YellowGreen" Margin="0,0,5,0" />
                        <TextBlock Text="{Binding State}" />
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding ParentStates}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding SelfStates}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding Parameters}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <TabItem Header="Управление" Visibility="{Binding CanControl, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TabControl>
                        <TabItem Header="Положение">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Закрыть" Command="{Binding CloseCommand}" />
                                <Button Content="Стоп" Command="{Binding StopCommand}" />
                                <Button Content="Открыть" Command="{Binding OpenCommand}" />
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="Автоматика">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Включение автоматики" Command="{Binding AutomaticOnCommand}" />
                                <Button Content="Отключение автоматики" Command="{Binding AutomaticOffCommand}" />
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="Задержка пуска">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Пуск" Command="{Binding StartCommand}" />
                                <Button Content="Стоп" Command="{Binding StopCommand}" />
                            </StackPanel>
                        </TabItem>
                    </TabControl>
                    <Button Content="Подтвердить" Command="{Binding ConfirmCommand}" Width="100" Margin="3" />
                </StackPanel>
            </TabItem>
            
        </TabControl>
        
    </Grid>
</UserControl>
