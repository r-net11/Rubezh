<UserControl x:Class="DevicesModule.Views.DeviceControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

<StackPanel>
        <TabControl>
            <TabItem Header="Вкл. Выкл.">
                <StackPanel>
                    <ListBox ItemsSource="{Binding Commands}" SelectedItem="{Binding SelectedCommand}" />
                    <Button Content="Закрыть" Command="{Binding CloseCommand}" Margin="2" />
                    <Button Content="Стоп" Command="{Binding StopCommand}" Margin="2" />
                    <Button Content="Открыть" Command="{Binding OpenCommand}" Margin="2" />
                </StackPanel>
            </TabItem>
        </TabControl>
        <Button Content="Подтвердить" Command="{Binding ConfirmCommand}" Width="100" Margin="3" />
    </StackPanel>
</UserControl>