<UserControl x:Class="DevicesModule.Views.MPTTimerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="300" MinHeight="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Device.Zone.PresentationName}" Foreground="White" FontSize="16" Margin="2" />
        <Label Grid.Row="1" Content="{Binding TimeLeft}" FontSize="40" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Foreground="Red" Margin="2" />
    </Grid>
</UserControl>