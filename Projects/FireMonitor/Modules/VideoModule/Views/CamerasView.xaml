<UserControl x:Class="VideoModule.Views.CamerasView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:DragDrop="clr-namespace:Infrastructure.Common.Services.DragDrop;assembly=Infrastructure.Common">
	<!--<DataGrid ItemsSource="{Binding Cameras}" SelectedItem="{Binding SelectedCamera}">
		<DataGrid.Columns>
			<DataGridTextColumn Width="200" Binding="{Binding Camera.Name}" Header="Название" />
			<DataGridTextColumn Width="200" Binding="{Binding Camera.Address}" Header="URL" />
			<DataGridTextColumn Width="*" Binding="{Binding PresenrationZones}" Header="Зоны" />
		</DataGrid.Columns>
		<DataGrid.ContextMenu>
			<ContextMenu>
				<IconedMenuItem Command="{Binding ShowOnPlanCommand}" ImageSource="/Controls;component/Images/map_black.png" Text="Показать на плане" />
                <IconedMenuItem Command="{Binding PlayVideoCommand}" ImageSource="/Controls;component/Images/map_black.png" Text="Показать видео" />
            </ContextMenu>
		</DataGrid.ContextMenu>
	</DataGrid>-->
   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="300" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <XDataGrid Grid.Row="0" ItemsSource="{Binding Cameras}" SelectedItem="{Binding SelectedCamera}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="200" Binding="{Binding Camera.Name}" Header="Название" />
                <DataGridTextColumn Width="200" Binding="{Binding Camera.Address}" Header="URL" />
                <DataGridTextColumn Width="200" Binding="{Binding PresentationZones}" Header="Зоны" />
            </DataGrid.Columns>
            <XDataGrid.ContextMenu>
                <ContextMenu OverridesDefaultStyle="True">
                    <IconedMenuItem Command="{Binding ShowOnPlanCommand}" ImageSource="/Controls;component/Images/map_black.png" Text="Показать на плане" />
                    <IconedMenuItem Command="{Binding PlayVideoCommand}" ImageSource="/Controls;component/Images/map_black.png" Text="Показать видео" />
                </ContextMenu>
            </XDataGrid.ContextMenu>
        </XDataGrid>
        <Grid Grid.Row="1">
            <Grid.Background>
                <VisualBrush AlignmentX="Center" Stretch="Uniform">
                    <VisualBrush.Visual>
                        <Image Source="{Binding StartedCamera.ImageSource, Mode=OneWay}" />
                    </VisualBrush.Visual>
                </VisualBrush>
            </Grid.Background>
        </Grid>
    </Grid>
</UserControl>