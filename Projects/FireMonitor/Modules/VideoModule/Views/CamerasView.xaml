<UserControl x:Class="VideoModule.Views.CamerasView"
	 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	 xmlns:dragDrop="clr-namespace:Infrastructure.Common.Services.DragDrop;assembly=Infrastructure.Common">
	<UserControl.Resources>
		<ContextMenu  x:Key="CameraContextMenu">
			<IconedMenuItem Command="{Binding ConnectCommand}" Text="Подключить" Visibility="{Binding SelectedCamera.IsChannel, Converter={StaticResource InversedBoolToVisibilityConverter}}" />
			<IconedMenuItem Command="{Binding DisconnectCommand}" Text="Отключить" Visibility="{Binding SelectedCamera.IsChannel, Converter={StaticResource InversedBoolToVisibilityConverter}}" />
			<IconedMenuItem Command="{Binding ShowOnPlanCommand}" ImageSource="/Controls;component/Images/map_black.png" Text="Показать на плане" />
			<IconedMenuItem Command="{Binding ShowPropertiesCommand}" ImageSource="/Controls;component/Images/BSettings.png" Text="Свойства" />
		</ContextMenu>
	</UserControl.Resources>
	<TreeList ContextMenu="{StaticResource CameraContextMenu}" Focusable="True" SelectedTreeNode="{Binding SelectedCamera, Mode=TwoWay, NotifyOnSourceUpdated=True,NotifyOnTargetUpdated=True,UpdateSourceTrigger=PropertyChanged}" Source="{Binding Cameras}">
		<TreeList.View>
			<GridView>
				<GridView.Columns>
					<GridViewColumn Width="250" Header="Камера">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<dragDrop:DragDropDecorator DragCommand="{Binding CreateDragObjectCommand}" DragVisualProvider="{Binding CreateDragVisual}" DynamicCursor="True" IsSource="True">
										<Image Margin="2,0,0,0" Source="{Binding VisualizationState, Converter={StaticResource VisualizationStateToImageSourceConverter}}" />
									</dragDrop:DragDropDecorator>
									<StackPanel Orientation="Horizontal">
										<Image Margin="2,0,0,0" Source="{Binding Status, Converter={StaticResource ConnectionStateToImageSourceConverter}}" Visibility="{Binding IsChannel, Converter={StaticResource InversedBoolToVisibilityConverter}}" />
										<Image Width="16" Height="16" Margin="5, 0, 5, 0" HorizontalAlignment="Left" Source="{Binding Camera.ImageSource}" />
										<TextBlock Text="{Binding  Camera.Name, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" VerticalAlignment="Center" />
									</StackPanel>
								</StackPanel>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					
					<GridViewColumn Width="100" Header="Адрес">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding PresentationAddress}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn Width="100" Header="Порт">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Camera.Port}" Visibility="{Binding IsDvr, Converter={StaticResource BoolToVisibilityConverter}}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn Width="150" Header="Имя пользователя">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Camera.Login}" Visibility="{Binding IsDvr, Converter={StaticResource BoolToVisibilityConverter}}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn Width="100" Header="Пароль">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Camera.Password}" Visibility="{Binding IsDvr, Converter={StaticResource BoolToVisibilityConverter}}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn Width="100" Header="Состояние">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding PresentationState}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>

					<GridViewColumn Width="200" Header="Зона или Логика">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding PresentationZones}" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView.Columns>
			</GridView>
		</TreeList.View>
	</TreeList>
</UserControl>