<UserControl x:Class="StrazhModule.Views.DeviceDetailsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Views="clr-namespace:StrazhModule.Views"
			 xmlns:langCommon="clr-namespace:Localization.Strazh.Common;assembly=Localization"
			 xmlns:lang="clr-namespace:Localization.Strazh.Views;assembly=Localization"
			 MinWidth="400" MinHeight="400" MaxWidth="600" MaxHeight="600">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<StackPanel Grid.Column="0" Orientation="Horizontal">
				<Border Width="50" Height="50" Margin="0, 0, 0, 5" Background="{Binding DevicePicture}" BorderThickness="0" />
				<StackPanel>
					<Views:DeviceStateView Margin="5 0 0 0" TextElement.Foreground="White" />
					<Button Content="{x:Static langCommon:CommonResources.ResetBreakinState}" Command="{Binding ClearPromptWarningCommand}" Visibility="{Binding CanShowClearPromptWarning, Converter={StaticResource BoolToVisibilityConverter}}" Width="160" Margin="5" />
				</StackPanel>
			</StackPanel>
		</Grid>
		<TabControl Grid.Row="1">
			<TabItem Header="{x:Static lang:CommonViews.Main}">
				<ScrollViewer VerticalScrollBarVisibility="Auto">
					<ItemsControl Background="White">
						<ItemsControl.Items>
							<StackPanel Margin="2" Orientation="Horizontal">
                                <TextBlock Width="110" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" Text="{x:Static lang:CommonViews.DeviceWithColon}" />
								<WrapPanel>
									<TextBlock>
										<Hyperlink Command="{Binding ShowCommand}" Style="{StaticResource HyperlinkStyle2}">
											<StackPanel Orientation="Horizontal">
												<Image Width="16" Source="{Binding Device.Driver.ImageSource}" />
												<TextBlock Margin="2" VerticalAlignment="Center" Text="{Binding Device.Name}" TextWrapping="Wrap" />
											</StackPanel>
										</Hyperlink>
									</TextBlock>
								</WrapPanel>
							</StackPanel>

							<WrapPanel Margin="2">
                                <TextBlock Width="110" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" Text="{x:Static lang:CommonViews.ConnectedTo}" />
								<TextBlock>
									<Hyperlink Command="{Binding ShowParentCommand}" Style="{StaticResource HyperlinkStyle2}">
										<StackPanel Orientation="Horizontal">
											<Image Width="16" Source="{Binding Device.Parent.Driver.ImageSource}" />
											<TextBlock Margin="2" VerticalAlignment="Center" Text="{Binding Device.Parent.Name}" TextWrapping="Wrap" />
										</StackPanel>
									</Hyperlink>
								</TextBlock>
								<WrapPanel.Style>
									<Style>
										<Style.Triggers>
											<DataTrigger Binding="{Binding Device.Parent}" Value="{x:Null}">
												<Setter Property="StackPanel.Visibility" Value="Collapsed" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</WrapPanel.Style>
							</WrapPanel>

							<WrapPanel Margin="2">
                                <TextBlock Width="110" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" Text="{x:Static lang:CommonViews.ZoneWithColon}" />
								<TextBlock>
									<Hyperlink Command="{Binding ShowZoneCommand}" Style="{StaticResource HyperlinkStyle2}">
										<StackPanel Orientation="Horizontal">
											<Image Width="16" Source="/Controls;component/Images/Zone.png" />
											<TextBlock Margin="2" VerticalAlignment="Center" Text="{Binding PresentationZone}" TextWrapping="Wrap" />
										</StackPanel>
									</Hyperlink>
								</TextBlock>
								<WrapPanel.Style>
									<Style>
										<Style.Triggers>
											<DataTrigger Binding="{Binding PresentationZone}" Value="{x:Null}">
												<Setter Property="StackPanel.Visibility" Value="Collapsed" />
											</DataTrigger>
											<DataTrigger Binding="{Binding PresentationZone}" Value="">
												<Setter Property="StackPanel.Visibility" Value="Collapsed" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</WrapPanel.Style>
							</WrapPanel>

							<StackPanel Margin="2" Orientation="Horizontal">
								<WrapPanel>
									<TextBlock>
										<Hyperlink Command="{Binding ShowJournalCommand}" Style="{StaticResource HyperlinkStyle2}">
											<StackPanel Orientation="Horizontal">
												<Grid Width="16" Background="{DynamicResource BJournal}" />
												<TextBlock Margin="2" VerticalAlignment="Center" FontWeight="Bold" Text="{x:Static langCommon:CommonResources.ShowRelatedEvents}" TextWrapping="Wrap" />
											</StackPanel>
										</Hyperlink>
									</TextBlock>
								</WrapPanel>
							</StackPanel>

							<ItemsControl Margin="2" ItemsSource="{Binding PlanNames}">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<StackPanel Orientation="Horizontal">
											<TextBlock Width="110" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" Text="{x:Static lang:CommonViews.Plan}" />
											<TextBlock>
												<Hyperlink Command="{Binding ShowOnPlanCommand}" Style="{StaticResource HyperlinkStyle2}">
													<StackPanel Orientation="Horizontal">
														<Image Width="16" Margin="1" Source="/Controls;component/Images/CMap.png" />
														<TextBlock Margin="1" VerticalAlignment="Center" Text="{Binding Name}" />
													</StackPanel>
												</Hyperlink>
											</TextBlock>
										</StackPanel>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</ItemsControl.Items>
					</ItemsControl>
				</ScrollViewer>
			</TabItem>
            <TabItem Header="{x:Static lang:CommonViews.Parameters}" Visibility="{Binding DevicePropertiesViewModel.HasAUParameters, Converter={StaticResource BoolToVisibilityConverter}}">
				<DataGrid CanUserSortColumns="True" ItemsSource="{Binding DevicePropertiesViewModel.Properties}">
					<DataGrid.Columns>
                        <DataGridTemplateColumn Width="*" Header="{x:Static lang:CommonViews.Parameter}">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" />
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="*" Header="{x:Static lang:CommonViews.Value}">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<TextBlock Margin="5 0 0 0" Text="{Binding Value}" ToolTip="{Binding Value}" />
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
					</DataGrid.Columns>
				</DataGrid>
			</TabItem>
            <TabItem DataContext="{Binding DeviceCommandsViewModel}" Header="{x:Static lang:CommonViews.Management}" Visibility="{Binding CanControl, Converter={StaticResource BoolToVisibilityConverter}}">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<StackPanel Grid.Row="1" Background="White">
                        <GroupBox Header="{x:Static lang:CommonViews.ChangeMode}" Margin="5" Padding="5">
							<StackPanel>
                                <Button Content="{x:Static langCommon:CommonResources.Opened}" Command="{Binding DeviceAccessStateOpenAlwaysCommand}" Width="200" Height="30" Margin="2" />
                                <Button Content="{x:Static lang:CommonViews.Norm}" Command="{Binding DeviceAccessStateNormalCommand}" Width="200" Height="30" Margin="2" />
								<Button Content="{x:Static langCommon:CommonResources.Closed}" Command="{Binding DeviceAccessStateCloseAlwaysCommand}" Width="200" Height="30" Margin="2" />
							</StackPanel>
						</GroupBox>
                        <GroupBox Header="{x:Static lang:CommonViews.OperManagementCommands}" Margin="5" Padding="5">
							<StackPanel>
                                <Button Content="{x:Static langCommon:CommonResources.Open}" Command="{Binding OpenCommand}" Width="200" Height="30" Margin="2" />
                                    <Button Content="{x:Static langCommon:CommonResources.Close}" Command="{Binding CloseCommand}" Width="200" Height="30" Margin="2" />
							</StackPanel>
						</GroupBox>
					</StackPanel>
				</Grid>
			</TabItem>
		</TabControl>
	</Grid>
</UserControl>