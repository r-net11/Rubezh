<UserControl x:Class="AlarmModule.Views.AlarmListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Views="clr-namespace:AlarmModule.Views"
             xmlns:WpfToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Button Content="Сбросить все" Command="{Binding ResetAllCommand}" />
        </StackPanel>
        <WpfToolkit:DataGrid Grid.Row="1" ItemsSource="{Binding Alarms}" SelectedItem="{Binding SelectedAlarm}"
                            ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                            AutoGenerateColumns="False" 
                            IsReadOnly="True"
                            CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False"
                            HorizontalGridLinesBrush="Beige" VerticalGridLinesBrush="Beige"
                            Background="White" BorderBrush="Black" BorderThickness="0" FrozenColumnCount="2"
                            SelectionMode="Single" SelectionUnit="FullRow"
                            HeadersVisibility="Column" RowHeaderWidth="0" RowHeight="20">
            <WpfToolkit:DataGrid.Columns>
                <WpfToolkit:DataGridTextColumn Header="Время" Binding="{Binding Time}" />
                <WpfToolkit:DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
            </WpfToolkit:DataGrid.Columns>
        </WpfToolkit:DataGrid>
    </Grid>
</UserControl>
