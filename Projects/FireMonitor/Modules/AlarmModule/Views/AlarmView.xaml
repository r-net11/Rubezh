<UserControl x:Class="AlarmModule.Views.AlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderThickness="2" BorderBrush="DarkGray" CornerRadius="5" Margin="5">
        <Border.Background>
            <LinearGradientBrush StartPoint="0.5, 0" EndPoint="0.5, 1">
                <GradientStop Offset="0" Color="DarkGray" />
                <GradientStop Offset="1" Color="LightGray" />
            </LinearGradientBrush>
        </Border.Background>
        <StackPanel HorizontalAlignment="Left" Width="Auto">
            <StackPanel>
                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12" Margin="2" />
                <TextBlock Text="{Binding Description}" Foreground="Black" Margin="2" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Button Content="Сбросить" Command="{Binding ResetCommand}" Height="30" />
                <Button Content="Закрыть" Command="{Binding CloseCommand}" Height="30" />
                <Button Content="Отложить" Command="{Binding LeaveCommand}" Height="30" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Button Content="Просмотр на плане" Command="{Binding ShowOnPlanCommand}" Height="30" />
                <Button Content="Устройство в дереве" Command="{Binding ShowDeviceCommand}" Height="30" />
                <Button Content="Зона в списке" Command="{Binding ShowZoneCommand}" Height="30" />
            </StackPanel>
            
            <StackPanel Orientation="Horizontal">
                <Button Content="Инструкции" Command="{Binding ShowInstructionCommand}" Height="30" />
                <Button Content="Оповещение по телефону" Command="{Binding NotifyPthoneCommand}" Height="30" />
                <Button Content="СМС сообщение" Command="{Binding NotifySmsCommand}" Height="30" />
                <Button Content="Включить видео" Command="{Binding ShowVideoCommand}" Height="30" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
