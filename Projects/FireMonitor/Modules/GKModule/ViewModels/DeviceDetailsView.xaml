<UserControl x:Class="GKModule.Views.DeviceDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:GKModule.Converters"
             xmlns:Views="clr-namespace:GKModule.Views"
             MinWidth="400" MinHeight="400" MaxWidth="600" MaxHeight="600">
    <UserControl.Resources>
        <Converters:DeviceControlRegimeToStringConverter x:Key="DeviceControlRegimeToStringConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <ContentControl Content="{Binding DeviceControlContent}" Width="50" Height="50" Background="Black" Margin="0, 0, 0, 5" />
            <Views:DeviceStateView TextElement.Foreground="White" />
        </StackPanel>
        <TabControl Grid.Row="1">
            <TabItem Header="Основные">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Device.Driver.ImageSource}" Width="16" Margin="2 2 5 2" />
                            <TextBlock Text="{Binding Device.PresentationAddressAndDriver}" FontWeight="Bold" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="Orange" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Подключено к " FontWeight="Bold" VerticalAlignment="Center" Foreground="Orange" Margin="2 2 5 2" />
                            <TextBlock Margin="2" Text="{Binding Device.Parent.Driver.ShortName}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="White" />
                            <StackPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Device.Parent}" Value="{x:Null}" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                        </StackPanel>
                        <WrapPanel Orientation="Horizontal">
                            <TextBlock Text="Зона или Логика: " FontWeight="Bold" VerticalAlignment="Center" Foreground="Orange" Margin="2 2 5 2" />
                            <TextBlock Margin="2" Text="{Binding PresentationZone}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="White" />
                            <WrapPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PresentationZone}" Value="{x:Null}" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding PresentationZone}" Value="" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </WrapPanel.Style>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Параметры" Visibility="{Binding HasAUParameters, Converter={StaticResource BoolToVisibilityConverter}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition SharedSizeGroup="group1" Width="300" />
                                <ColumnDefinition SharedSizeGroup="group2" Width="100" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                        <ItemsControl ItemsSource="{Binding AUParameterValues}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition SharedSizeGroup="group1" Width="300" />
                                            <ColumnDefinition SharedSizeGroup="group2" Width="100" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="White" HorizontalAlignment="Right" />
                                        <TextBlock Grid.Column="1" Text="{Binding Value}" Foreground="White" Margin="5, 0, 0, 0" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>                
            </TabItem>
            <TabItem Header="Управление" DataContext="{Binding DeviceCommandsViewModel}" Visibility="{Binding IsTriStateControl, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Режим" Foreground="White" HorizontalAlignment="Center" Margin="2" />
                        <TextBlock Text="{Binding ControlRegime, Converter={StaticResource DeviceControlRegimeToStringConverter}}" Foreground="White" Margin="2" />
                    </StackPanel>
                    <Grid Grid.Row="1" Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Button Content="Автоматика" Command="{Binding SetAutomaticStateCommand}" Width="200" Margin="2" />
                            <Button Content="Ручное" Command="{Binding SetManualStateCommand}" Width="200" Margin="2" />
                            <Button Content="Отключение" Command="{Binding SetIgnoreStateCommand}" Width="200" Margin="2" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" IsEnabled="{Binding IsControlRegime}">
                            <Button Content="Включить" Command="{Binding TurnOnCommand}" Width="200" Margin="2" />
                            <Button Content="Включить немедленно" Command="{Binding TurnOnNowCommand}" Width="200" Margin="2" />
                            <Button Content="Выключить" Command="{Binding TurnOffCommand}" Width="200" Margin="2" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Управление" DataContext="{Binding DeviceCommandsViewModel}" Visibility="{Binding IsBiStateControl, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Режим" Foreground="White" HorizontalAlignment="Center" Margin="2" />
                        <TextBlock Text="{Binding ControlRegime, Converter={StaticResource DeviceControlRegimeToStringConverter}}" Foreground="White" Margin="2" />
                    </StackPanel>
                    <Grid Grid.Row="1" Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Снять отключение" Command="{Binding SetOnStateCommand}" Width="200" VerticalAlignment="Top" Margin="2" />
                        <Button Grid.Column="1" Content="Отключение" Command="{Binding SetOffStateCommand}" Width="200" VerticalAlignment="Top" Margin="2" />
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>