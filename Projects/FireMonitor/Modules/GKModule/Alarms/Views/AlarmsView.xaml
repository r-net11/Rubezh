<UserControl x:Class="GKModule.Views.AlarmsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:GKModule.Converters">
    <UserControl.Resources>
        <Converters:AlarmTypeToColorConverter x:Key="AlarmTypeToColorConverter" />
        <Converters:AlarmTypeToStringConverter x:Key="AlarmTypeToStringConverter" />
        <Converters:AlarmTypeToIconPathConverter x:Key="AlarmTypeToIconPathConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="100" MinHeight="100" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" ItemsSource="{Binding Alarms}" SelectedItem="{Binding SelectedAlarm}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Состояние" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="16" Source="{Binding Alarm.AlarmType, Converter={StaticResource AlarmTypeToIconPathConverter}}" VerticalAlignment="Center"/>
                                <TextBlock VerticalAlignment="Center" Text="{Binding Alarm.AlarmType, Converter={StaticResource AlarmTypeToStringConverter}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTemplateColumn Header="Объект" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="16" Source="{Binding ImageSource}" VerticalAlignment="Center" />
                                <TextBlock VerticalAlignment="Center" Margin="2">
                                    <Hyperlink Command="{Binding ShowObjectOrPlanCommand}" Style="{StaticResource HyperlinkStyle2}">
                                        <TextBlock Text="{Binding ObjectName}" />
                                    </Hyperlink>
                                </TextBlock>    
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="План" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding Plans}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="2 0">
                                            <Hyperlink Command="{Binding ShowOnPlanCommand}" Style="{StaticResource HyperlinkStyle2}">
                                                <StackPanel Orientation="Horizontal">
                                                    <Image Width="16" Source="/Controls;component/Images/map2.png" VerticalAlignment="Center" Margin="1" />
                                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="1" />
                                                </StackPanel>
                                            </Hyperlink>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Команды" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="2 0">
                                    <Hyperlink Command="{Binding ShowJournalCommand}" Style="{StaticResource HyperlinkStyle2}">
                                        <TextBlock Text="Журнал" />
                                    </Hyperlink>
                                </TextBlock>
                                
                                <TextBlock Margin="2 0" Visibility="{Binding CanResetCommand, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Hyperlink Command="{Binding ResetCommand}" Style="{StaticResource HyperlinkStyle2}">
                                        <TextBlock Text="Сбросить" />
                                    </Hyperlink>
                                </TextBlock>

                                <TextBlock Margin="2 0" Visibility="{Binding CanResetIgnoreCommand, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Hyperlink Command="{Binding ResetIgnoreCommand}" Style="{StaticResource HyperlinkStyle2}">
                                        <TextBlock Text="Снять отключение" />
                                    </Hyperlink>
                                </TextBlock>

                                <TextBlock Margin="2 0" Visibility="{Binding CanTurnOnAutomaticCommand, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Hyperlink Command="{Binding TurnOnAutomaticCommand}" Style="{StaticResource HyperlinkStyle2}">
                                        <TextBlock Text="Включить автоматику" />
                                    </Hyperlink>
                                </TextBlock>

                                <TextBlock Margin="2 0" Visibility="{Binding CanTurnOffCommand, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Hyperlink Command="{Binding TurnOffCommand}" Style="{StaticResource HyperlinkStyle2}">
                                        <TextBlock Text="Выключить" />
                                    </Hyperlink>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Setters>
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Header="Сбросить" Command="{Binding ResetCommand}" />
                                    <MenuItem Header="Снять отключение" Command="{Binding ResetIgnoreCommand}" />
                                    <MenuItem Header="Включить автоматику" Command="{Binding TurnOnAutomaticCommand}" />
                                    <MenuItem Header="Выключить" Command="{Binding TurnOffCommand}" />
                                    <MenuItem Header="Показать на плане" Command="{Binding ShowOnPlanCommand}" />
                                    <MenuItem Header="Показать" Command="{Binding ShowObjectCommand}" />
                                    <MenuItem Header="Показать связанные события" Command="{Binding ShowJournalCommand}" />
                                    <MenuItem Header="Свойства" Command="{Binding ShowPropertiesCommand}" />
                                    <MenuItem Header="Инструкции" Command="{Binding ShowInstructionCommand}" />
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style.Setters>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <Style.Setters>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type DataGridCell}">
                                    <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Background" Value="{Binding Alarm.AlarmType, Converter={StaticResource AlarmTypeToColorConverter}}" />
                    </Style.Setters>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PressedBackgroundBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>
        <GridSplitter Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Height="5" Background="Transparent" Cursor="SizeNS" />
        <Border Grid.Row="2" DataContext="{Binding SelectedAlarm}" TextBlock.FontSize="11" TextBlock.Foreground="White"
                CornerRadius="5" BorderThickness="0" Margin="0, 5, 0, 0" Padding="5" Background="{DynamicResource BackgroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <WrapPanel Margin="1">
                    <TextBlock Text="{Binding Instruction.Text}" Foreground="White" TextWrapping="Wrap" />
                </WrapPanel>
            </ScrollViewer>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Снять все отключения" Command="{Binding ResetIgnoreAllCommand}" Width="150" Margin="2" />
        </StackPanel>
    </Grid>
</UserControl>