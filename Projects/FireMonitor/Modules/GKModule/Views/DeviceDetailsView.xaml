<UserControl x:Class="GKModule.Views.DeviceDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:GKModule.Converters"
             xmlns:Views="clr-namespace:GKModule.Views"
             MinWidth="400" MinHeight="400" MaxWidth="600" MaxHeight="600">
    <UserControl.Resources>
        <Converters:DeviceControlRegimeToStringConverter x:Key="DeviceControlRegimeToStringConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
				<Border Background="{Binding DevicePicture}" BorderThickness="0" Width="50" Height="50" Margin="0, 0, 0, 5" />
                <Views:DeviceStateView TextElement.Foreground="White" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <StackPanel>
                    <StackPanel Visibility="{Binding HasOnDelay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Задержка на включение" Foreground="White" Margin="2" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DeviceState.OnDelay}" Foreground="Red" FontSize="24" Margin="2" />
                            <TextBlock Text="Сек" Foreground="Red" FontSize="24" Margin="2" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Visibility="{Binding HasHoldDelay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Удержание" Foreground="White" Margin="2" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DeviceState.HoldDelay}" Foreground="Red" FontSize="24" Margin="2" />
                            <TextBlock Text="Сек" Foreground="Red" FontSize="24" Margin="2" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Visibility="{Binding HasOffDelay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Задержка на выключение" Foreground="White" Margin="2" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DeviceState.OffDelay}" Foreground="Red" FontSize="24" Margin="2" />
                            <TextBlock Text="Сек" Foreground="Red" FontSize="24" Margin="2" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Grid>
        <TabControl Grid.Row="1">
            <TabItem Header="Основные">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Устройство: " Width="100" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="Orange" Margin="2" />
                            <Image Source="{Binding Device.Driver.ImageSource}" Width="16" Margin="2 2 5 2" />
                            <WrapPanel>
                                <TextBlock>
				                    <Hyperlink Command="{Binding ShowCommand}" Style="{StaticResource HyperlinkStyle2}">
                                        <TextBlock Text="{Binding Device.PresentationAddressAndDriver}" VerticalAlignment="Center" TextWrapping="Wrap" FontWeight="Bold" Foreground="White" Margin="2" />
				                    </Hyperlink>
                                </TextBlock>
                            </WrapPanel>
                        </StackPanel>
                        
                        <WrapPanel>
                            <TextBlock Text="Подключено к: " Width="100" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="Orange" Margin="2" />
                            <Image Source="{Binding Device.Parent.Driver.ImageSource}" Width="16" Margin="2 2 5 2" />
                            <TextBlock>
				                <Hyperlink Command="{Binding ShowParentCommand}" Style="{StaticResource HyperlinkStyle2}">
                                    <TextBlock Text="{Binding Device.Parent.Driver.ShortName}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="White" Margin="2" />
				                </Hyperlink>
                            </TextBlock>
                            <WrapPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Device.Parent}" Value="{x:Null}" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </WrapPanel.Style>
                        </WrapPanel>

                        <WrapPanel Orientation="Horizontal">
                            <TextBlock Text="Зона или Логика: " Width="100" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="Orange" Margin="2 2 5 2" />
                            <TextBlock>
                                <Hyperlink Command="{Binding ShowZoneCommand}" Style="{StaticResource HyperlinkStyle2}">
                                    <TextBlock Text="{Binding PresentationZone}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="White" Margin="2" />
                                </Hyperlink>
                            </TextBlock>
                            <WrapPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PresentationZone}" Value="{x:Null}" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding PresentationZone}" Value="" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </WrapPanel.Style>
                        </WrapPanel>

                        <WrapPanel>
                            <TextBlock Text="План: " Width="100" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="Orange" Margin="2" />
                            <TextBlock>
				                <Hyperlink Command="{Binding ShowOnPlanCommand}" Style="{StaticResource HyperlinkStyle2}">
                                    <TextBlock Text="{Binding PlanName}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="White" Margin="2" />
				                </Hyperlink>
                            </TextBlock>
                            <WrapPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PlanName}" Value="{x:Null}" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding PlanName}" Value="" >
                                            <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </WrapPanel.Style>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Измерения" Visibility="{Binding HasAUParameters, Converter={StaticResource BoolToVisibilityConverter}}">
                <DataGrid ItemsSource="{Binding AUParameterValues}" CanUserSortColumns="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Параметр" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="Значение" Binding="{Binding StringValue}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Управление" DataContext="{Binding DeviceCommandsViewModel}" Visibility="{Binding IsTriStateControl, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Режим" Foreground="White" HorizontalAlignment="Center" Margin="2" />
                        <TextBlock Text="{Binding ControlRegime, Converter={StaticResource DeviceControlRegimeToStringConverter}}" Foreground="White" Margin="2" />
                    </StackPanel>
                    <Grid Grid.Row="1" Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Button Content="Автоматика" Command="{Binding SetAutomaticStateCommand}" Width="200" Height="50" Margin="2" />
                            <Button Content="Ручное" Command="{Binding SetManualStateCommand}" Width="200" Height="50" Margin="2" />
                            <Button Content="Отключение" Command="{Binding SetIgnoreStateCommand}" Width="200" Height="50" Margin="2" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" IsEnabled="{Binding IsControlRegime}">
                            <ItemsControl ItemsSource="{Binding DeviceExecutableCommands}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding StateType, Converter={StaticResource XStateTypeToStringConverter}}"
                                                Command="{Binding ExecuteControlCommand}" Width="200" Height="50" Margin="2" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Управление" DataContext="{Binding DeviceCommandsViewModel}" Visibility="{Binding IsBiStateControl, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Режим" Foreground="White" HorizontalAlignment="Center" Margin="2" />
                        <TextBlock Text="{Binding ControlRegime, Converter={StaticResource DeviceControlRegimeToStringConverter}}" Foreground="White" Margin="2" />
                    </StackPanel>
                    <Grid Grid.Row="2" Background="White">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" Content="Снять отключение" Command="{Binding SetAutomaticStateCommand}" Width="200" Height="50" VerticalAlignment="Top" Margin="2" />
                        <Button Grid.Row="1" Content="Отключение" Command="{Binding SetIgnoreStateCommand}" Width="200" Height="50" VerticalAlignment="Top" Margin="2" />
                        <Button Grid.Row="2" Content="Сбросить" Command="{Binding ResetCommand}" Width="200" Height="50" Margin="2"
                            Visibility="{Binding HasReset, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>