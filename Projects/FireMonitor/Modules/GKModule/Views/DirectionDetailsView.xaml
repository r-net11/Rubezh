<UserControl x:Class="GKModule.Views.DirectionDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:GKModule.Converters"
             xmlns:Views="clr-namespace:GKModule.Views"
             MinWidth="400" MinHeight="400" MaxWidth="600" MaxHeight="600">
    <UserControl.Resources>
        <Converters:DeviceControlRegimeToStringConverter x:Key="DeviceControlRegimeToStringConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="1">
            <TabItem Header="Основные">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Состояние: " FontWeight="Bold" Foreground="Orange" Margin="1, 1, 5, 1" />
                            <TextBlock Text="{Binding DirectionState.StateClass, Converter={StaticResource XStateClassToStringConverter}}" FontWeight="Bold" Foreground="Orange" />
                            <Image Source="{Binding DirectionState.StateClass, Converter={StaticResource XStateClassToIconConverter}}" Width="16" Height="16" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Входных зон: " Foreground="White" />
                            <TextBlock Text="{Binding InputZonesCount}" Foreground="White" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Входных устройств: " Foreground="White" />
                            <TextBlock Text="{Binding InputDevicesCount}" Foreground="White" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Выходных устройств: " Foreground="White" />
                            <TextBlock Text="{Binding OutputDevicesCount}" Foreground="White" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" DataContext="{Binding DirectionViewModel}"
                                Visibility="{Binding ShowTimeLeft, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Задержка: " Foreground="White" />
                            <TextBlock Text="{Binding TimeLeft}" Foreground="White" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Управление">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Режим" Foreground="White" HorizontalAlignment="Center" Margin="2" />
                        <TextBlock Text="{Binding ControlRegime, Converter={StaticResource DeviceControlRegimeToStringConverter}}" Foreground="White" Margin="2" />
                    </StackPanel>
                    <Grid Grid.Row="1" Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <Button Content="Автоматика" Command="{Binding SetAutomaticStateCommand}" Width="200" Margin="2" />
                            <Button Content="Ручное" Command="{Binding SetManualStateCommand}" Width="200" Margin="2" />
                            <Button Content="Отключение" Command="{Binding SetIgnoreStateCommand}" Width="200" Margin="2" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" IsEnabled="{Binding IsControlRegime}">
                            <Button Content="Включить" Command="{Binding TurnOnCommand}" Width="200" Margin="2" />
                            <Button Content="Включить немедленно" Command="{Binding TurnOnNowCommand}" Width="200" Margin="2" />
                            <Button Content="Выключить" Command="{Binding TurnOffCommand}" Width="200" Margin="2" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>