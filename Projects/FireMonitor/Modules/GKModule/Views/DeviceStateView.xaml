<UserControl x:Class="GKModule.Views.DeviceStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:GKModule.Converters"
             xmlns:CommonConverters="clr-namespace:Controls.Converters;assembly=Controls">
    <UserControl.Resources>
        <CommonConverters:StateTypeToIconConverter x:Key="StateTypeToIconConverter" />
        <Converters:XStateTypeConverter x:Key="XStateTypeConverter" />
    </UserControl.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Image Source="{Binding DeviceState.StateType, Converter={StaticResource StateTypeToIconConverter}}" Width="16" Height="16" VerticalAlignment="Center" Margin="2" />
            <TextBlock Text="Состояние" Margin="10, 0, 0, 0"  VerticalAlignment="Center" />
            <TextBlock Text="{Binding DeviceState.StateType, Converter={StaticResource StateTypeToStringConverter}}" VerticalAlignment="Center" Margin="5, 2, 2, 2" />
        </StackPanel>
        <ItemsControl ItemsSource="{Binding DeviceState.States}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding DeviceState.StateType, Converter={StaticResource StateTypeToIconConverter}}" Width="16" Height="16" VerticalAlignment="Center" Margin="2" />
                        <TextBlock Text="{Binding ., Converter={StaticResource XStateTypeConverter}}" VerticalAlignment="Center" Margin="5, 2, 2, 2" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>