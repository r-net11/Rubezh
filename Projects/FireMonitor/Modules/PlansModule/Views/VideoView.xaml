<UserControl x:Class="PlansModule.Views.VideoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             MinWidth="320" MinHeight="350">
    <Grid Loaded="UserControl_Loaded" Margin="10">
        <StackPanel>
            <MediaElement Name="_mediaElement" Source="D:/video1.mp4" Width="300" Volume="0" LoadedBehavior="Manual" />
            <TextBlock />
            <Button Content="Запись" Width="100" />
            <TextBlock />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Grid.Column="1" Content="вверх" Width="100" />
                <Button Grid.Row="1" Grid.Column="0" Content="влево" Width="100" />
                <Button Grid.Row="1" Grid.Column="2" Content="вправо" Width="100" />
                <Button Grid.Row="2" Grid.Column="1" Content="вниз" Width="100" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
