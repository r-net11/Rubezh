<UserControl x:Class="PlansModule.Views.PlanSelectationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:PlansModule.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <StackPanel DataContext="{Binding SelectedDeviceViewModel}" Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding Name}" />
            <Button Content="Показать в дереве" Command="{Binding ShowCommand}" />
        </StackPanel>

        <StackPanel DataContext="{Binding SelectedZoneViewModel}" Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding Name}" />
            <Button Content="Показать зону" Command="{Binding ShowInListCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
