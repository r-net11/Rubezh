<UserControl x:Class="PlansModule.Views.PlanTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             xmlns:Converters="clr-namespace:PlansModule.Converters">
    <UserControl.Resources>
        <Converters:StateToIconConverter x:Key="StateToIconConverter" />
    </UserControl.Resources>
    <DataGrid ItemsSource="{Binding Plans}" SelectedItem="{Binding SelectedPlan}">
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="Название">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="{Binding Level, Converter={StaticResource LevelToMarginConverter}}" VerticalAlignment="Center">
                            <StackPanel Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityConverter}}">
                                <CheckBox Style="{StaticResource TreeExpanderStyle}" IsChecked="{Binding IsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <Image Source="{Binding StateType, Converter={StaticResource StateToIconConverter}}" Width="16" Height="16" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Caption}" Margin="2, 0, 0, 0" />
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>