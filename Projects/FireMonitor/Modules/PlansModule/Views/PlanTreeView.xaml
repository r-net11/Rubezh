<UserControl x:Class="PlansModule.Views.PlanTreeView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:Controls;assembly=Controls" xmlns:Converters="clr-namespace:Controls.Converters;assembly=Controls" xmlns:LacalConverters="clr-namespace:PlansModule.Converters" xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls">
	<UserControl.Resources>
        <LacalConverters:XStateClassToPlanColorConverter x:Key="XStateClassToPlanColorConverter" />
	</UserControl.Resources>

	<tree:TreeList Source="{Binding Plans}" SelectedTreeNode="{Binding SelectedPlan}">
		<tree:TreeList.View>
			<GridView>
				<GridViewColumn Header="Название">
					<GridViewColumn.CellTemplate>
						<DataTemplate>
                            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Background="{Binding StateClass, Converter={StaticResource XStateClassToPlanColorConverter}}">
                                <Image Width="16" Height="16" VerticalAlignment="Center" Source="{Binding StateClass, Converter={StaticResource XStateClassToIconConverter}}" Margin="2" />
                                <Image Width="16" Height="16" VerticalAlignment="Center" Source="/Controls;component/Images/CMap.png" Margin="2" />
                                <TextBlock VerticalAlignment="Center" Text="{Binding Plan.Caption}" ToolTip="{Binding StateClass, Converter={StaticResource XStateClassToStringConverter}}" />
							</StackPanel>
						</DataTemplate>
					</GridViewColumn.CellTemplate>
				</GridViewColumn>
			</GridView>
		</tree:TreeList.View>
	</tree:TreeList>
</UserControl>