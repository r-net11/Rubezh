<UserControl
	x:Class="PlansModule.Views.PlansView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:Views="clr-namespace:PlansModule.Views"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DesignHeight="300"
	d:DesignWidth="300"
	mc:Ignorable="d">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" MinWidth="100" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="{Binding PlanNavigationWidth, Mode=TwoWay}" />
		</Grid.ColumnDefinitions>
		<Border
			Background="{DynamicResource BackgroundBrush}"
			CornerRadius="5"
			Padding="5">
			<ContentPresenter Content="{Binding PlanDesignerViewModel}" />
		</Border>
		<GridSplitter
			Grid.Column="1"
			Width="5"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			Background="Transparent"
			Cursor="SizeWE"
			Visibility="{Binding IsPlanTreeVisible,
		                                   Converter={StaticResource BoolToVisibilityConverter}}" />
		<Grid Grid.Column="2" Visibility="{Binding IsPlanTreeVisible, Converter={StaticResource BoolToVisibilityConverter}}">
			<ContentPresenter Content="{Binding PlanTreeViewModel}" />
		</Grid>
	</Grid>
</UserControl>