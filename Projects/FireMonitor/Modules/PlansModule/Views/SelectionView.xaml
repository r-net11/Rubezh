<UserControl x:Class="PlansModule.Views.SelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Controls:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <StackPanel>
        <StackPanel Visibility="{Binding HasSelectedDevice, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel DataContext="{Binding SelectedDevice}">
                <TextBlock Text="{Binding Name}" />
                <Button Content="Показать в дереве" Command="{Binding ShowCommand}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Visibility="{Binding HasSelectedZone, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel DataContext="{Binding SelectedZone}">
                <TextBlock Text="{Binding Name}" />
                <Button Content="Показать зону" Command="{Binding ShowInListCommand}" />
                <TextBlock Text="{Binding Zone}" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
