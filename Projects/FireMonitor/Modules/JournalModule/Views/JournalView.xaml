<UserControl x:Class="JournalModule.Views.JournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:DataGridProperties="clr-namespace:Controls;assembly=Controls"
             xmlns:Converter="clr-namespace:JournalModule.Converters"
             xmlns:Controls="clr-namespace:Controls;assembly=Controls"
             xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended">
    <UserControl.Resources>
        <Converter:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <Converter:StateTypeToEventNameConverter x:Key="StateTypeToEventNameConverter" />
        <Converter:ObjectToVisibilityConverter x:Key="ObjectToVisibilityConverter" />
    </UserControl.Resources>
    
    <DockPanel>
        <Border DockPanel.Dock="Bottom" CornerRadius="5" BorderThickness="0" Margin="0, 5, 0, 0" Background="{DynamicResource BackgroundBrush}"
                Visibility="{Binding SelectedRecord, Converter={StaticResource ObjectToVisibilityConverter}}">
            <Grid DataContext="{Binding SelectedRecord}" TextElement.Foreground="White">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Класс" HorizontalAlignment="Right" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Пользователь" Visibility="{Binding User, Converter={StaticResource StringToVisibilityConverter}}" HorizontalAlignment="Right" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Время" HorizontalAlignment="Right" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Устройство - Датчик" Visibility="{Binding Device, Converter={StaticResource StringToVisibilityConverter}}" HorizontalAlignment="Right" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Устройство" Visibility="{Binding Panel, Converter={StaticResource StringToVisibilityConverter}}" HorizontalAlignment="Right" Margin="0, 0, 10, 0" />

                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding StateType, Converter={StaticResource StateTypeToEventNameConverter}}" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding User}" Visibility="{Binding User, Converter={StaticResource StringToVisibilityConverter}}" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SystemTime}" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Device}" Visibility="{Binding Device, Converter={StaticResource StringToVisibilityConverter}}" />
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Panel}" Visibility="{Binding Panel, Converter={StaticResource StringToVisibilityConverter}}" />
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Description}" />
                <Controls:XRichTextBox Grid.Row="6" Grid.Column="1" Text="{Binding Detalization, Mode=OneWay}" BorderThickness="0" Background="Transparent" Foreground="White" />
            </Grid>
        </Border>
        <Controls:ScrollDataGrid DataGridProperties:DataGridProperties.HeaderBrush="{DynamicResource AlternativeBackgroundBrush}"
                  ItemsSource="{Binding JournalRecords}" SelectedItem="{Binding SelectedRecord}"
                  EnableRowVirtualization="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Время устройства" Binding="{Binding DeviceTime}" />
                <DataGridTextColumn Header="Системное время" Binding="{Binding SystemTime}" />
                <DataGridTextColumn Header="Зона" Binding="{Binding ZoneName}" />
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" />
                <DataGridTextColumn Header="Устройство - датчик" Binding="{Binding Device}" />
                <DataGridTextColumn Header="Устройство" Binding="{Binding Panel}" />
                <DataGridTextColumn Header="Пользователь" Binding="{Binding User}" />
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Setters>
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Header="Показать на плане" Command="{Binding ShowPlanCommand}" />
                                    <MenuItem Header="Показать в дереве" Command="{Binding ShowTreeCommand}" />
                                    <MenuItem Header="Показать зону" Command="{Binding ShowZoneCommand}" />
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style.Setters>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <Style.Setters>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type DataGridCell}">
                                    <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Background"
                            Value="{Binding StateType, Converter={StaticResource StateToColorConverter}}" />
                    </Style.Setters>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PressedBackgroundBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
        </Controls:ScrollDataGrid>
    </DockPanel>
</UserControl>