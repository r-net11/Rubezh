<UserControl x:Class="JournalModule.Views.FilterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        MinHeight="300" MinWidth="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Временной диапазон" Margin="2" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="С" Margin="2" />
                <TextBlock Grid.Row="0" Grid.Column="2" Text="По" Margin="2" />
                <DatePicker SelectedDate="{Binding StartDate}" Grid.Row="1" Grid.Column="0" Margin="2" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="2" />
                <DatePicker SelectedDate="{Binding EndDate}" Grid.Row="1" Grid.Column="2" Margin="2" />
                <TextBox Grid.Row="1" Grid.Column="3" Margin="2" />
            </Grid>

            <CheckBox Content="Использовать дату регистрации события в системе(иначе дату возникновения)" IsChecked="{Binding UseSystemDate}" Margin="2" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Классы событий" HorizontalAlignment="Center" Margin="2" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="События" HorizontalAlignment="Center" Margin="2" />

            <ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding JournalTypes}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsEnabled}" IsThreeState="True" Margin="2" />
                            <TextBlock Text="{Binding Name}" Margin="2" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ListBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding JournalEvents}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsEnabled}" Margin="2" />
                            <TextBlock Text="{Binding Name}" Margin="2" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Очистить" Command="{Binding ClearCommand}" HorizontalAlignment="Left" Width="100" Margin="2" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Ok" Command="{Binding SaveCommand}" Width="100" Margin="2" />
                <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="2" />
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
