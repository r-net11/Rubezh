<UserControl x:Class="JournalModule.Views.ArchiveFilterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
        MinHeight="300" MinWidth="500">
    <DockPanel Margin="10">

        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="Временной диапазон" Margin="2" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="С" Margin="2" />
                <TextBlock Grid.Row="0" Grid.Column="2" Text="До" Margin="2" />
                <DatePicker Grid.Row="1" Grid.Column="0" SelectedDate="{Binding StartDate}" Margin="2" />
                <extToolkit:TimePicker Grid.Row="1" Grid.Column="1" Value="{Binding StartTime}" Margin="2" />
                <DatePicker Grid.Row="1" Grid.Column="2" SelectedDate="{Binding EndDate}" Margin="2" />
                <extToolkit:TimePicker Grid.Row="1" Grid.Column="3" Value="{Binding EndTime}" Margin="2" />
            </Grid>

            <CheckBox Content="Использовать дату регистрации события в системе (иначе дату возникновения)" 
                      IsChecked="{Binding UseSystemDate}" Margin="2" />
        </StackPanel>

        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Очистить" Command="{Binding ClearCommand}" 
                    HorizontalAlignment="Left" Width="100" Margin="2" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Ok" Command="{Binding SaveCommand}" Width="100" Margin="2" IsDefault="True" />
                <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="2" />
            </StackPanel>
        </Grid>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Классы событий" HorizontalAlignment="Center" Margin="2" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="События" HorizontalAlignment="Center" Margin="2" />

            <ListBox Grid.Row="1" Grid.Column="0" 
                     ItemsSource="{Binding JournalTypes, 
                                           Converter={StaticResource CollectionViewFactoryConverter}, 
                                           ConverterParameter=Id}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel>
                            <CheckBox IsChecked="{Binding IsEnable}" IsThreeState="True" Margin="2"
                                      Checked="CheckBox_Checked" Unchecked="CheckBox_Checked" />
                            <TextBlock Text="{Binding Id, Converter={StaticResource IdToClassNameConverter}}" Margin="2" />
                        </WrapPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ListBox Grid.Row="1" Grid.Column="1" 
                     ItemsSource="{Binding JournalEvents, 
                                           Converter={StaticResource CollectionViewFactoryConverter}, 
                                           ConverterParameter=ClassId}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel>
                            <CheckBox IsChecked="{Binding IsEnable}" Margin="2"
                                      Checked="CheckBox_Checked_1" Unchecked="CheckBox_Checked_1"/>
                            <TextBlock Text="{Binding Name}" Margin="2" />
                        </WrapPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>

    </DockPanel>
</UserControl>
