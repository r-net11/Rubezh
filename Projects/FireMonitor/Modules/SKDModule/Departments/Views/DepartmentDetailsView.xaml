<UserControl x:Class="SKDModule.Views.DepartmentDetailsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
			 MinWidth="400" MinHeight="400">
	<TabControl>
		<TabItem Header="Основные">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Label Grid.Row="0" Grid.Column="0" Content="Наименование" HorizontalAlignment="Right" />
				<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="2" />
				
				<Label Grid.Row="1" Grid.Column="0" Content="Примечание" HorizontalAlignment="Right" />
				<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="2" />

				<Label Grid.Row="2" Grid.Column="0" Content="Руководитель" HorizontalAlignment="Right" VerticalAlignment="Center" />
				<StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" >
					<TextBlock VerticalAlignment="Center">
							<Hyperlink Command="{Binding ChiefViewModel.SelectChiefCommand}" Style="{StaticResource HyperlinkStyle}">
								<TextBlock Text="{Binding ChiefViewModel.Chief.Name}" TextDecorations="Underline" />
								<TextBlock>
									<TextBlock.Style>
										<Style>
											<Style.Triggers>
												<DataTrigger Binding="{Binding ChiefViewModel.HasChief}" Value="false">
													<Setter Property="TextBlock.Text" Value="Нажмите для выбора руководителя" />
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
							</Hyperlink>
						</TextBlock>
				</StackPanel>

				<Label Grid.Row="3" Grid.Column="0" Content="Руководитель отдела кадров" HorizontalAlignment="Right" VerticalAlignment="Center" />
				<StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" >
					<TextBlock VerticalAlignment="Center">
							<Hyperlink Command="{Binding HRChiefViewModel.SelectChiefCommand}" Style="{StaticResource HyperlinkStyle}">
								<TextBlock Text="{Binding HRChiefViewModel.Chief.Name}" TextDecorations="Underline" />
								<TextBlock>
									<TextBlock.Style>
										<Style>
											<Style.Triggers>
												<DataTrigger Binding="{Binding HRChiefViewModel.HasChief}" Value="false">
													<Setter Property="TextBlock.Text" Value="Нажмите для выбора руководителя" />
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
							</Hyperlink>
						</TextBlock>
				</StackPanel>

				<Label Grid.Row="4" Grid.Column="0" Content="Телефон" HorizontalAlignment="Right" />
				<TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Margin="2" />
			</Grid>
		</TabItem>
		<TabItem Header="Иконка">
			<Controls:PhotoSelectation Data="{Binding PhotoData}" />	
		</TabItem>
	</TabControl>
</UserControl>