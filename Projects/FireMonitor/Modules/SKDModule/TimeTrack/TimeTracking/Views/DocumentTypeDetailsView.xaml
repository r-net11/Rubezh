<UserControl
	x:Class="SKDModule.Views.DocumentTypeDetailsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="clr-namespace:Controls.Converters;assembly=Controls"
	xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
	xmlns:behaviours="clr-namespace:Controls.Behaviours;assembly=Controls"
	xmlns:langCommon="clr-namespace:Localization.SKD.Common;assembly=Localization"
	MinWidth="400" MinHeight="200">
	<UserControl.Resources>
		<converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
	</UserControl.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock
			Text="{x:Static langCommon:CommonResources.Name}"
			VerticalAlignment="Center"
			Foreground="White"
			Margin="5 0"/>
		<TextBox
			Grid.Column="1"
			MaxLength="4000"
			Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
			IsEnabled="{Binding IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
			Margin="10 5 0 5" />
		<TextBlock
			Grid.Row="1"
			Text="{x:Static langCommon:CommonResources.LiteralCode}"
			VerticalAlignment="Center"
			Foreground="White"
			Margin="5 0" />
		<TextBox
			Grid.Row="1"
			Grid.Column="1"
			MaxLength="50"
			Text="{Binding ShortName, UpdateSourceTrigger=PropertyChanged}"
			IsEnabled="{Binding IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
			Margin="10 5 0 5" />
		<TextBlock
			Grid.Row="2"
			Text="{x:Static langCommon:CommonResources.NumericCode}"
			VerticalAlignment="Center"
			Foreground="White"
			Margin="5 0" />
		<TextBox
			Grid.Row="2"
			Grid.Column="1"
			MaxLength="9"
			Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"
			IsEnabled="{Binding IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
			Margin="10 5 0 5">
			<i:Interaction.Behaviors>
				<behaviours:NumericValidator />
			</i:Interaction.Behaviors>
		</TextBox>
		<TextBlock
			Grid.Row="3"
			Text="{x:Static langCommon:CommonResources.Type}"
			VerticalAlignment="Center"
			Foreground="White"
			Margin="5 0" />
		<ComboBox
			Grid.Row="3"
			Grid.Column="1"
			ItemsSource="{Binding AvailableDocumentTypes}"
			SelectedItem="{Binding SelectedDocumentType}"
			Margin="10 5 0 5">
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" VerticalAlignment="Center" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>
	</Grid>
</UserControl>