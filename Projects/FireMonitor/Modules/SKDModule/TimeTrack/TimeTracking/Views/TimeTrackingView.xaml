<UserControl x:Class="SKDModule.Views.TimeTrackingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:SKDModule.Converters">
    <UserControl.Resources>
        <Converters:TimeSpanToStringConverter x:Key="TimeSpanToStringConverter" />
    </UserControl.Resources>

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
            <RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Orientation="Horizontal">
			<ToolBarButton Command="{Binding ShowFilterCommand}" ImageSource="/Controls;component/Images/Filter.png" ToolTip="Задать фильтр" />
			<ToolBarButton Command="{Binding ShowSettingsCommand}" ImageSource="/Controls;component/Images/Edit.png" ToolTip="Настройки" />
			<ToolBarButton Command="{Binding RefreshCommand}" ImageSource="/Controls;component/Images/Refresh.png" ToolTip="Обновить" />
			<ToolBarButton Command="{Binding PrintCommand}" ImageSource="/Controls;component/Images/Print.png" ToolTip="Печать" />
		</StackPanel>
	    <DataGrid x:Name="grid" Grid.Row="1" ItemsSource="{Binding TimeTracks}" SelectedItem="{Binding SelectedTimeTrack}" FrozenColumnCount="4" RowHeight="60" Margin="0 4 0 0">
			<DataGrid.Columns>
	            <DataGridTemplateColumn Header="ФИО">
	                <DataGridTemplateColumn.CellTemplate>
	                    <DataTemplate>
	                        <StackPanel Orientation="Horizontal">
	                            <TextBlock Text="{Binding ShortEmployee.LastName}" Margin="4 0" />
	                            <TextBlock Text="{Binding ShortEmployee.FirstName}" Margin="4 0" />
	                            <TextBlock Text="{Binding ShortEmployee.SecondName}" Margin="4 0" />
	                        </StackPanel>
	                    </DataTemplate>
	                </DataGridTemplateColumn.CellTemplate>
	            </DataGridTemplateColumn>
	            <DataGridTextColumn Binding="{Binding ShortEmployee.DepartmentName}" Header="Отдел" />
	            <DataGridTextColumn Binding="{Binding ShortEmployee.PositionName}" Header="Должность" />
	            <DataGridTemplateColumn Header="Тип">
	                <DataGridTemplateColumn.CellTemplate>
	                    <DataTemplate>
	                        <StackPanel>
	                            <TextBlock Text="Присутствие" Margin="4 0" />
	                            <TextBlock Text="Пропущено" Margin="4 0" />
	                            <TextBlock Text="По графику" Margin="4 0" />
	                            <TextBlock Text="Переработка" Margin="4 0" />
	                        </StackPanel>
	                    </DataTemplate>
	                </DataGridTemplateColumn.CellTemplate>
	            </DataGridTemplateColumn>
	            <DataGridTemplateColumn Header="Итого">
	                <DataGridTemplateColumn.CellTemplate>
	                    <DataTemplate>
	                        <StackPanel>
                                <TextBlock Text="{Binding Total, Converter={StaticResource TimeSpanToStringConverter}}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding TotalMissed, Converter={StaticResource TimeSpanToStringConverter}}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding TotalInSchedule, Converter={StaticResource TimeSpanToStringConverter}}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding TotalOutSchedule, Converter={StaticResource TimeSpanToStringConverter}}" VerticalAlignment="Center" />
	                        </StackPanel>
	                    </DataTemplate>
	                </DataGridTemplateColumn.CellTemplate>
	            </DataGridTemplateColumn>
	        </DataGrid.Columns>
            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <Style.Setters>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type DataGridCell}">
                                    <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style.Setters>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PressedBackgroundBrush}" />
                            <Setter Property="Foreground" Value="Black" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>
        <Grid Grid.Row="2" DataContext="{Binding SelectedTimeTrack}">
            <DataGrid ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Документ" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
	</Grid>
</UserControl>