<UserControl x:Class="SKDModule.Views.ReportSettingsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended" MinWidth="650" MinHeight="400">

	<Border Margin="2" Background="White" CornerRadius="5">
		<Grid Margin="5">
			<Grid.Resources>
				<Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="{x:Type TextBlock}">
					<Setter Property="HorizontalAlignment" Value="Right" />
					<Setter Property="Margin" Value="2" />
					<Setter Property="VerticalAlignment" Value="Center" />
				</Style>
				<Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type TextBox}">
					<Setter Property="Background" Value="LightGray" />
					<Setter Property="Margin" Value="2" />
					<Setter Property="VerticalAlignment" Value="Center" />
				</Style>
			</Grid.Resources>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<TextBlock Grid.Row="0" Grid.Column="0" Text="Ответственное лицо" />
			<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FillName, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="1" Grid.Column="0" Text="Должность ответственного лица" />
			<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FillPosition, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="2" Grid.Column="0" Text="Руководитель структурного подразделения" />
			<TextBox Grid.Row="2" Grid.Column="1" Text="{Binding LeadName, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="3" Grid.Column="0" Text="Должность руководителя структурного подразделения" />
			<TextBox Grid.Row="3" Grid.Column="1" Text="{Binding LeadPosition, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="4" Grid.Column="0" Text="Руководитель кадровой службы" />
			<TextBox Grid.Row="4" Grid.Column="1" Text="{Binding HRName, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="5" Grid.Column="0" Text="Должность руководителя кадровой службы" />
			<TextBox Grid.Row="5" Grid.Column="1" Text="{Binding HRPosition, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="6" Grid.Column="0" Text="Дата формирования" VerticalAlignment="Top" />
			<Calendar Grid.Row="6" Grid.Column="1" Margin="2" HorizontalAlignment="Left" DisplayDate="{Binding DateTime, UpdateSourceTrigger=PropertyChanged}" SelectedDate="{Binding DateTime, UpdateSourceTrigger=PropertyChanged}" SelectionMode="SingleDate" />

			<TextBlock Grid.Row="8" Grid.Column="0" Text="Число записей на странице" />
			<extToolkit:IntegerUpDown Grid.Row="9" Grid.Column="1" Maximum="10" Minimum="0" Value="{Binding RecordsPerPage, UpdateSourceTrigger=PropertyChanged}" />
		</Grid>
	</Border>
</UserControl>