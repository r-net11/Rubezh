<UserControl
	x:Class="SKDModule.Views.WarningIntersectionIntervalDialogWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="300"
	d:DesignWidth="300"
	Width="450"
	Height="200">
    <Grid Language="ru-RU">
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Border Margin="5" CornerRadius="5" Background="{DynamicResource BackgroundBrush}">
			<StackPanel Margin="10">
				<TextBlock Foreground="White" Text="Границы редактируемого интервала:" />
				<TextBlock Foreground="White">
					<Run Text="с" />
					<Run Text="{Binding CurrentDayTimeTrackPart.EnterDateTime}" />
					<Run Text="по" />
					<Run Text="{Binding CurrentDayTimeTrackPart.ExitDateTime}" />
					<Run Text="(зона" />
					<Run>
						<Run.Text>
							<MultiBinding StringFormat='"{0}")'>
								<Binding Path="CurrentDayTimeTrackPart.TimeTrackZone.Name" />
							</MultiBinding>
						</Run.Text>
					</Run>
				</TextBlock>
				<TextBlock Foreground="White" Text="пересекаются с границами других интервалов:" />
				<ItemsControl ItemsSource="{Binding IntersectionPartsCollection}">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<TextBlock Foreground="White">
								<Run Text="с" />
								<Run Text="{Binding EnterDateTime}" />
								<Run Text="по" />
								<Run Text="{Binding ExitDateTime}" />
								<Run Text=' (зона' />
								<Run>
									<Run.Text>
										<MultiBinding StringFormat='"{0}")'>
											<Binding Path="TimeTrackZone.Name" />
										</MultiBinding>
									</Run.Text>
								</Run>
							</TextBlock>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
				<TextBlock Foreground="White" Margin="0 15" Text="Для добавления интервала укажите другие границы." />
			</StackPanel>
		</Border>
		<Button
			Grid.Row="1"
			Content="OK"
			VerticalAlignment="Center"
			HorizontalAlignment="Right"
			Command="{Binding OkCommand}"
			Width="100"
			Margin="5"/>
    </Grid>
</UserControl>
