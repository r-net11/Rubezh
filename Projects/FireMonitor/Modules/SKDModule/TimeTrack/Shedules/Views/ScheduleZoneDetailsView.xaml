<UserControl
	x:Class="SKDModule.Views.ScheduleZoneDetailsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:csm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
	xmlns:langCommon="clr-namespace:Localization.SKD.Common;assembly=Localization"
	Width="400"
	Height="400">
	<UserControl.Resources>
		<CollectionViewSource x:Key="cvsZones" Source="{Binding Zones, IsAsync=True}">
			<CollectionViewSource.SortDescriptions>
				<csm:SortDescription PropertyName="Name" />
			</CollectionViewSource.SortDescriptions>
		</CollectionViewSource>
	</UserControl.Resources>
	<Grid
		ScrollViewer.IsDeferredScrollingEnabled="True"
		VirtualizingStackPanel.VirtualizationMode="Recycling"
		Margin="2">
		<DataGrid
			CanUserSortColumns="True"
			Focusable="True"
			ItemsSource="{Binding Source={StaticResource cvsZones}}"
			SelectedItem="{Binding SelectedZone}">
			<DataGrid.Columns>
				<DataGridTextColumn
					Binding="{Binding No}"
					Header="№" />
				<DataGridTemplateColumn
					Width="250"
					Header="{x:Static langCommon:CommonResources.Zone}"
					SortMemberPath="Name">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Image
									Margin="1 0 0 0"
									VerticalAlignment="Center"
									Source="/Controls;component/Images/Zone.png" />
								<TextBlock
									Margin="2"
									VerticalAlignment="Center"
									Text="{Binding Name}" />
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTextColumn
					Binding="{Binding Description}"
					Header="{x:Static langCommon:CommonResources.Note}"
					Width="*" />
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>