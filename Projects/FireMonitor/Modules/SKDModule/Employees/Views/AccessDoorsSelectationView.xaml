<UserControl x:Class="SKDModule.Views.AccessDoorsSelectationView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
			 xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="200" />
		</Grid.RowDefinitions>
		<Grid Grid.Row="0" Margin="2" ScrollViewer.IsDeferredScrollingEnabled="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
			<DataGrid ItemsSource="{Binding Doors}" SelectedItem="{Binding SelectedDoor}" Margin="2">
				<DataGrid.Columns>
					<DataGridTemplateColumn Header="Точка доступа" Width="*">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image VerticalAlignment="Center" Source="/Controls;component/Images/zone.png" Margin="1 0 0 0" />
									<CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="2" />
									<TextBlock Text="{Binding Door.Name}" Width="200" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="2" />
								</StackPanel>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
				</DataGrid.Columns>
			</DataGrid>
		</Grid>
		<Grid Grid.Row="1" Margin="2" DataContext="{Binding SelectedDoor}">
			<StackPanel IsEnabled="{Binding IsChecked}">
				<CheckBox Margin="2" Content="Защита от передачи пропусков(Antipassback)" Foreground="White" IsChecked="{Binding IsAntiPassback}" />
				<CheckBox Margin="2" Content="Доступ с комиссионированием" Foreground="White" IsChecked="{Binding IsComission}" />
				<StackPanel Orientation="Horizontal">
					<TextBlock Width="200" Margin="2" Foreground="White" Text="Временной критерий для входа" />
					<ComboBox Margin="2" ItemsSource="{Binding TimeCreterias}" SelectedItem="{Binding SelectedEnterTimeCreteria}">
						<ComboBox.ItemTemplate>
							<DataTemplate>
								<TextBlock Margin="2" Text="{Binding Description}" />
							</DataTemplate>
						</ComboBox.ItemTemplate>
					</ComboBox>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<TextBlock Width="200" Margin="2" Text="" />
					<ComboBox Margin="2" ItemsSource="{Binding EnterTimeTypes}" SelectedItem="{Binding SelectedEnterTimeType}">
						<ComboBox.ItemTemplate>
							<DataTemplate>
								<TextBlock Margin="2" Text="{Binding Name}" />
							</DataTemplate>
						</ComboBox.ItemTemplate>
					</ComboBox>
				</StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Width="200" Margin="2" Foreground="White" Text="Временной критерий для выхода" />
                    <ComboBox Margin="2" ItemsSource="{Binding TimeCreterias}" SelectedItem="{Binding SelectedExitTimeCreteria}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Margin="2" Text="{Binding Description}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Width="200" Margin="2" Text="" />
                    <ComboBox Margin="2" ItemsSource="{Binding ExitTimeTypes}" SelectedItem="{Binding SelectedExitTimeType}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Margin="2" Text="{Binding Name}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
		</Grid>
	</Grid>
</UserControl>