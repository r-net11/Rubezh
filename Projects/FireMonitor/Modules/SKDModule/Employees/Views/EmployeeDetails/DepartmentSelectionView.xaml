<UserControl
	x:Class="SKDModule.Views.DepartmentSelectionView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls"
    xmlns:Controls="clr-namespace:Controls;assembly=Controls"
	xmlns:viewModel="clr-namespace:SKDModule.ViewModels"
	xmlns:listViewLayoutManager="clr-namespace:Controls.ListViewLayoutManager;assembly=Controls"
	MinWidth="500" MinHeight="500">
	<UserControl.Resources>
		<viewModel:DepartmentSelectionItemViewModelNameComparer x:Key="DepartmentSelectionItemViewModelNameComparer"/>
	</UserControl.Resources>
	<Grid>
        <Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<!-- Дерево подразделений -->
		<tree:TreeList
			Grid.Row="0"
			Focusable="True"
			Source="{Binding RootDepartments}"
			SelectedItem="{Binding SelectedDepartment}"
			listViewLayoutManager:ListViewLayoutManager.Enabled="True"
			tree:SortBehavior.CanUserSortColumns="True">
            <tree:TreeList.Resources>
                <InputBindingCollection x:Key="inputCollection" x:Shared="False">
                    <MouseBinding Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType={x:Type tree:TreeList}}}" MouseAction="LeftDoubleClick" />
                </InputBindingCollection>
            </tree:TreeList.Resources>
            <tree:TreeList.ItemContainerStyle>
                <Style BasedOn="{StaticResource {x:Type tree:TreeListItem}}" TargetType="{x:Type tree:TreeListItem}">
                    <Setter Property="Controls:Attach.InputBindings" Value="{StaticResource inputCollection}" />
                </Style>
            </tree:TreeList.ItemContainerStyle>
            <tree:TreeList.View>
				<GridView>
					<GridViewColumn
						Header="Название"
						listViewLayoutManager:ProportionalColumn.Width="1"
						tree:SortBehavior.SortDirection="Ascending"
						tree:SortBehavior.SortComparer="{StaticResource DepartmentSelectionItemViewModelNameComparer}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image Width="16" Source="/Controls;component/Images/Department.png" VerticalAlignment="Center" Margin="1 0 0 0" />
									<TextBlock Text="{Binding Department.Name}" VerticalAlignment="Center" Margin="2" />
								</StackPanel>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</tree:TreeList.View>
		</tree:TreeList>
		<!-- Панель настроек, применяемых для сотрудника/посетителя -->
		<StackPanel
			Grid.Row="1"
			Visibility="{Binding ShowApplyToEmployeeSettings, Converter={StaticResource BoolToVisibilityConverter}}">
			<TextBlock
				Text="Применить для сотрудника настройки подразделения по умолчанию:"
				Foreground="{DynamicResource ForegroundColorLight}"
				TextWrapping="Wrap"
				Margin="2"/>
			<CheckBox
				IsChecked="{Binding NeedApplyAccessTemplateToEmployee}"
				Margin="2">
				<TextBlock
					Text="Шаблон доступа"
					Foreground="{DynamicResource ForegroundColorLight}"
					Margin="2"/>
			</CheckBox>
			<CheckBox
				Visibility="{Binding IsEmployee, Converter={StaticResource BoolToVisibilityConverter}}"
				IsChecked="{Binding NeedApplyScheduleToEmployee}"
				Margin="2">
				<TextBlock
					Text="График работы"
					Foreground="{DynamicResource ForegroundColorLight}"
					Margin="2"/>
			</CheckBox>
		</StackPanel>
        <!-- Инструментальная панель -->
		<StackPanel
			Grid.Row="2"
			Orientation="Horizontal"
			HorizontalAlignment="Right"
			Margin="0 5">
            <Button Content="Открепить от подразделения" Command="{Binding ClearCommand}" Width="205" Margin="2" />
            <Button Content="Добавить новое подразделение" Command="{Binding AddCommand}" Width="205" Margin="2" />
        </StackPanel>
	</Grid>
</UserControl>