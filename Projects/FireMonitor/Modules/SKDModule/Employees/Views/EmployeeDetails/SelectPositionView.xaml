<UserControl x:Class="SKDModule.Views.SelectPositionView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 MinWidth="500" MinHeight="500">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="0">
			<DataGrid ItemsSource="{Binding Positions}">
				<DataGrid.Columns>
					<DataGridTemplateColumn Header="Название" Width="*">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<RadioButton GroupName="Positions" VerticalAlignment="Center" Margin="2" 
										Command="{Binding SelectCommand}" IsChecked="{Binding IsChecked}" />
									<Image Width="16" Source="/Controls;component/Images/Position.png" VerticalAlignment="Center" Margin="1 0 0 0" />
									<TextBlock Text="{Binding Name}" />
								</StackPanel>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
	                <DataGridTemplateColumn Header="Примечание" Width="*">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Description}" VerticalAlignment="Center" Margin="2" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
				</DataGrid.Columns>
			</DataGrid>
		</ScrollViewer>
        <Button Grid.Row="1" Content="Добавить новую должность" Command="{Binding AddCommand}" HorizontalAlignment="Right" Width="205" Margin="0 5 0 0" />
	</Grid>
</UserControl>