<UserControl x:Class="SKDModule.Views.SelectScheduleView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
			 MinWidth="500" MinHeight="300">

    <TabControl>
        <TabItem Header="Основные">
            <Grid>
		        <Grid.ColumnDefinitions>
			        <ColumnDefinition Width="*" />
			        <ColumnDefinition Width="*" />
		        </Grid.ColumnDefinitions>
		        <StackPanel Grid.Column="0">
			        <TextBlock Text="Начало действия графика" Foreground="White" HorizontalAlignment="Center" />
			        <Calendar SelectionMode="SingleDate" 
				          SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" 
				          DisplayDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" />
		        </StackPanel>
		
		        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
			        <DataGrid ItemsSource="{Binding Schedules}" >
				        <DataGrid.Columns>
					        <DataGridTemplateColumn Header="Название" Width="*">
						        <DataGridTemplateColumn.CellTemplate>
							        <DataTemplate>
								        <RadioButton GroupName="Schedules" VerticalAlignment="Center" Margin="2"
									        Content="{Binding Name}" IsChecked="{Binding IsChecked}" Command="{Binding SelectCommand}"/>
							        </DataTemplate>
						        </DataGridTemplateColumn.CellTemplate>
					        </DataGridTemplateColumn>
				        </DataGrid.Columns>
			        </DataGrid>
		        </ScrollViewer>  
	        </Grid>
        </TabItem>
    </TabControl>
</UserControl>