<UserControl x:Class="SKDModule.Views.SelectDepartmentView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls"
			 MinWidth="300" MinHeight="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<tree:TreeList Grid.Row="0" Focusable="True" Source="{Binding RootDepartments}" SelectedItem="{Binding HighlightedDepartment}">
			<tree:TreeList.View>
				<GridView>
					<GridViewColumn Header="Название" Width="150">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<RadioButton GroupName="Departments" VerticalAlignment="Center" 
												 IsChecked="{Binding IsChecked, Mode=OneWay}" Command="{Binding SelectCommand}" />
                                    <Image Width="16" Source="/Controls;component/Images/Department.png" VerticalAlignment="Center" Margin="1 0 0 0" />
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="2" />
								</StackPanel>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					<GridViewColumn Header="Примечание">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Description}" VerticalAlignment="Center" Margin="2" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</tree:TreeList.View>
		</tree:TreeList>
		<Button Grid.Row="1" HorizontalAlignment="Right" Content="Добавить новый отдел" Width="200"
				Command="{Binding AddCommand}" />
	</Grid>
</UserControl>