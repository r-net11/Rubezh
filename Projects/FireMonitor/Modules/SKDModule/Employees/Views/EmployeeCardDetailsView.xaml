<UserControl x:Class="SKDModule.Views.EmployeeCardDetailsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Converters="clr-namespace:SKDModule.Converters"
			 xmlns:Views="clr-namespace:SKDModule.Views"
			 MinWidth="600" MinHeight="500">

	<TabControl>
		<TabItem Header="Основные">
			<StackPanel>
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="200" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Top" Style="{DynamicResource TextBlockDetailsStyle}" Text="Тип пропуска" Margin="2 4"
							   Visibility="{Binding CanChangeCardType, Converter={StaticResource BoolToVisibilityConverter}}" />
					<ComboBox Grid.Row="0" Grid.Column="1" Margin="2"
							  ItemsSource="{Binding GKCardTypes}" SelectedItem="{Binding SelectedGKCardType}" Visibility="{Binding CanChangeCardType, Converter={StaticResource BoolToVisibilityConverter}}" >
						<ComboBox.ItemTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding ., Converter={StaticResource EnumToDescriptionConverter}}" />
							</DataTemplate>
						</ComboBox.ItemTemplate>
					</ComboBox>

					<TextBlock Grid.Row="1" Grid.Column="0" Margin="2" HorizontalAlignment="Right" Style="{DynamicResource TextBlockDetailsStyle}" Text="Конец действия" />
					<Calendar Grid.Row="1" Grid.Column="1"  Margin="2" HorizontalAlignment="Left" VerticalAlignment="Top" BorderBrush="#66ff9829" BorderThickness="2" IsTodayHighlighted="False" SelectionMode="SingleDate" 
							  DisplayDate="{Binding EndDate}" SelectedDate="{Binding EndDate}"  />


					<TextBlock Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource TextBlockDetailsStyle}" Text="Уровень доступа" Margin="2" />
					<TextBox Grid.Row="2" Grid.Column="1" Text="{Binding GKLevel}" Margin="2" />

					<TextBlock Grid.Row="3" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource TextBlockDetailsStyle}" Text="График доступа" Margin="2" />
                    <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding GKSchedules}" SelectedItem="{Binding SelectedGKSchedule}" Margin="2">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding No}" Margin="2" />
                                    <TextBlock Text="{Binding Name}" Margin="2" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
				</Grid>

				<Grid IsEnabled="{Binding IsNewCard}">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="200" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<TextBlock Grid.Row="0" Grid.Column="0" Text="Номер" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource TextBlockDetailsStyle}" Margin="2" />
					<TextBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" Width="100" Margin="2" 
							 IsEnabled="{Binding UseStopList, Converter={StaticResource InverseBooleanConverter}}" Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}" />

					<CheckBox Grid.Row="1" Grid.Column="0" Content="Из списка деактивированных" IsChecked="{Binding UseStopList}" Foreground="White" VerticalAlignment="Center" Margin="2" />
					<ComboBox Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Width="100" Margin="2"
							  IsEnabled="{Binding UseStopList}" ItemsSource="{Binding StopListCards}" SelectedItem="{Binding SelectedStopListCard}" >
						<ComboBox.ItemTemplate>
							<DataTemplate>
								<TextBlock Margin="1" Text="{Binding Number}" />
							</DataTemplate>
						</ComboBox.ItemTemplate>
					</ComboBox>

					<CheckBox Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Content="От контрольного считывателя" Foreground="White" IsChecked="{Binding UseReader}" Margin="2" />
					<TextBlock Grid.Row="2" Grid.Column="1" TextWrapping="Wrap" VerticalAlignment="Center">
						<Hyperlink Command="{Binding ChangeReaderCommand}" Style="{StaticResource HyperlinkStyle}" IsEnabled="{Binding IsNewCard}">
							<TextBlock Text="{Binding ReaderName}" />
						</Hyperlink>
					</TextBlock>

					<Button Grid.Row="3" Grid.Column="1" Command="{Binding ShowUSBCardReaderCommand}" Content="От USB-считывателя" HorizontalAlignment="Left" Width="150" Margin="2" />
				</Grid>
			</StackPanel>
		</TabItem>
        <TabItem Header="Контроллеры" Visibility="{Binding CanSelectGKControllers, Converter={StaticResource BoolToVisibilityConverter}}">
            <DataGrid ItemsSource="{Binding AvailableGKControllers}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="*" Header="Контроллер">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="{Binding Device.PresentationName}" Margin="2" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </TabItem>
        <TabItem Header="Точки доступа">
			<Views:AccessDoorsSelectationView DataContext="{Binding AccessDoorsSelectationViewModel}" />
		</TabItem>
		<TabItem Header="Шаблон доступа">
			<DataGrid ItemsSource="{Binding AvailableAccessTemplates}" SelectedItem="{Binding SelectedAccessTemplate}">
				<DataGrid.Columns>
					<DataGridTemplateColumn Width="*" Header="Шаблон доступа">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image Margin="1 0 0 0" VerticalAlignment="Center" Source="/Controls;component/Images/Zone.png" />
									<TextBlock Margin="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding Name}" />
								</StackPanel>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
				</DataGrid.Columns>
			</DataGrid>
		</TabItem>
	</TabControl>
</UserControl>