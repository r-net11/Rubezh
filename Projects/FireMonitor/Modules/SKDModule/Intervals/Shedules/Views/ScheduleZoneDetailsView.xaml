<UserControl x:Class="SKDModule.Views.ScheduleZoneDetailsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:Controls;assembly=Controls" xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended" xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls" Width="400" Height="400">

	<TabControl>
		<TabItem Header="Основные">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="*" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Grid Grid.Row="0" Margin="2" ScrollViewer.IsDeferredScrollingEnabled="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <DataGrid Name="_zonesDataGrid" Focusable="True" ItemsSource="{Binding RootZones}" SelectedItem="{Binding SelectedZone}" Width="100">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="250" Header="Зона">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Image Margin="1 0 0 0" VerticalAlignment="Center" Source="/Controls;component/Images/zone.png" />
                                            <TextBlock Width="200" Margin="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding Zone.Name}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Binding="{Binding Zone.Description}" Header="Примечание" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
				</Grid>
				<Grid Grid.Row="1" Margin="2">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<TextBlock Grid.Row="1" Grid.Column="0" Margin="2" Foreground="White" Text="Контроль нарушений дисциплины в течение рабочего дня" />
					<CheckBox Grid.Row="1" Grid.Column="1" Margin="2" IsChecked="{Binding IsControl, UpdateSourceTrigger=PropertyChanged}" />
				</Grid>
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>