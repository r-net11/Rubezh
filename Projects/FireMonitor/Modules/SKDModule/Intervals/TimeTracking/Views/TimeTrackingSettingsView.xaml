<UserControl x:Class="SKDModule.Views.TimeTrackingSettingsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" MinWidth="400" MinHeight="400">
	<UserControl.Resources>
		<DataTemplate x:Key="EnumDescription">
			<TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
		</DataTemplate>
	</UserControl.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Label Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Content="Период отчета" />
		<Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Content="Дата начала" />
		<Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" Content="Дата конца" />

        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Periods}" ItemTemplate="{StaticResource EnumDescription}" SelectedItem="{Binding Period}" Margin="2" />
		<Calendar Grid.Row="1" Grid.Column="1" DisplayDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsFreePeriod}" SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" SelectionMode="SingleDate" />
		<Calendar Grid.Row="2" Grid.Column="1" DisplayDate="{Binding EndDate, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsFreePeriod}" SelectedDate="{Binding EndDate, UpdateSourceTrigger=PropertyChanged}" SelectionMode="SingleDate" />
	</Grid>
</UserControl>