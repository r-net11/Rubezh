<UserControl x:Class="SKDModule.Views.JournalExportDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Экспорт сведений картотеки"  />
        <Label Grid.Row="2" Grid.Column="0" Content="Каталог для выгрузки" HorizontalAlignment="Right" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Path}" />
        <ToolBarButton Grid.Row="2" Grid.Column="2" ImageSource="Add" ToolTip="Выбор папки" Command="{Binding SelectFolderCommand}" VerticalAlignment="Center" HorizontalAlignment="Left" />
        <Label Grid.Row="3" Grid.Column="0" Content="Выгружаемые сведения" />
        <CheckBox Grid.Row="4" Grid.Column="0" IsChecked="{Binding IsWithJournal}" Content="Журнал событий" Foreground="White" />
        <CheckBox Grid.Row="5" Grid.Column="0" IsChecked="{Binding IsWithPassJournal}" Content="Журнал проходов" Foreground="White" />
        
        <Grid Grid.Row="0" Grid.ColumnSpan="3" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="C" />
            <StackPanel Grid.Column="1" >
                <Calendar SelectionMode="SingleDate" 
                          DisplayDate="{Binding MinDate, UpdateSourceTrigger=PropertyChanged}" SelectedDate="{Binding MinDate, UpdateSourceTrigger=PropertyChanged}" />
                <TimePicker Margin="2" TimeSpan="{Binding MinTime, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <Label Grid.Column="2" Content="До" />
            <StackPanel Grid.Column="3" >
                <Calendar SelectionMode="SingleDate" 
                          DisplayDate="{Binding MaxDate, UpdateSourceTrigger=PropertyChanged}" SelectedDate="{Binding MaxDate, UpdateSourceTrigger=PropertyChanged}" />
                <TimePicker Margin="2" TimeSpan="{Binding MaxTime, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </Grid>
        
    </Grid>
</UserControl>
