<UserControl
	x:Class="SKDModule.Reports.Views.ZonePageView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:component="clr-namespace:System.ComponentModel;assembly=WindowsBase"
	xmlns:langCommon="clr-namespace:Localization.SKD.Common;assembly=Localization"
	xmlns:lang="clr-namespace:Localization.SKD.Views;assembly=Localization"
	d:DesignHeight="600"
	d:DesignWidth="600"
	mc:Ignorable="d">
	<UserControl.Resources>
		<CollectionViewSource x:Key="cvsZones" Source="{Binding Zones}">
			<CollectionViewSource.SortDescriptions>
				<component:SortDescription PropertyName="Item.Name" />
			</CollectionViewSource.SortDescriptions>
		</CollectionViewSource>
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<DataGrid
			ItemsSource="{Binding Source={StaticResource cvsZones}}"
			CanUserSortColumns="True">
			<DataGrid.Columns>
				<DataGridTemplateColumn Header="{x:Static langCommon:CommonResources.Name}" SortMemberPath="Item.Name" Width="0.5*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<CheckBox Margin="2" IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
								<Image Width="16" Margin="1 0" VerticalAlignment="Center" Source="/Controls;component/Images/Zone.png" />
								<TextBlock Text="{Binding Item.Name}" VerticalAlignment="Center"/>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTemplateColumn Header="{x:Static langCommon:CommonResources.Note}" SortMemberPath="Item.Description" Width="*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								Text="{Binding Item.Description}"
								VerticalAlignment="Center" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>
		<StackPanel
			Grid.Row="1"
			Orientation="Horizontal">
			<ToolBarButton
				Command="{Binding SelectAllCommand}"
				ImageSource="SelectAll"
				ToolTip="{x:Static lang:CommonViews.SelectAll}" />
			<ToolBarButton
				Command="{Binding SelectNoneCommand}"
				ImageSource="SelectNone"
				ToolTip="{x:Static lang:CommonViews.ChooseCancel}" />
		</StackPanel>
		<StackPanel
			Grid.Row="2"
			Orientation="Vertical"
			Visibility="{Binding WithDirection, Converter={StaticResource BoolToVisibilityConverter}}">
			<CheckBox
				Margin="3"
				VerticalAlignment="Center"
				Content="{x:Static langCommon:CommonResources.FromZone}"
				Foreground="White"
				IsChecked="{Binding ZoneOut}" />
			<CheckBox
				Margin="3"
				VerticalAlignment="Center"
				Content="{x:Static langCommon:CommonResources.ToZone}"
				Foreground="White"
				IsChecked="{Binding ZoneIn}" />
		</StackPanel>
	</Grid>
</UserControl>
