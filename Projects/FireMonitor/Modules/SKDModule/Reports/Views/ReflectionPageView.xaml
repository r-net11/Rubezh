<UserControl x:Class="SKDModule.Reports.Views.ReflectionPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" >
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<ListView Grid.Row="0" ItemsSource="{Binding Mirror}" SelectedItem="{Binding SelectedMirror}" UIBehavior.ShowSelectionMark="True" UIBehavior.IsActiveSelectionMark="True">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Название">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image Width="16" Margin="1 0" VerticalAlignment="Center" Source ="{Binding ImageSource}" />
									<TextBlock Text="{Binding PresentationName}" />
								</StackPanel>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					<GridViewColumn DisplayMemberBinding="{Binding Mirror.Description}" Header="Примечание" />
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</UserControl>
