<UserControl x:Class="SKDModule.Views.ReportSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Converters="clr-namespace:SKDModule.Converters"
             MinWidth="400" MinHeight="400">

    <UserControl.Resources>
        <Converters:EmployeeReportTypeToStringConverter x:Key="EmployeeReportTypeToStringConverter" />
    </UserControl.Resources>

    <TabControl>
        <TabItem Header="Основные">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Дата начала" Foreground="White" Margin="2" />
                <Calendar Grid.Row="0" Grid.Column="1"
                    SelectedDate="{Binding StartDateTime, UpdateSourceTrigger=PropertyChanged}"
                    DisplayDate="{Binding StartDateTime, UpdateSourceTrigger=PropertyChanged}" 
                    SelectionMode="SingleDate" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата конца" Foreground="White" Margin="2" />
                <Calendar Grid.Row="1" Grid.Column="1"
                    SelectedDate="{Binding EndDateTime, UpdateSourceTrigger=PropertyChanged}"
                    DisplayDate="{Binding EndDateTime, UpdateSourceTrigger=PropertyChanged}" 
                    SelectionMode="SingleDate" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Тип отчета" Foreground="White" Margin="2" />
                <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding AvailableReportTypes}" SelectedItem="{Binding ReportType}" Margin="2">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding ., Converter={StaticResource EmployeeReportTypeToStringConverter}}" Foreground="Black" />
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>