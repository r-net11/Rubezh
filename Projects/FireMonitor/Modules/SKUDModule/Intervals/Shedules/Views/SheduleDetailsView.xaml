<UserControl x:Class="SKDModule.Views.SheduleDetailsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Converters="clr-namespace:SKDModule.Converters"
			 Width="400" Height="400">

	<UserControl.Resources>
		<Converters:EmployeeSheduleTypeToStringConverter x:Key="EmployeeSheduleTypeToStringConverter" />
	</UserControl.Resources>

	<TabControl>
		<TabItem Header="Основные">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<TextBlock Grid.Row="0" Grid.Column="0" Text="Наименование" Foreground="White" Margin="2" />
				<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="2" />

				<CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding IsIgnoreHoliday, UpdateSourceTrigger=PropertyChanged}" Content="Не учитывать праздники" Foreground="White" Margin="2" />
				<CheckBox Grid.Row="2" Grid.Column="1" IsChecked="{Binding IsOnlyFirstEnter, UpdateSourceTrigger=PropertyChanged}" Content="Учитывать только первый вход последний выход" Foreground="White" Margin="2" />

				<TextBlock Grid.Row="3" Grid.Column="0" Text="Схема работы" Foreground="White" Margin="2" />
				<ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding AvailableSheduleTypes}" SelectedItem="{Binding SelectedSheduleType, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="2">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding ., Converter={StaticResource EmployeeSheduleTypeToStringConverter}}" Foreground="Black" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
				<ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding AvailableSheduleScemes}" SelectedItem="{Binding SelectedSheduleSceme, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="2">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Name}" Foreground="Black" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
			</Grid>
		</TabItem>
	</TabControl>
</UserControl>