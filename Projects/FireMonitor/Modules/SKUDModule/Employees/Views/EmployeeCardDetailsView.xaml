<UserControl x:Class="SKDModule.Views.EmployeeCardDetailsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Views="clr-namespace:SKDModule.Views" MinWidth="600" MinHeight="500">

	<TabControl>
		<TabItem Header="Основные">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                    <StackPanel>
                        <TextBlock Text="Начало действия" HorizontalAlignment="Center" Foreground="White" Margin="2" />
                        <Calendar VerticalAlignment="Top" Margin="2"
				            BorderBrush="#66ff9829" BorderThickness="2"
				            DisplayDateStart="{Binding StartDate, Mode=OneTime}"
				            IsTodayHighlighted="False"
				            SelectedDate="{Binding StartDate}"
				            SelectionMode="SingleDate" />
                    </StackPanel>

                    <StackPanel Margin="10 0 0 0">
                        <TextBlock Text="Конец действия" HorizontalAlignment="Center" Foreground="White" Margin="2" />
                        <Calendar VerticalAlignment="Top" Margin="2"
				            BorderBrush="#66ff9829" BorderThickness="2"
				            DisplayDateStart="{Binding EndDate, Mode=OneTime}"
				            IsTodayHighlighted="False"
				            SelectedDate="{Binding EndDate}"
				            SelectionMode="SingleDate" />
                    </StackPanel>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Foreground="White" Text="Семейство" Margin="2" />
                <TextBox Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" IsEnabled="{Binding UseStopList, Converter={StaticResource InverseBooleanConverter}}" Text="{Binding Series}" Width="100" Margin="2" />

                <TextBlock Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" Foreground="White" Text="Номер" Margin="2" />
                <TextBox Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" IsEnabled="{Binding UseStopList, Converter={StaticResource InverseBooleanConverter}}" Text="{Binding Number}" Width="100" Margin="2" />

                <CheckBox Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Content="Из СТОП-листа" Foreground="White" IsChecked="{Binding UseStopList}" Margin="2" />
                <ComboBox Grid.Row="3" Grid.Column="1" IsEnabled="{Binding UseStopList}" ItemsSource="{Binding StopListCards}" SelectedItem="{Binding SelectedStopListCard}" HorizontalAlignment="Left" Width="100" Margin="2">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Margin="1" Text="{Binding PresentationName}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>

                <CheckBox Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Content="От контрольного считывателя" Foreground="White" IsChecked="{Binding UseReader}" Margin="2" />
                <StackPanel Grid.Row="4" Grid.Column="1" Margin="2">
                    <TextBlock TextWrapping="Wrap">
						<Hyperlink Command="{Binding ChangeReaderCommand}" Style="{StaticResource HyperlinkStyle}">
							<TextBlock Text="{Binding ReaderName}" />
						</Hyperlink>
					</TextBlock>
                </StackPanel>

                <CheckBox Grid.Row="5" Grid.Column="1" Content="Заблокирован" IsChecked="{Binding IsBlocked}" Foreground="White" Margin="2" />
            </Grid>
		</TabItem>
		<TabItem Header="Зоны доступа">
			<Views:AccessZonesSelectationView DataContext="{Binding AccessZones}" />
		</TabItem>
		<TabItem Header="Шаблон доступа">
			<DataGrid ItemsSource="{Binding AvailableAccessTemplates}" SelectedItem="{Binding SelectedAccessTemplate}">
				<DataGrid.Columns>
					<DataGridTemplateColumn Width="*" Header="ГУД">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image Margin="1 0 0 0" VerticalAlignment="Center" Source="/Controls;component/Images/zone.png" />
									<TextBlock Width="200" Margin="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding Name}" />
								</StackPanel>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
				</DataGrid.Columns>
			</DataGrid>
		</TabItem>
	</TabControl>
</UserControl>