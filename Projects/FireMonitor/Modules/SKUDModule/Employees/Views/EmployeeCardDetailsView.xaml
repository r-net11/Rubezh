<UserControl x:Class="SKDModule.Views.EmployeeCardDetailsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Views="clr-namespace:SKDModule.Views" MinWidth="600" MinHeight="500">

	<TabControl>
		<TabItem Header="Основные">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Grid.Column="0" Margin="2" HorizontalAlignment="Right" Foreground="White" Text="Начало действия" />
				<Calendar Grid.Row="0" Grid.Column="1" Margin="2" HorizontalAlignment="Left" VerticalAlignment="Top"
				          BorderBrush="#66ff9829" BorderThickness="2"
				          DisplayDateStart="{Binding StartDate,
				                                     Mode=OneTime}"
				          IsTodayHighlighted="False"
				          SelectedDate="{Binding StartDate}"
				          SelectionMode="SingleDate" />

				<TextBlock Grid.Row="1" Grid.Column="0" Margin="2" Foreground="White" Text="Конец действия" />
				<Calendar Grid.Row="1" Grid.Column="1" Margin="2" HorizontalAlignment="Left" VerticalAlignment="Top"
				          BorderBrush="#66ff9829" BorderThickness="2"
				          DisplayDateStart="{Binding EndDate,
				                                     Mode=OneTime}"
				          IsTodayHighlighted="False"
				          SelectedDate="{Binding EndDate}"
				          SelectionMode="SingleDate" />

				<TextBlock Grid.Row="2" Grid.Column="0" Margin="2" HorizontalAlignment="Right" Foreground="White" Text="Семейство" />
				<TextBox Grid.Row="2" Grid.Column="1" Margin="2" HorizontalAlignment="Left" IsEnabled="{Binding UseStopList, Converter={StaticResource InverseBooleanConverter}}" Text="{Binding Series}" />

				<TextBlock Grid.Row="3" Grid.Column="0" Margin="2" HorizontalAlignment="Right" Foreground="White" Text="Номер" />
				<TextBox Grid.Row="3" Grid.Column="1" Margin="2" HorizontalAlignment="Left" IsEnabled="{Binding UseStopList, Converter={StaticResource InverseBooleanConverter}}" Text="{Binding Number}" />

				<CheckBox Grid.Row="4" Grid.Column="0" Margin="2" VerticalAlignment="Center" Content="Из СТОП-листа" Foreground="White" IsChecked="{Binding UseStopList}" />
				<ComboBox Grid.Row="4" Grid.Column="1" IsEnabled="{Binding UseStopList}" ItemsSource="{Binding StopListCards}" SelectedItem="{Binding SelectedStopListCard}">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Margin="2" Text="{Binding PresentationName}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
			</Grid>
		</TabItem>
		<TabItem Header="Зоны доступа">
			<Views:AccessZonesSelectationView DataContext="{Binding AccessZones}" />
		</TabItem>
		<TabItem Header="Шаблон доступа">
			<DataGrid ItemsSource="{Binding AvailableAccessTemplates}" SelectedItem="{Binding SelectedAccessTemplate}">
				<DataGrid.Columns>
					<DataGridTemplateColumn Width="*" Header="ГУД">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<Image Margin="1 0 0 0" VerticalAlignment="Center" Source="/Controls;component/Images/zone.png" />
									<TextBlock Width="200" Margin="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding Name}" />
								</StackPanel>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
				</DataGrid.Columns>
			</DataGrid>
		</TabItem>
	</TabControl>
</UserControl>