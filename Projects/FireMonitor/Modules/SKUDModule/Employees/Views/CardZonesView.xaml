<UserControl x:Class="SKDModule.Views.CardZonesView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:Controls="clr-namespace:Controls;assembly=Controls"
			 xmlns:tree="clr-namespace:Controls.TreeList;assembly=Controls">
	<Grid>
	    <tree:TreeList Grid.Column="0" Focusable="True" SelectedTreeNode="{Binding SelectedZone}" Source="{Binding RootZones}" Margin="2">
		    <tree:TreeList.View>
			    <GridView>
				    <GridViewColumn Width="250" Header="Зона">
					    <GridViewColumn.CellTemplate>
						    <DataTemplate>
							    <StackPanel Orientation="Horizontal">
								    <Image Source="/Controls;component/Images/zone.png" VerticalAlignment="Center" Margin="1 0 0 0" />
								    <TextBlock Text="{Binding Zone.Name}" HorizontalAlignment="Stretch" VerticalAlignment="Center" Width="200" Margin="2">
									    <TextBlock.Style>
										    <Style>
											    <Style.Triggers>
												    <DataTrigger Binding="{Binding IsChecked}" Value="True">
													    <DataTrigger.Setters>
														    <Setter Property="TextBlock.FontWeight" Value="Bold" />
													    </DataTrigger.Setters>
												    </DataTrigger>
											    </Style.Triggers>
										    </Style>
									    </TextBlock.Style>
								    </TextBlock>
							    </StackPanel>
						    </DataTemplate>
					    </GridViewColumn.CellTemplate>
				    </GridViewColumn>
                    <GridViewColumn Width="100" DisplayMemberBinding="{Binding Zone.Description}" Header="Примечание" />
                    <GridViewColumn Header="Защита от передачи" Width="125">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding IsAntiPassback, Converter={StaticResource BoolToYesConverter}}" Margin="2" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Доступ с комиссионированием" Width="195">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding IsAntiPassback, Converter={StaticResource BoolToYesConverter}}" Margin="2" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Временной критерий">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Visibility="{Binding IsChecked, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="{Binding SelectedTimeCreteria.Description}" Margin="0 0 10 0" />
                                    <TextBlock Text="{Binding SelectedTimeType.Name}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
			    </GridView>
		    </tree:TreeList.View>
	    </tree:TreeList>
	</Grid>
</UserControl>