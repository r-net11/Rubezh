<UserControl x:Class="RubezhMonitor.Layout.Views.TimePresenterView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Focusable="False">
	<Border x:Name="Border" Margin="2" Background="{Binding BackgroundBrush}" BorderBrush="{Binding BorderBrush}" BorderThickness="{Binding BorderThickness}" CornerRadius="2">
		<Viewbox Stretch="{Binding Stretch}" HorizontalAlignment="{Binding HorizontalAlignment}" VerticalAlignment="{Binding VerticalAlignment}">
			<TextBlock FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}" FontStyle="{Binding FontStyle}" FontWeight="{Binding FontWeight}" Foreground="{Binding ForegroundBrush}" Text="{Binding Text}" />
		</Viewbox>
	</Border>
</UserControl>