<UserControl x:Class="MultiClient.Views.ServerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="2">
        <StackPanel>
            <TextBlock Text="{Binding Name}" Margin="2" />
            <Button Content="Test" Command="{Binding TestCommand}" Margin="2" Width="100" />
        </StackPanel>
    </Grid>
</UserControl>
