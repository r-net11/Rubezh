<Window x:Class="ManagementConsole.ManagementConsoleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Консоль управления ОПС Firesec-2" MinHeight="500" MinWidth="500" SizeToContent="WidthAndHeight">
    <TabControl>
        <TabItem Header="Автозапуск">
            <StackPanel Background="LightBlue" Margin="1">
                <TextBlock Text="Следующие приложения будут автоматически запускаться при старте системы:" TextWrapping="Wrap" FontSize="12" Margin="2"/>
                <CheckBox Content="Сервер приложений ОПС FireSec-2" IsChecked="{Binding IsServerAuto}" Margin="5,5,5,1" FontSize="12"/>
                <CheckBox Content="OPC Сервер ОПС FireSec-2" IsChecked="{Binding IsOpcServerAuto}" Margin="5 1" FontSize="12"/>
                <CheckBox Content="Агент FireSec-2" IsChecked="{Binding IsFsAgentAuto}" Margin="5 1" FontSize="12"/>
            </StackPanel>
        </TabItem>
        <TabItem Header="Сбор логов">
            <StackPanel Background="LightBlue" Margin="2">
                <TextBox Text="{Binding LogsFolderPath, Mode=OneWay}" Margin="2" />
                <Button Background="#C0C0C0" Content="Собрать логи" Command="{Binding GetLogsCommand}" Height="25" FontSize="13" Margin="2" />
                <Button Background="#C0C0C0" Content="Удалить логи" Command="{Binding RemLogsCommand}" Height="25" FontSize="13" Margin="2" />
            </StackPanel>
        </TabItem>
        <TabItem Header="Настройки" DataContext="{Binding GlobalSettingsViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Адрес сервера" HorizontalAlignment="Right" Margin="2" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Порт сервера" HorizontalAlignment="Right" Margin="2" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Порт агента" HorizontalAlignment="Right" Margin="2" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Автоконнект" HorizontalAlignment="Right" Margin="2" />
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Не перезаписывать Firesec-1" HorizontalAlignment="Right" Margin="2" />
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Путь к VLC" HorizontalAlignment="Right" Margin="2" />
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Модули" HorizontalAlignment="Right" Margin="2" />

                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding RemoteAddress}" Margin="2" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding RemotePort}" Margin="2" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RemoteFSAgentPort}" Margin="2" />
                <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding AutoConnect}" HorizontalAlignment="Left" Margin="2" />
                <CheckBox Grid.Row="4" Grid.Column="1" IsChecked="{Binding DoNotOverrideFS1}" HorizontalAlignment="Left" Margin="2" />
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding LibVlcDllsPath}" Margin="2" />
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Modules}" TextWrapping="Wrap" AcceptsReturn="True" Margin="2" />
                <Button Grid.Row="7" Grid.Column="1" Content="Сохранить" Command="{Binding SaveCommand}" Width="100" HorizontalAlignment="Left" Margin="2" />
            </Grid>
        </TabItem>
    </TabControl>
</Window>