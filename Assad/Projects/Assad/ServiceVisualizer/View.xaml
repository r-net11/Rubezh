<Window x:Class="ServiceVisualizer.View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ServiceVisualizer"
    xmlns:r="http://schemas.aspitalia.com/Ricciolo/Controls"
    Title="View" Height="600" Width="600">
    <StackPanel>
        <Button Content="Connect" Command="{Binding ConnectCommand}" />

        <r:TreeListView ItemsSource="{Binding DevicesViewModels}">
            <r:TreeListView.Columns>
                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="250" />
                <GridViewColumn Header="Address" DisplayMemberBinding="{Binding Address}" Width="100" />
                <GridViewColumn Header="Zone" DisplayMemberBinding="{Binding Zone}" Width="100" />
                <GridViewColumn Header="State" DisplayMemberBinding="{Binding State}" Width="100" />
            </r:TreeListView.Columns>
            <TreeView.ItemContainerStyle>
                <Style>
                    <Setter Property="TreeViewItem.IsExpanded" Value="True"/>
                    <Setter Property="TreeViewItem.IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </r:TreeListView>
        
        <StackPanel DataContext="{Binding SelectedDevicesViewModel}">
            <TextBlock Text="{Binding Name}" />
            <TextBlock Text="{Binding State}" />
            <TextBlock Text="{Binding States}" />
        </StackPanel>

    </StackPanel>
</Window>
