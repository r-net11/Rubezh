<Window x:Class="WpfApplication1.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Window1" Height="300" Width="300">
    <Window.Resources>
        <EventTrigger RoutedEvent="TextBlock.MouseDown" SourceName="textBlock">
            <EventTrigger.Actions>
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="textBlock" Storyboard.TargetProperty="(TextBlock.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:0.5" RepeatBehavior="4x" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger.Actions>
        </EventTrigger>
    </Window.Resources>
    <StackPanel>
        <Button Content="Set Binding" Click="Button_Click" />
        <TextBlock Name="textBlock"
                   Text="Hello World"
                   Margin="0, 100, 0, 0"
                   HorizontalAlignment="Center"
                   FontSize="35"
                   Cursor="Hand">
            <TextBlock.RenderTransform>
                <RotateTransform Angle="45"
                                 CenterX="95"
                                 CenterY="20" />
            </TextBlock.RenderTransform>
            
        </TextBlock>
    </StackPanel>
</Window>
