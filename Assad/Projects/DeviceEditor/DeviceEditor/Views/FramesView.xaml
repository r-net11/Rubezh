<UserControl x:Class="DeviceEditor.FramesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <ListBox ItemsSource ="{Binding FrameViewModels}" SelectedItem="{Binding SelectedFrameViewModel}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <DockPanel>
                    <TextBlock Text="Кадр "/>
                    <TextBlock Text="{Binding Id}"/>
                </DockPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.Resources>
            <ContextMenu x:Key="contextMenu">
                <MenuItem Header="Добавить кадр" Command="{Binding AddFrameCommand}"/>
                <MenuItem Header="Удалить кадр" Command="{Binding RemoveFrameCommand}"/>
            </ContextMenu>
        </ListBox.Resources>
        <ListBox.ItemContainerStyle>
            <Style >
                <Setter Property="ListBoxItem.ContextMenu" Value="{StaticResource contextMenu}" />
            </Style>
        </ListBox.ItemContainerStyle>
    </ListBox>
    
</UserControl>
