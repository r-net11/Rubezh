<Window x:Class="DeviceEditor.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DeviceEditor"
        xmlns:r="http://schemas.aspitalia.com/Ricciolo/Controls"
        xmlns:WpfToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
        Title="View" Height="351" Width="518">
    <Grid>
        <Label Content="Путь к библиотеке устройств" Height="26" HorizontalAlignment="Left" Margin="12,12,0,0" Name="label1" VerticalAlignment="Top" Width="160" />
        <TextBox Height="26" HorizontalAlignment="Left" Margin="194,12,0,0" Name="textBox1" VerticalAlignment="Top" Width="291" />
        <StackPanel Height="248" HorizontalAlignment="Left" Margin="12,54,0,0" Name="stackPanel1" VerticalAlignment="Top" Width="470">
            <ListBox ItemsSource="{Binding DeviceViewModels}" Height="400">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Id}" />
                            <Expander>
                                <ListBox ItemsSource ="{Binding StateViewModels}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Id}" />
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Expander>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>
