<Window x:Class="DeviceEditor.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DeviceEditor"
        xmlns:r="http://schemas.aspitalia.com/Ricciolo/Controls"
        xmlns:WpfToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
        Title="View" Height="351" Width="518">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.RowSpan="2">
            <ScrollViewer>
                <ListBox ItemsSource="{Binding DeviceViewModels}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBox Text="{Binding Id}" />
                                <Expander>
                                    <ListBox ItemsSource ="{Binding StateViewModels}" SelectedItem="{Binding SelectedStateViewModel}">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <TextBox Text="{Binding Id}"/>
                                                    <Expander>
                                                        <StackPanel>
                                                            <ListBox ItemsSource ="{Binding CadrViewModels}" SelectedItem="{Binding SelectedCadrViewModel}">
                                                                <ListBox.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <DockPanel>
                                                                            <TextBlock Text="Кадр "/>
                                                                            <TextBlock Text="{Binding Id}"/>
                                                                        </DockPanel>
                                                                    </DataTemplate>
                                                                </ListBox.ItemTemplate>
                                                                <ListBox.Resources>
                                                                    <ContextMenu x:Key="contextMenu">
                                                                        <MenuItem Header="Добавить кадр" Command="{Binding AddCadrCommand}"/>
                                                                        <MenuItem Header="Удалить кадр" Command="{Binding RemoveCadrCommand}"/>
                                                                    </ContextMenu>
                                                                </ListBox.Resources>
                                                                <ListBox.ItemContainerStyle>
                                                                    <Style >
                                                                        <Setter Property="ListBoxItem.ContextMenu" Value="{StaticResource contextMenu}" />
                                                                    </Style>
                                                                </ListBox.ItemContainerStyle>
                                                            </ListBox>
                                                        </StackPanel>
                                                    </Expander>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        <ListBox.Resources>
                                            <ContextMenu x:Key="contextMenu">
                                                <MenuItem Header="Добавить состояние" Command="{Binding AddStateCommand}"/>
                                                <MenuItem Header="Удалить состояние" Command="{Binding RemoveStateCommand}"/>
                                            </ContextMenu>
                                        </ListBox.Resources>
                                        <ListBox.ItemContainerStyle>
                                            <Style >
                                                <Setter Property="ListBoxItem.ContextMenu" Value="{StaticResource contextMenu}" />
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                    </ListBox>
                                </Expander>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.Resources>
                        <ContextMenu x:Key="contextMenu">
                            <MenuItem Header="Добавить устройство" Command="{Binding AddDeviceCommand}"/>
                            <MenuItem Header="Удалить устройство" Command="{Binding RemoveDeviceCommand}"/>
                        </ContextMenu>
                    </ListBox.Resources>
                    <ListBox.ItemContainerStyle>
                        <Style >
                            <Setter Property="ListBoxItem.ContextMenu" Value="{StaticResource contextMenu}" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </ScrollViewer>
        </Grid>
        <Grid DataContext="{Binding SelectedCadrViewModel}" Grid.Row='0' Grid.Column='1'>
            <ScrollViewer>
                <TextBox Text="{Binding Image, UpdateSourceTrigger=PropertyChanged}" Name="textBox2" TextWrapping="Wrap" />
            </ScrollViewer>
        </Grid>
        <Grid Grid.Row='1' Grid.Column='1' VerticalAlignment="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Row="0" Grid.Column="0">
                <ContentControl Content="{Binding ReaderLoadButton}" HorizontalAlignment="Right" Margin="5">
                    <ContentControl.LayoutTransform>
                        <ScaleTransform ScaleX="0.3" ScaleY="0.3"/>
                    </ContentControl.LayoutTransform>
                </ContentControl>
            </Grid>
            <Grid Grid.Row="0" Grid.Column="1">
                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
                    <TextBox Width="72" Height="22" Name="textBoxDuration" DataContext="{Binding SelectedCadrViewModel}" Text="{Binding Duration}" AcceptsTab="True" AcceptsReturn="True" IsManipulationEnabled="True" IsTabStop="False" />
                    <Button Width="72" Height="22" Command="{Binding StopTimerCommand}">Стоп таймер</Button>
                    <Button Width="72" Height="22" Command="{Binding SaveCommand}">Сохранить</Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
