<UserControl x:Class="RubezhAX.PropertyPage.Views.ComTree"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
<TreeView ItemsSource="{Binding Devices}" SelectedItemChanged="ComServerTreeView_SelectedItemChanged" BorderThickness="0" >

        <TreeView.ItemContainerStyle>
            <Style>
                <Setter Property="TreeViewItem.IsExpanded" Value="True"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=Type}" Value="menu">
                        <Setter Property="TreeViewItem.IsSelected" Value="True"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </TreeView.ItemContainerStyle>

        <TreeView.ItemTemplate>
            <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}" >
                <TextBlock Text="{Binding DeviceName}" />
            </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
</UserControl>
